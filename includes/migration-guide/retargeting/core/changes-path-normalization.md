### <a name="changes-in-path-normalization"></a><span data-ttu-id="b046e-101">路径规范化中的更改</span><span class="sxs-lookup"><span data-stu-id="b046e-101">Changes in path normalization</span></span>

|   |   |
|---|---|
|<span data-ttu-id="b046e-102">详细信息</span><span class="sxs-lookup"><span data-stu-id="b046e-102">Details</span></span>|<span data-ttu-id="b046e-103">从面向 .NET Framework 4.6.2 的应用开始，运行时规范路径的方式发生了变化。路径规范化涉及修改用于标识路径或文件的字符串，使其与目标操作系统上的有效路径一致。</span><span class="sxs-lookup"><span data-stu-id="b046e-103">Starting with apps that target the .NET Framework 4.6.2, the way in which the runtime normalizes paths has changed.Normalizing a path involves modifying the string that identifies a path or file so that it conforms to a valid path on the target operating system.</span></span> <span data-ttu-id="b046e-104">路径规范化通常涉及以下操作：</span><span class="sxs-lookup"><span data-stu-id="b046e-104">Normalization typically involves:</span></span><ul><li><span data-ttu-id="b046e-105">规范化处理组件和目录分隔符。</span><span class="sxs-lookup"><span data-stu-id="b046e-105">Canonicalizing component and directory separators.</span></span></li><li><span data-ttu-id="b046e-106">将当前目录应用到相对路径。</span><span class="sxs-lookup"><span data-stu-id="b046e-106">Applying the current directory to a relative path.</span></span></li><li><span data-ttu-id="b046e-107">评估路径中的相对目录 (.) 或父目录 (..)。</span><span class="sxs-lookup"><span data-stu-id="b046e-107">Evaluating the relative directory (.) or the parent directory (..) in a path.</span></span></li><li><span data-ttu-id="b046e-108">删减指定字符。</span><span class="sxs-lookup"><span data-stu-id="b046e-108">Trimming specified characters.</span></span></li></ul><span data-ttu-id="b046e-109">从面向 .NET Framework 4.6.2 的应用开始，在路径规范化中默认启用以下更改：</span><span class="sxs-lookup"><span data-stu-id="b046e-109">Starting with apps that target the .NET Framework 4.6.2, the following changes in path normalization are enabled by default:</span></span><ul><li><span data-ttu-id="b046e-110">运行时在规范化处理路径时以操作系统的 [GetFullPathName](https://msdn.microsoft.com/library/windows/desktop/aa364963(v=vs.85).aspx) 函数为准。</span><span class="sxs-lookup"><span data-stu-id="b046e-110">The runtime defers to the operating system's [GetFullPathName](https://msdn.microsoft.com/library/windows/desktop/aa364963(v=vs.85).aspx) function to normalize paths.</span></span></li><li><span data-ttu-id="b046e-111">路径规范化再也不用删减目录部分的末尾内容（如目录名称末尾的空格）。</span><span class="sxs-lookup"><span data-stu-id="b046e-111">Normalization no longer involves trimming the end of directory segments (such as a space at the end of a directory name).</span></span></li><li><span data-ttu-id="b046e-112">支持完全信任的设备路径语法，包括 <code>\.&lt;/code&gt; and, for file I/O APIs in mscorlib.dll, <code>\?&lt;/code&gt;.</span><span class="sxs-lookup"><span data-stu-id="b046e-112">Support for device path syntax in full trust, including <code>\.&lt;/code&gt; and, for file I/O APIs in mscorlib.dll, <code>\?&lt;/code&gt;.</span></span></li><li>The runtime does not validate device syntax paths.</li><li>The use of device syntax to access alternate data streams is supported.</li></ul>These changes improve performance while allowing methods to access previously inaccessible paths. Apps that target the .NET Framework 4.6.1 and earlier versions but are running under the .NET Framework 4.6.2 or later are unaffected by this change.|
|<span data-ttu-id="b046e-113">建议</span><span class="sxs-lookup"><span data-stu-id="b046e-113">Suggestion</span></span>|<span data-ttu-id="b046e-114">对于面向 .NET Framework 4.6.2 或更高版本的应用，可通过将以下内容添加到应用程序配置文件的 <code>&lt;runtime&gt;</code> 部分，选择弃用此更改而使用旧版规范化：</span><span class="sxs-lookup"><span data-stu-id="b046e-114">Apps that target the .NET Framework 4.6.2 or later can opt out of this change and use legacy normalization by adding the following to the <code>&lt;runtime&gt;</code> section of the application configuration file:</span></span><pre><code class="lang-xml">&lt;runtime&gt;&#13;&#10;&lt;AppContextSwitchOverrides value=&quot;Switch.System.IO.UseLegacyPathHandling=true&quot; /&gt;&#13;&#10;&lt;/runtime&gt;&#13;&#10;</code></pre><span data-ttu-id="b046e-115">对于面向 .NET Framework 4.6.1 及更低版本，但在 .NET Framework 4.6.2 或更高版本上运行的应用，可通过将以下行添加到应用程序配置文件的 <code>&lt;runtime&gt;</code> 部分，启用对路径规范化的更改：</span><span class="sxs-lookup"><span data-stu-id="b046e-115">Apps that target the .NET Framework 4.6.1 or earlier but are running on the .NET Framework 4.6.2 or later can enable the changes to path normalization by adding the following line to the <code>&lt;runtime&gt;</code> section of the application .configuration file:</span></span><pre><code class="lang-xml">&lt;runtime&gt;&#13;&#10;&lt;AppContextSwitchOverrides value=&quot;Switch.System.IO.UseLegacyPathHandling=false&quot; /&gt;&#13;&#10;&lt;/runtime&gt;&#13;&#10;</code></pre>|
|<span data-ttu-id="b046e-116">范围</span><span class="sxs-lookup"><span data-stu-id="b046e-116">Scope</span></span>|<span data-ttu-id="b046e-117">次要</span><span class="sxs-lookup"><span data-stu-id="b046e-117">Minor</span></span>|
|<span data-ttu-id="b046e-118">版本</span><span class="sxs-lookup"><span data-stu-id="b046e-118">Version</span></span>|<span data-ttu-id="b046e-119">4.6.2</span><span class="sxs-lookup"><span data-stu-id="b046e-119">4.6.2</span></span>|
|<span data-ttu-id="b046e-120">类型</span><span class="sxs-lookup"><span data-stu-id="b046e-120">Type</span></span>|<span data-ttu-id="b046e-121">重定目标</span><span class="sxs-lookup"><span data-stu-id="b046e-121">Retargeting</span></span>|

