---
title: 布拉佐应用程序托管模型
description: 了解托管 Blazor 应用的不同方式，包括在 WebAssembly 上的浏览器或服务器上。
author: danroth27
ms.author: daroth
ms.date: 09/11/2019
ms.openlocfilehash: 77a022b01efba01038790c9601ea03f315a28fdf
ms.sourcegitcommit: d9470d8b2278b33108332c05224d86049cb9484b
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 04/17/2020
ms.locfileid: "81607927"
---
# <a name="blazor-app-hosting-models"></a><span data-ttu-id="c3150-103">布拉佐应用程序托管模型</span><span class="sxs-lookup"><span data-stu-id="c3150-103">Blazor app hosting models</span></span>

[!INCLUDE [book-preview](../../../includes/book-preview.md)]

<span data-ttu-id="c3150-104">Blazor 应用可以像 ASP.NET Web 窗体应用一样托管在 IIS 中。</span><span class="sxs-lookup"><span data-stu-id="c3150-104">Blazor apps can be hosted in IIS just like ASP.NET Web Forms apps.</span></span> <span data-ttu-id="c3150-105">Blazor 应用程序也可以以以下方式之一托管：</span><span class="sxs-lookup"><span data-stu-id="c3150-105">Blazor apps can also be hosted in one of the following ways:</span></span>

- <span data-ttu-id="c3150-106">WebAssembly 浏览器中的客户端。</span><span class="sxs-lookup"><span data-stu-id="c3150-106">Client-side in the browser on WebAssembly.</span></span>
- <span data-ttu-id="c3150-107">ASP.NET核心应用中的服务器端。</span><span class="sxs-lookup"><span data-stu-id="c3150-107">Server-side in an ASP.NET Core app.</span></span>

## <a name="blazor-webassembly-apps"></a><span data-ttu-id="c3150-108">布拉佐尔网络装配应用程序</span><span class="sxs-lookup"><span data-stu-id="c3150-108">Blazor WebAssembly apps</span></span>

<span data-ttu-id="c3150-109">Blazor WebAssembly 应用直接在浏览器中基于 Web 大会的 .NET 运行时执行。</span><span class="sxs-lookup"><span data-stu-id="c3150-109">Blazor WebAssembly apps execute directly in the browser on a WebAssembly-based .NET runtime.</span></span> <span data-ttu-id="c3150-110">Blazor WebAssembly 应用程序的工作方式与前端 JavaScript 框架（如"角"或"反应"）类似。</span><span class="sxs-lookup"><span data-stu-id="c3150-110">Blazor WebAssembly apps function in a similar way to front-end JavaScript frameworks like Angular or React.</span></span> <span data-ttu-id="c3150-111">但是，您编写 C# 时不是编写 JavaScript。</span><span class="sxs-lookup"><span data-stu-id="c3150-111">However, instead of writing JavaScript you write C#.</span></span> <span data-ttu-id="c3150-112">.NET 运行时随应用以及应用程序集和任何必需的依赖项一起下载。</span><span class="sxs-lookup"><span data-stu-id="c3150-112">The .NET runtime is downloaded with the app along with the app assembly and any required dependencies.</span></span> <span data-ttu-id="c3150-113">无需浏览器插件或扩展。</span><span class="sxs-lookup"><span data-stu-id="c3150-113">No browser plugins or extensions are required.</span></span>

<span data-ttu-id="c3150-114">下载的程序集是正常的 .NET 程序集，就像在任何其他 .NET 应用中使用一样。</span><span class="sxs-lookup"><span data-stu-id="c3150-114">The downloaded assemblies are normal .NET assemblies, like you would use in any other .NET app.</span></span> <span data-ttu-id="c3150-115">由于运行时支持 .NET 标准，因此您可以将现有的 .NET 标准库与 Blazor WebAssembly 应用一起使用。</span><span class="sxs-lookup"><span data-stu-id="c3150-115">Because the runtime supports .NET Standard, you can use existing .NET Standard libraries with your Blazor WebAssembly app.</span></span> <span data-ttu-id="c3150-116">但是，这些程序集仍将在浏览器安全沙盒中执行。</span><span class="sxs-lookup"><span data-stu-id="c3150-116">However, these assemblies will still execute in the browser security sandbox.</span></span> <span data-ttu-id="c3150-117">某些功能可能会引发<xref:System.PlatformNotSupportedException>，例如尝试访问文件系统或打开任意网络连接。</span><span class="sxs-lookup"><span data-stu-id="c3150-117">Some functionality may throw a <xref:System.PlatformNotSupportedException>, like trying to access the file system or opening arbitrary network connections.</span></span>

<span data-ttu-id="c3150-118">当应用加载时，将启动 .NET 运行时并指向应用程序集。</span><span class="sxs-lookup"><span data-stu-id="c3150-118">When the app loads, the .NET runtime is started and pointed at the app assembly.</span></span> <span data-ttu-id="c3150-119">应用启动逻辑运行，并呈现根组件。</span><span class="sxs-lookup"><span data-stu-id="c3150-119">The app startup logic runs, and the root components are rendered.</span></span> <span data-ttu-id="c3150-120">Blazor 根据组件呈现的输出计算 UI 更新。</span><span class="sxs-lookup"><span data-stu-id="c3150-120">Blazor calculates the UI updates based on the rendered output from the components.</span></span> <span data-ttu-id="c3150-121">然后应用 DOM 更新。</span><span class="sxs-lookup"><span data-stu-id="c3150-121">The DOM updates are then applied.</span></span>

![Blazor WebAssembly](media/hosting-models/blazor-webassembly.png)

<span data-ttu-id="c3150-123">Blazor WebAssembly 应用程序运行纯客户端。</span><span class="sxs-lookup"><span data-stu-id="c3150-123">Blazor WebAssembly apps run purely client-side.</span></span> <span data-ttu-id="c3150-124">此类应用可以部署到静态网站托管解决方案（如 GitHub 页面或 Azure 静态网站托管）。</span><span class="sxs-lookup"><span data-stu-id="c3150-124">Such apps can be deployed to static site hosting solutions like GitHub Pages or Azure Static Website Hosting.</span></span> <span data-ttu-id="c3150-125">服务器根本不需要 .NET。</span><span class="sxs-lookup"><span data-stu-id="c3150-125">.NET isn't required on the server at all.</span></span> <span data-ttu-id="c3150-126">深度链接到应用的某些部分通常需要服务器上的路由解决方案。</span><span class="sxs-lookup"><span data-stu-id="c3150-126">Deep linking to parts of the app typically requires a routing solution on the server.</span></span> <span data-ttu-id="c3150-127">路由解决方案将请求重定向到应用的根目录。</span><span class="sxs-lookup"><span data-stu-id="c3150-127">The routing solution redirects requests to the root of the app.</span></span> <span data-ttu-id="c3150-128">例如，可以使用 IIS 中的 URL 重写规则来处理此重定向。</span><span class="sxs-lookup"><span data-stu-id="c3150-128">For example, this redirection can be handled using URL rewrite rules in IIS.</span></span>

<span data-ttu-id="c3150-129">要获得 Blazor 和全堆栈 .NET Web 开发的所有优势，请使用 ASP.NET 核心托管 Blazor WebAssembly 应用程序。</span><span class="sxs-lookup"><span data-stu-id="c3150-129">To get all the benefits of Blazor and full-stack .NET web development, host your Blazor WebAssembly app with ASP.NET Core.</span></span> <span data-ttu-id="c3150-130">通过在客户端和服务器上使用 .NET，可以轻松地共享代码并使用一组一致的语言、框架和工具构建应用。</span><span class="sxs-lookup"><span data-stu-id="c3150-130">By using .NET on both the client and server, you can easily share code and build your app using one consistent set of languages, frameworks, and tools.</span></span> <span data-ttu-id="c3150-131">Blazor 提供了方便的模板来设置包含 Blazor WebAssembly 应用和ASP.NET核心主机项目的解决方案。</span><span class="sxs-lookup"><span data-stu-id="c3150-131">Blazor provides convenient templates for setting up a solution that contains both a Blazor WebAssembly app and an ASP.NET Core host project.</span></span> <span data-ttu-id="c3150-132">生成解决方案后，Blazor 应用中生成的静态文件由已设置回退路由的ASP.NET酷睿应用托管。</span><span class="sxs-lookup"><span data-stu-id="c3150-132">When the solution is built, the built static files from the Blazor app are hosted by the ASP.NET Core app with fallback routing already setup.</span></span>

## <a name="blazor-server-apps"></a><span data-ttu-id="c3150-133">布拉佐尔服务器应用程序</span><span class="sxs-lookup"><span data-stu-id="c3150-133">Blazor Server apps</span></span>

<span data-ttu-id="c3150-134">回想一下[Blazor架构](architecture-comparison.md#blazor)讨论，Blazor组件将其输出呈现为称为`RenderTree`的中间抽象。</span><span class="sxs-lookup"><span data-stu-id="c3150-134">Recall from the [Blazor architecture](architecture-comparison.md#blazor) discussion that Blazor components render their output to an intermediate abstraction called a `RenderTree`.</span></span> <span data-ttu-id="c3150-135">然后，Blazor 框架将呈现的内容与以前呈现的内容进行比较。</span><span class="sxs-lookup"><span data-stu-id="c3150-135">The Blazor framework then compares what was rendered with what was previously rendered.</span></span> <span data-ttu-id="c3150-136">差异应用于 DOM。</span><span class="sxs-lookup"><span data-stu-id="c3150-136">The differences are applied to the DOM.</span></span> <span data-ttu-id="c3150-137">Blazor 组件与其呈现输出的应用方式分离。</span><span class="sxs-lookup"><span data-stu-id="c3150-137">Blazor components are decoupled from how their rendered output is applied.</span></span> <span data-ttu-id="c3150-138">因此，组件本身不必在更新 UI 的进程相同的进程中运行。</span><span class="sxs-lookup"><span data-stu-id="c3150-138">Consequently, the components themselves don't have to run in the same process as the process updating the UI.</span></span> <span data-ttu-id="c3150-139">事实上，他们甚至不必在同一台计算机上运行。</span><span class="sxs-lookup"><span data-stu-id="c3150-139">In fact, they don't even have to run on the same machine.</span></span>

<span data-ttu-id="c3150-140">在 Blazor Server 应用中，组件在服务器上运行，而不是在浏览器中客户端运行。</span><span class="sxs-lookup"><span data-stu-id="c3150-140">In Blazor Server apps, the components run on the server instead of client-side in the browser.</span></span> <span data-ttu-id="c3150-141">浏览器中发生的 UI 事件通过实时连接发送到服务器。</span><span class="sxs-lookup"><span data-stu-id="c3150-141">UI events that occur in the browser are sent to the server over a real-time connection.</span></span> <span data-ttu-id="c3150-142">事件将发送到正确的组件实例。</span><span class="sxs-lookup"><span data-stu-id="c3150-142">The events are dispatched to the correct component instances.</span></span> <span data-ttu-id="c3150-143">组件渲染和计算的 UI 差异被序列化并发送到浏览器，并将其应用于 DOM。</span><span class="sxs-lookup"><span data-stu-id="c3150-143">The components render, and the calculated UI diff is serialized and sent to the browser where it's applied to the DOM.</span></span>

![Blazor 服务器](media/hosting-models/blazor-server.png)

<span data-ttu-id="c3150-145">如果您使用了ASP.NETAJAX和控制，Blazor<xref:System.Web.UI.UpdatePanel>服务器托管模型听起来可能很熟悉。</span><span class="sxs-lookup"><span data-stu-id="c3150-145">The Blazor Server hosting model may sound familiar if you've used ASP.NET AJAX and the <xref:System.Web.UI.UpdatePanel> control.</span></span> <span data-ttu-id="c3150-146">控件`UpdatePanel`处理应用部分页面更新以响应以触发页面上的事件。</span><span class="sxs-lookup"><span data-stu-id="c3150-146">The `UpdatePanel` control handles applying partial page updates in response to trigger events on the page.</span></span> <span data-ttu-id="c3150-147">触发时，请求`UpdatePanel`部分更新，然后应用它，而无需刷新页面。</span><span class="sxs-lookup"><span data-stu-id="c3150-147">When triggered, the `UpdatePanel` requests a partial update and then applies it without needing to refresh the page.</span></span> <span data-ttu-id="c3150-148">使用 管理 UI 的状态`ViewState`。</span><span class="sxs-lookup"><span data-stu-id="c3150-148">The state of the UI is managed using `ViewState`.</span></span> <span data-ttu-id="c3150-149">Blazor Server 应用略有不同，因为应用需要与客户端建立活动连接。</span><span class="sxs-lookup"><span data-stu-id="c3150-149">Blazor Server apps are slightly different in that the app requires an active connection with the client.</span></span> <span data-ttu-id="c3150-150">此外，服务器上维护所有 UI 状态。</span><span class="sxs-lookup"><span data-stu-id="c3150-150">Additionally, all UI state is maintained on the server.</span></span> <span data-ttu-id="c3150-151">除了这些差异之外，这两个模型在概念上是相似的。</span><span class="sxs-lookup"><span data-stu-id="c3150-151">Aside from those differences, the two models are conceptually similar.</span></span>

## <a name="how-to-choose-the-right-blazor-hosting-model"></a><span data-ttu-id="c3150-152">如何选择正确的布拉佐托管模式</span><span class="sxs-lookup"><span data-stu-id="c3150-152">How to choose the right Blazor hosting model</span></span>

<span data-ttu-id="c3150-153">如[Blazor 托管模型文档](/aspnet/core/blazor/hosting-models)中所述，不同的 Blazor 托管模型有不同的权衡。</span><span class="sxs-lookup"><span data-stu-id="c3150-153">As described in the [Blazor hosting model docs](/aspnet/core/blazor/hosting-models), the different Blazor hosting models have different tradeoffs.</span></span>

<span data-ttu-id="c3150-154">Blazor WebAssembly 托管模型具有以下优点：</span><span class="sxs-lookup"><span data-stu-id="c3150-154">The Blazor WebAssembly hosting model has the following benefits:</span></span>

- <span data-ttu-id="c3150-155">没有 .NET 服务器端依赖项。</span><span class="sxs-lookup"><span data-stu-id="c3150-155">There's no .NET server-side dependency.</span></span> <span data-ttu-id="c3150-156">应用程序在下载到客户端后已完全正常运行。</span><span class="sxs-lookup"><span data-stu-id="c3150-156">The app is fully functioning after downloaded to the client.</span></span>
- <span data-ttu-id="c3150-157">充分利用客户端资源和功能。</span><span class="sxs-lookup"><span data-stu-id="c3150-157">Client resources and capabilities are fully leveraged.</span></span>
- <span data-ttu-id="c3150-158">工作从服务器卸载到客户端。</span><span class="sxs-lookup"><span data-stu-id="c3150-158">Work is offloaded from the server to the client.</span></span>
- <span data-ttu-id="c3150-159">ASP.NET 酷网络服务器不需要托管应用。</span><span class="sxs-lookup"><span data-stu-id="c3150-159">An ASP.NET Core web server isn't required to host the app.</span></span> <span data-ttu-id="c3150-160">可以使用无服务器部署方案（例如，从 CDN 为应用提供服务）。</span><span class="sxs-lookup"><span data-stu-id="c3150-160">Serverless deployment scenarios are possible (for example, serving the app from a CDN).</span></span>

<span data-ttu-id="c3150-161">Blazor WebAssembly 托管模型的缺点有：</span><span class="sxs-lookup"><span data-stu-id="c3150-161">The downsides of the Blazor WebAssembly hosting model are:</span></span>

- <span data-ttu-id="c3150-162">浏览器功能限制应用。</span><span class="sxs-lookup"><span data-stu-id="c3150-162">Browser capabilities restrict the app.</span></span>
- <span data-ttu-id="c3150-163">需要有能力的客户端硬件和软件（例如，Web 组装支持）。</span><span class="sxs-lookup"><span data-stu-id="c3150-163">Capable client hardware and software (for example, WebAssembly support) is required.</span></span>
- <span data-ttu-id="c3150-164">下载大小较大，并且加载应用需要更长的时间。</span><span class="sxs-lookup"><span data-stu-id="c3150-164">Download size is larger, and apps take longer to load.</span></span>
- <span data-ttu-id="c3150-165">.NET 运行时和工具支持不太成熟。</span><span class="sxs-lookup"><span data-stu-id="c3150-165">.NET runtime and tooling support is less mature.</span></span> <span data-ttu-id="c3150-166">例如[，.NET 标准](../../standard/net-standard.md)支持和调试存在限制。</span><span class="sxs-lookup"><span data-stu-id="c3150-166">For example, there are limitations in [.NET Standard](../../standard/net-standard.md) support and debugging.</span></span>

<span data-ttu-id="c3150-167">相反，Blazor 服务器托管模型具有以下优点：</span><span class="sxs-lookup"><span data-stu-id="c3150-167">Conversely, the Blazor Server hosting model offers the following benefits:</span></span>

- <span data-ttu-id="c3150-168">下载大小比客户端应用小得多，并且应用程序的加载速度也快得多。</span><span class="sxs-lookup"><span data-stu-id="c3150-168">Download size is much smaller than a client-side app, and the app loads much faster.</span></span>
- <span data-ttu-id="c3150-169">该应用程序充分利用了服务器功能，包括使用任何 .NET 核心兼容 API。</span><span class="sxs-lookup"><span data-stu-id="c3150-169">The app takes full advantage of server capabilities, including use of any .NET Core-compatible APIs.</span></span>
- <span data-ttu-id="c3150-170">服务器上的 .NET Core 用于运行应用，因此现有的 .NET 工具（如调试）按预期工作。</span><span class="sxs-lookup"><span data-stu-id="c3150-170">.NET Core on the server is used to run the app, so existing .NET tooling, such as debugging, works as expected.</span></span>
- <span data-ttu-id="c3150-171">支持精简客户端。</span><span class="sxs-lookup"><span data-stu-id="c3150-171">Thin clients are supported.</span></span> <span data-ttu-id="c3150-172">例如，服务器端应用适用于不支持 WebAssembly 和资源受限的设备上的浏览器。</span><span class="sxs-lookup"><span data-stu-id="c3150-172">For example, server-side apps work with browsers that don't support WebAssembly and on resource-constrained devices.</span></span>
- <span data-ttu-id="c3150-173">应用程序的 .NET/C++ 代码库（包括应用的组件代码）不提供给客户端。</span><span class="sxs-lookup"><span data-stu-id="c3150-173">The app's .NET/C# code base, including the app's component code, isn't served to clients.</span></span>

<span data-ttu-id="c3150-174">Blazor 服务器托管模型的缺点有：</span><span class="sxs-lookup"><span data-stu-id="c3150-174">The downsides to the Blazor Server hosting model are:</span></span>

- <span data-ttu-id="c3150-175">更高的 UI 延迟。</span><span class="sxs-lookup"><span data-stu-id="c3150-175">Higher UI latency.</span></span> <span data-ttu-id="c3150-176">每个用户交互都涉及网络跃点。</span><span class="sxs-lookup"><span data-stu-id="c3150-176">Every user interaction involves a network hop.</span></span>
- <span data-ttu-id="c3150-177">没有脱机支持。</span><span class="sxs-lookup"><span data-stu-id="c3150-177">There's no offline support.</span></span> <span data-ttu-id="c3150-178">如果客户端连接失败，应用将停止工作。</span><span class="sxs-lookup"><span data-stu-id="c3150-178">If the client connection fails, the app stops working.</span></span>
- <span data-ttu-id="c3150-179">对于许多用户的应用来说，可扩展性具有挑战性。</span><span class="sxs-lookup"><span data-stu-id="c3150-179">Scalability is challenging for apps with many users.</span></span> <span data-ttu-id="c3150-180">服务器必须管理多个客户端连接并处理客户端状态。</span><span class="sxs-lookup"><span data-stu-id="c3150-180">The server must manage multiple client connections and handle client state.</span></span>
- <span data-ttu-id="c3150-181">需要ASP.NET核心服务器才能为应用提供服务。</span><span class="sxs-lookup"><span data-stu-id="c3150-181">An ASP.NET Core server is required to serve the app.</span></span> <span data-ttu-id="c3150-182">无法使用无服务器部署方案。</span><span class="sxs-lookup"><span data-stu-id="c3150-182">Serverless deployment scenarios aren't possible.</span></span> <span data-ttu-id="c3150-183">例如，不能从 CDN 为应用提供服务。</span><span class="sxs-lookup"><span data-stu-id="c3150-183">For example, you can't serve the app from a CDN.</span></span>

<span data-ttu-id="c3150-184">前面的权衡列表可能很吓人，但您的托管模型可以在以后更改。</span><span class="sxs-lookup"><span data-stu-id="c3150-184">The preceding list of trade-offs may be intimidating, but your hosting model can be changed later.</span></span> <span data-ttu-id="c3150-185">无论选择了何种 Blazor 托管模型，组件模型都是*相同的*。</span><span class="sxs-lookup"><span data-stu-id="c3150-185">Regardless of the Blazor hosting model selected, the component model is *the same*.</span></span> <span data-ttu-id="c3150-186">原则上，相同的组件可用于任一托管模型。</span><span class="sxs-lookup"><span data-stu-id="c3150-186">In principle, the same components can be used with either hosting model.</span></span> <span data-ttu-id="c3150-187">你的应用代码不会更改;但是，你的应用代码不会更改。但是，引入抽象是一个很好的做法，这样您的组件将保持与模型无关的托管。</span><span class="sxs-lookup"><span data-stu-id="c3150-187">Your app code doesn't change; however, it's a good practice to introduce abstractions so that your components stay hosting model-agnostic.</span></span> <span data-ttu-id="c3150-188">这些抽象允许你的应用更容易采用不同的托管模型。</span><span class="sxs-lookup"><span data-stu-id="c3150-188">The abstractions allow your app to more easily adopt a different hosting model.</span></span>

## <a name="deploy-your-app"></a><span data-ttu-id="c3150-189">部署应用</span><span class="sxs-lookup"><span data-stu-id="c3150-189">Deploy your app</span></span>

<span data-ttu-id="c3150-190">ASP.NET Web 窗体应用通常托管在 Windows 服务器计算机或群集上的 IIS 上。</span><span class="sxs-lookup"><span data-stu-id="c3150-190">ASP.NET Web Forms apps are typically hosted on IIS on a Windows Server machine or cluster.</span></span> <span data-ttu-id="c3150-191">Blazor 应用程序还可以：</span><span class="sxs-lookup"><span data-stu-id="c3150-191">Blazor apps can also:</span></span>

- <span data-ttu-id="c3150-192">托管在 IIS 上，作为静态文件或作为ASP.NET核心应用。</span><span class="sxs-lookup"><span data-stu-id="c3150-192">Be hosted on IIS, either as static files or as an ASP.NET Core app.</span></span>
- <span data-ttu-id="c3150-193">利用 ASP.NET Core 的灵活性，托管在各种平台和服务器基础架构上。</span><span class="sxs-lookup"><span data-stu-id="c3150-193">Leverage ASP.NET Core's flexibility to be hosted on various platforms and server infrastructures.</span></span> <span data-ttu-id="c3150-194">例如，您可以在 Linux 上使用[Nginx](/aspnet/core/host-and-deploy/linux-nginx)或[Apache](/aspnet/core/host-and-deploy/linux-apache)托管 Blazor 应用程序。</span><span class="sxs-lookup"><span data-stu-id="c3150-194">For example, you can host a Blazor App using [Nginx](/aspnet/core/host-and-deploy/linux-nginx) or [Apache](/aspnet/core/host-and-deploy/linux-apache) on Linux.</span></span> <span data-ttu-id="c3150-195">有关如何发布和部署 Blazor 应用的详细信息，请参阅 Blazor[托管和部署](/aspnet/core/host-and-deploy/blazor/)文档。</span><span class="sxs-lookup"><span data-stu-id="c3150-195">For more information about how to publish and deploy Blazor apps, see the Blazor [Hosting and deployment](/aspnet/core/host-and-deploy/blazor/) documentation.</span></span>

<span data-ttu-id="c3150-196">在下一节中，我们将介绍如何设置 Blazor WebAssembly 和 Blazor Server 应用的项目。</span><span class="sxs-lookup"><span data-stu-id="c3150-196">In the next section, we'll look at how the projects for Blazor WebAssembly and Blazor Server apps are set up.</span></span>

>[!div class="step-by-step"]
><span data-ttu-id="c3150-197">[上一页](architecture-comparison.md)
>[下一页](project-structure.md)</span><span class="sxs-lookup"><span data-stu-id="c3150-197">[Previous](architecture-comparison.md)
[Next](project-structure.md)</span></span>
