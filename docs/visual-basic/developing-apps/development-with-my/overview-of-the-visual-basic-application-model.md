---
title: Visual Basic 应用程序模型概述
ms.date: 07/20/2015
helpviewer_keywords:
- My.Application object [Visual Basic], Visual Basic application model
- Visual Basic application model
ms.assetid: 17538984-84fe-43c9-82c8-724c9529fe8b
ms.openlocfilehash: 02cc71dbda47d078284d9a2ec07538dfa063ac75
ms.sourcegitcommit: 0be8a279af6d8a43e03141e349d3efd5d35f8767
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 04/18/2019
ms.locfileid: "58819757"
---
# <a name="overview-of-the-visual-basic-application-model"></a><span data-ttu-id="effac-102">Visual Basic 应用程序模型概述</span><span class="sxs-lookup"><span data-stu-id="effac-102">Overview of the Visual Basic Application Model</span></span>
<span data-ttu-id="effac-103">Visual Basic 提供定义完善的模型，用于控制 Windows 窗体应用程序的行为： Visual Basic 应用程序模型。</span><span class="sxs-lookup"><span data-stu-id="effac-103">Visual Basic provides a well-defined model for controlling the behavior of Windows Forms applications: the Visual Basic Application model.</span></span> <span data-ttu-id="effac-104">此模型包括用于处理应用程序的启动和关闭，以及用于捕获未经处理的异常事件的事件。</span><span class="sxs-lookup"><span data-stu-id="effac-104">This model includes events for handling the application's startup and shutdown, as well as events for catching unhandled exceptions.</span></span> <span data-ttu-id="effac-105">它还提供用于开发单实例应用程序的支持。</span><span class="sxs-lookup"><span data-stu-id="effac-105">It also provides support for developing single-instance applications.</span></span> <span data-ttu-id="effac-106">应用程序模型是可扩展的因此需要更多控制的开发人员可以自定义其可重写方法。</span><span class="sxs-lookup"><span data-stu-id="effac-106">The application model is extensible, so developers that need more control can customize its overridable methods.</span></span>  
  
## <a name="uses-for-the-application-model"></a><span data-ttu-id="effac-107">应用程序模型的用途</span><span class="sxs-lookup"><span data-stu-id="effac-107">Uses for the Application Model</span></span>  
 <span data-ttu-id="effac-108">典型的应用程序需要它启动和关闭时执行任务。</span><span class="sxs-lookup"><span data-stu-id="effac-108">A typical application needs to perform tasks when it starts up and shuts down.</span></span> <span data-ttu-id="effac-109">例如，它启动时，应用程序可以显示初始屏幕、 建立数据库连接、 加载已保存的状态等。</span><span class="sxs-lookup"><span data-stu-id="effac-109">For example, when it starts up, the application can display a splash screen, make database connections, load a saved state, and so on.</span></span> <span data-ttu-id="effac-110">当应用程序关闭时，它可以关闭数据库连接、 保存当前状态，等等。</span><span class="sxs-lookup"><span data-stu-id="effac-110">When the application shuts down, it can close database connections, save the current state, and so on.</span></span> <span data-ttu-id="effac-111">此外，应用程序可以执行特定的代码，该应用程序关闭后在的情况下，此类未经处理的异常。</span><span class="sxs-lookup"><span data-stu-id="effac-111">In addition, the application can execute specific code when the application shuts down unexpectedly, such as during an unhandled exception.</span></span>  
  
 <span data-ttu-id="effac-112">Visual Basic 应用程序模型可以轻松创建*单实例*应用程序。</span><span class="sxs-lookup"><span data-stu-id="effac-112">The Visual Basic Application model makes it easy to create a *single-instance* application.</span></span> <span data-ttu-id="effac-113">单实例应用程序不同一次可以正常的应用程序中的运行的应用程序只有一个实例。</span><span class="sxs-lookup"><span data-stu-id="effac-113">A single-instance application differs from a normal application in that only one instance of the application can be running at a time.</span></span> <span data-ttu-id="effac-114">尝试启动单实例应用程序的另一个实例会导致在收到通知的原始实例 — 通过`StartupNextInstance`事件，另一次启动尝试所做的。</span><span class="sxs-lookup"><span data-stu-id="effac-114">An attempt to launch another instance of a single-instance application results in the original instance being notified—by means of the `StartupNextInstance` event—that another launch attempt was made.</span></span> <span data-ttu-id="effac-115">该通知包括后续实例的命令行参数。</span><span class="sxs-lookup"><span data-stu-id="effac-115">The notification includes the subsequent instance's command-line arguments.</span></span> <span data-ttu-id="effac-116">然后才能进行任何初始化，然后关闭应用程序的后续实例。</span><span class="sxs-lookup"><span data-stu-id="effac-116">The subsequent instance of the application is then closed before any initialization can occur.</span></span>  
  
 <span data-ttu-id="effac-117">单实例应用程序启动，并检查它是否为第一个实例或应用程序的后续实例：</span><span class="sxs-lookup"><span data-stu-id="effac-117">A single-instance application starts and checks whether it is the first instance or a subsequent instance of the application:</span></span>  
  
-   <span data-ttu-id="effac-118">如果它是第一个实例，它将正常启动。</span><span class="sxs-lookup"><span data-stu-id="effac-118">If it is the first instance, it starts as usual.</span></span>  
  
-   <span data-ttu-id="effac-119">每个后续尝试启动应用程序，第一个实例时，会导致非常不同的行为。</span><span class="sxs-lookup"><span data-stu-id="effac-119">Each subsequent attempt to start the application, while the first instance runs, results in very different behavior.</span></span> <span data-ttu-id="effac-120">后续尝试通知有关命令行参数，第一个实例，随后立即退出。</span><span class="sxs-lookup"><span data-stu-id="effac-120">The subsequent attempt notifies the first instance about the command-line arguments, and then immediately exits.</span></span> <span data-ttu-id="effac-121">第一个实例句柄`StartupNextInstance`事件来确定后续实例的命令行参数，并继续运行。</span><span class="sxs-lookup"><span data-stu-id="effac-121">The first instance handles the `StartupNextInstance` event to determine what the subsequent instance's command-line arguments were, and continues to run.</span></span>  
  
     <span data-ttu-id="effac-122">此图显示了后续实例发出的第一个实例的信号：</span><span class="sxs-lookup"><span data-stu-id="effac-122">This diagram shows how a subsequent instance signals the first instance:</span></span>  
  
     ![图示显示单实例应用程序映像。](./media/overview-of-the-visual-basic-application-model/single-instance-application.gif)  
  
 <span data-ttu-id="effac-124">通过处理`StartupNextInstance`事件，您可以控制单实例应用程序的行为方式。</span><span class="sxs-lookup"><span data-stu-id="effac-124">By handling the `StartupNextInstance` event, you can control how your single-instance application behaves.</span></span> <span data-ttu-id="effac-125">例如，Microsoft Outlook 通常运行作为单实例应用程序;当 Outlook 正在运行，在尝试启动 Outlook 时同样，重心转移到原始实例，但不会打开另一个实例。</span><span class="sxs-lookup"><span data-stu-id="effac-125">For example, Microsoft Outlook typically runs as a single-instance application; when Outlook is running and you attempt to start Outlook again, focus shifts to the original instance but another instance does not open.</span></span>  
  
## <a name="events-in-the-application-model"></a><span data-ttu-id="effac-126">应用程序模型中的事件</span><span class="sxs-lookup"><span data-stu-id="effac-126">Events in the Application Model</span></span>  
 <span data-ttu-id="effac-127">在应用程序模型中找到以下事件：</span><span class="sxs-lookup"><span data-stu-id="effac-127">The following events are found in the application model:</span></span>  
  
-   <span data-ttu-id="effac-128">**应用程序启动**。</span><span class="sxs-lookup"><span data-stu-id="effac-128">**Application startup**.</span></span> <span data-ttu-id="effac-129">应用程序将引发<xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase.Startup>事件启动时。</span><span class="sxs-lookup"><span data-stu-id="effac-129">The application raises the <xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase.Startup> event when it starts.</span></span> <span data-ttu-id="effac-130">通过处理此事件，你可以添加在加载主窗体之前初始化应用程序的代码。</span><span class="sxs-lookup"><span data-stu-id="effac-130">By handling this event, you can add code that initializes the application before the main form is loaded.</span></span> <span data-ttu-id="effac-131">`Startup`事件还提供了用于取消执行启动过程中，在该阶段的应用程序的必要。</span><span class="sxs-lookup"><span data-stu-id="effac-131">The `Startup` event also provides for canceling execution of the application during that phase of the startup process, if desired.</span></span>  
  
     <span data-ttu-id="effac-132">可以配置要显示的初始屏幕，当应用程序启动代码在运行时的应用程序。</span><span class="sxs-lookup"><span data-stu-id="effac-132">You can configure the application to show a splash screen while the application startup code runs.</span></span> <span data-ttu-id="effac-133">默认情况下，应用程序模型将取消显示初始屏幕上时既`/nosplash`或`-nosplash`使用命令行参数。</span><span class="sxs-lookup"><span data-stu-id="effac-133">By default, the application model suppresses the splash screen when either the `/nosplash` or `-nosplash` command-line argument is used.</span></span>  
  
-   <span data-ttu-id="effac-134">**单实例应用程序**。</span><span class="sxs-lookup"><span data-stu-id="effac-134">**Single-instance applications**.</span></span> <span data-ttu-id="effac-135"><xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase.StartupNextInstance>单实例应用程序的后续实例启动时引发事件。</span><span class="sxs-lookup"><span data-stu-id="effac-135">The <xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase.StartupNextInstance> event is raised when a subsequent instance of a single-instance application starts.</span></span> <span data-ttu-id="effac-136">该事件传递的后续实例的命令行参数。</span><span class="sxs-lookup"><span data-stu-id="effac-136">The event passes the command-line arguments of the subsequent instance.</span></span>  
  
-   <span data-ttu-id="effac-137">**未经处理的异常**。</span><span class="sxs-lookup"><span data-stu-id="effac-137">**Unhandled exceptions**.</span></span> <span data-ttu-id="effac-138">如果应用程序遇到未经处理的异常，它会发出<xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase.UnhandledException>事件。</span><span class="sxs-lookup"><span data-stu-id="effac-138">If the application encounters an unhandled exception, it raises the <xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase.UnhandledException> event.</span></span> <span data-ttu-id="effac-139">该事件的处理程序可以检查异常，并确定是否要继续执行。</span><span class="sxs-lookup"><span data-stu-id="effac-139">Your handler for that event can examine the exception and determine whether to continue execution.</span></span>  
  
     <span data-ttu-id="effac-140">`UnhandledException`不在某些情况下会引发事件。</span><span class="sxs-lookup"><span data-stu-id="effac-140">The `UnhandledException` event is not raised in some circumstances.</span></span> <span data-ttu-id="effac-141">有关详细信息，请参阅 <xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase.UnhandledException>。</span><span class="sxs-lookup"><span data-stu-id="effac-141">For more information, see <xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase.UnhandledException>.</span></span>  
  
-   <span data-ttu-id="effac-142">**网络连接更改**。</span><span class="sxs-lookup"><span data-stu-id="effac-142">**Network-connectivity changes**.</span></span> <span data-ttu-id="effac-143">如果计算机的网络可用性更改，应用程序将引发<xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase.NetworkAvailabilityChanged>事件。</span><span class="sxs-lookup"><span data-stu-id="effac-143">If the computer's network availability changes, the application raises the <xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase.NetworkAvailabilityChanged> event.</span></span>  
  
     <span data-ttu-id="effac-144">`NetworkAvailabilityChanged`不在某些情况下会引发事件。</span><span class="sxs-lookup"><span data-stu-id="effac-144">The `NetworkAvailabilityChanged` event is not raised in some circumstances.</span></span> <span data-ttu-id="effac-145">有关详细信息，请参阅 <xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase.NetworkAvailabilityChanged>。</span><span class="sxs-lookup"><span data-stu-id="effac-145">For more information, see <xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase.NetworkAvailabilityChanged>.</span></span>  
  
-   <span data-ttu-id="effac-146">**应用程序关闭**。</span><span class="sxs-lookup"><span data-stu-id="effac-146">**Application shut down**.</span></span> <span data-ttu-id="effac-147">该应用程序提供<xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase.Shutdown>即将关闭时发出信号通知的事件。</span><span class="sxs-lookup"><span data-stu-id="effac-147">The application provides the <xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase.Shutdown> event to signal when it is about to shut down.</span></span> <span data-ttu-id="effac-148">这种情况下处理程序中，你可以确保，操作你的应用程序需要对其执行 — 关闭并保存，例如 — 都已完成。</span><span class="sxs-lookup"><span data-stu-id="effac-148">In that event handler, you can make sure that the operations your application needs to perform—closing and saving, for example—are completed.</span></span> <span data-ttu-id="effac-149">你可以配置你的应用程序主窗体关闭时，关闭或要关闭的情况下仅当所有窗体关闭。</span><span class="sxs-lookup"><span data-stu-id="effac-149">You can configure your application to shut down when the main form closes, or to shut down only when all forms close.</span></span>  
  
## <a name="availability"></a><span data-ttu-id="effac-150">可用性</span><span class="sxs-lookup"><span data-stu-id="effac-150">Availability</span></span>  
 <span data-ttu-id="effac-151">默认情况下，Visual Basic 应用程序模型是适用于 Windows 窗体项目。</span><span class="sxs-lookup"><span data-stu-id="effac-151">By default, the Visual Basic Application model is available for Windows Forms projects.</span></span> <span data-ttu-id="effac-152">如果应用程序配置为使用不同的启动对象，或使用自定义启动应用程序代码`Sub Main`，则该对象或类可能需要提供的实现<xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase>类，以使用应用程序模型。</span><span class="sxs-lookup"><span data-stu-id="effac-152">If you configure the application to use a different startup object, or start the application code with a custom `Sub Main`, then that object or class may need to provide an implementation of the <xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase> class to use the application model.</span></span> <span data-ttu-id="effac-153">有关更改启动对象的信息，请参阅[应用程序页，项目设计器 (Visual Basic)](/visualstudio/ide/reference/application-page-project-designer-visual-basic)。</span><span class="sxs-lookup"><span data-stu-id="effac-153">For information about changing the startup object, see [Application Page, Project Designer (Visual Basic)](/visualstudio/ide/reference/application-page-project-designer-visual-basic).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="effac-154">请参阅</span><span class="sxs-lookup"><span data-stu-id="effac-154">See also</span></span>

- <xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase>
- <xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase.Startup>
- <xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase.StartupNextInstance>
- <xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase.UnhandledException>
- <xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase.Shutdown>
- <xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase.NetworkAvailabilityChanged>
- <xref:Microsoft.VisualBasic.ApplicationServices.WindowsFormsApplicationBase>
- [<span data-ttu-id="effac-155">扩展 Visual Basic 应用程序模型</span><span class="sxs-lookup"><span data-stu-id="effac-155">Extending the Visual Basic Application Model</span></span>](../../../visual-basic/developing-apps/customizing-extending-my/extending-the-visual-basic-application-model.md)
