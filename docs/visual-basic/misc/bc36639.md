---
title: '&#39;ByRef&#39;参数&#39; &lt;parametername&gt; &#39;不能在 lambda 表达式'
ms.date: 07/20/2015
f1_keywords:
- bc36639
- vbc36639
helpviewer_keywords:
- BC36639
ms.assetid: 5913f9b6-2929-4c05-8dd1-00b10fcd5a83
ms.openlocfilehash: 60322b07a0536c3b7f514699b5de17123aae2250
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 05/04/2018
ms.locfileid: "33631688"
---
# <a name="39byref39-parameter-39ltparameternamegt39-cannot-be-used-in-a-lambda-expression"></a><span data-ttu-id="b235f-102">&#39;ByRef&#39;参数&#39; &lt;parametername&gt; &#39;不能在 lambda 表达式</span><span class="sxs-lookup"><span data-stu-id="b235f-102">&#39;ByRef&#39; parameter &#39;&lt;parametername&gt;&#39; cannot be used in a lambda expression</span></span>
<span data-ttu-id="b235f-103">在 `Sub` 或函数中声明的 lambda 表达式不可使用该 `ByRef` 或函数的任何 `Sub` 参数。</span><span class="sxs-lookup"><span data-stu-id="b235f-103">A lambda expression declared within a `Sub` or function cannot use any `ByRef` parameters of that `Sub` or function.</span></span> <span data-ttu-id="b235f-104">例如，下面的代码会导致此错误，因为在 lambda 表达式中使用了 `ByRef` 参数 `n` 。</span><span class="sxs-lookup"><span data-stu-id="b235f-104">For example, the following code will cause this error because the `ByRef` parameter `n` is used in the lambda expression.</span></span>  
  
```  
'' Not valid.   
'Sub ExampleSub(ByRef n As Integer)  
  
'    Dim lambda = Function(p As Integer) p + n  
  
'End Sub  
```  
  
 <span data-ttu-id="b235f-105">**错误 ID：** BC36639</span><span class="sxs-lookup"><span data-stu-id="b235f-105">**Error ID:** BC36639</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="b235f-106">更正此错误</span><span class="sxs-lookup"><span data-stu-id="b235f-106">To correct this error</span></span>  
  
-   <span data-ttu-id="b235f-107">将 `ByRef` 参数分配到局部变量，并在 lambda 表达式中使用该局部变量，如以下代码所示：</span><span class="sxs-lookup"><span data-stu-id="b235f-107">Assign the `ByRef` parameter to a local variable, and use the local variable in the lambda expression, as shown in the following code:</span></span>  
  
    ```  
    Sub ExampleSub(ByRef n As Integer)  
  
        Dim temp = n  
        Dim lambda = Function(p As Integer) p + temp  
  
    End Sub  
    ```  
  
## <a name="see-also"></a><span data-ttu-id="b235f-108">请参阅</span><span class="sxs-lookup"><span data-stu-id="b235f-108">See Also</span></span>  
 [<span data-ttu-id="b235f-109">Lambda 表达式</span><span class="sxs-lookup"><span data-stu-id="b235f-109">Lambda Expressions</span></span>](../../visual-basic/programming-guide/language-features/procedures/lambda-expressions.md)
