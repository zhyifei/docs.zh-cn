---
title: '&#39;下一步&#39;前面必须是匹配的&#39;为&#39;'
ms.date: 07/20/2015
f1_keywords:
- vbc30092
- bc30092
helpviewer_keywords:
- BC30092
ms.assetid: 4bf49bb2-c69b-443d-aa58-cb40fcfb1370
ms.openlocfilehash: d117fa1a74e0ffec8e9cc32563de0d4c348e83f0
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 05/04/2018
ms.locfileid: "33617439"
---
# <a name="39next39-must-be-preceded-by-a-matching-39for39"></a><span data-ttu-id="c699c-102">&#39;下一步&#39;前面必须是匹配的&#39;为&#39;</span><span class="sxs-lookup"><span data-stu-id="c699c-102">&#39;Next&#39; must be preceded by a matching &#39;For&#39;</span></span>
<span data-ttu-id="c699c-103">出现 `Next` 语句而没有相应的 `For` 或 `For Each` 语句。</span><span class="sxs-lookup"><span data-stu-id="c699c-103">A `Next` statement occurs without a corresponding `For` or `For Each` statement.</span></span> <span data-ttu-id="c699c-104">`Next` 前面必须有相应的 `For` 或 `For Each` 语句。</span><span class="sxs-lookup"><span data-stu-id="c699c-104">`Next` must be preceded by a corresponding `For` or `For Each` statement.</span></span>  
  
 <span data-ttu-id="c699c-105">**错误 ID：** BC30092</span><span class="sxs-lookup"><span data-stu-id="c699c-105">**Error ID:** BC30092</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="c699c-106">更正此错误</span><span class="sxs-lookup"><span data-stu-id="c699c-106">To correct this error</span></span>  
  
1.  <span data-ttu-id="c699c-107">如果此 `For` 循环是一组嵌套 `For` 循环的一部分，请确保每个循环正常终止。</span><span class="sxs-lookup"><span data-stu-id="c699c-107">If this `For` loop is part of a set of nested `For` loops, make sure each loop is properly terminated.</span></span>  
  
2.  <span data-ttu-id="c699c-108">验证 `For` 循环内的其他控制结构是否被正确终止。</span><span class="sxs-lookup"><span data-stu-id="c699c-108">Verify that other control structures within the `For` loop are correctly terminated.</span></span>  
  
3.  <span data-ttu-id="c699c-109">确保此 `For` 循环的格式正确。</span><span class="sxs-lookup"><span data-stu-id="c699c-109">Ensure that this `For` loop is correctly formatted.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="c699c-110">请参阅</span><span class="sxs-lookup"><span data-stu-id="c699c-110">See Also</span></span>  
 [<span data-ttu-id="c699c-111">For...Next 语句</span><span class="sxs-lookup"><span data-stu-id="c699c-111">For...Next Statement</span></span>](../../visual-basic/language-reference/statements/for-next-statement.md)  
 [<span data-ttu-id="c699c-112">For Each...Next 语句</span><span class="sxs-lookup"><span data-stu-id="c699c-112">For Each...Next Statement</span></span>](../../visual-basic/language-reference/statements/for-each-next-statement.md)
