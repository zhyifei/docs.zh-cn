---
title: 类的<classname>必须声明一个 Sub New，因为 '<constructorname>在其基本类<baseclassname>标记为已过时
ms.date: 07/20/2015
f1_keywords:
- vbc30917
- bc30917
helpviewer_keywords:
- BC30917
ms.assetid: 764d222d-e058-40ad-a354-29b956a8027b
ms.openlocfilehash: d68dbeb82f73663bce63252faf60e50763a6c9e2
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: HT
ms.contentlocale: zh-CN
ms.lasthandoff: 04/23/2019
ms.locfileid: "61768254"
---
# <a name="class-classname-must-declare-a-sub-new-because-the-constructorname-in-its-base-class-baseclassname-is-marked-obsolete"></a><span data-ttu-id="874a1-102">类的\<类名 > 必须声明一个 Sub New，因为 '\<constructorname > 在其基类的\<a m e > 标记为已过时</span><span class="sxs-lookup"><span data-stu-id="874a1-102">Class '\<classname>' must declare a 'Sub New' because the '\<constructorname>' in its base class '\<baseclassname>' is marked obsolete</span></span>
<span data-ttu-id="874a1-103">类声明不包括构造函数，并且基类构造函数标记有 <xref:System.ObsoleteAttribute> 特性和一个将其视为一个错误的指令。</span><span class="sxs-lookup"><span data-stu-id="874a1-103">A class declaration does not include a constructor, and the base class constructor is marked with the <xref:System.ObsoleteAttribute> attribute and the directive to treat it as an error.</span></span>  
  
 <span data-ttu-id="874a1-104">当派生的类没有声明构造函数时，尝试生成隐式无参数构造函数调用的 Visual Basic `MyBase.New()`。</span><span class="sxs-lookup"><span data-stu-id="874a1-104">When a derived class does not declare a constructor, Visual Basic attempts to generate an implicit parameterless constructor that calls `MyBase.New()`.</span></span> <span data-ttu-id="874a1-105">如果可以调用不带参数的基类中没有任何可访问的构造函数，Visual Basic 无法生成隐式构造函数。</span><span class="sxs-lookup"><span data-stu-id="874a1-105">If there is no accessible constructor in the base class that can be called without arguments, Visual Basic cannot generate an implicit constructor.</span></span> <span data-ttu-id="874a1-106">在这种情况下，所需的构造函数标有<xref:System.ObsoleteAttribute>属性，因此 Visual Basic 不能调用它。</span><span class="sxs-lookup"><span data-stu-id="874a1-106">In this case, the required constructor is marked with the <xref:System.ObsoleteAttribute> attribute, so Visual Basic cannot call it.</span></span>  
  
 <span data-ttu-id="874a1-107">可以通过将 <xref:System.ObsoleteAttribute> 应用于任意编程元素，将其标记为不再使用。</span><span class="sxs-lookup"><span data-stu-id="874a1-107">You can mark any programming element as being no longer in use by applying <xref:System.ObsoleteAttribute> to it.</span></span> <span data-ttu-id="874a1-108">如果执行此操作，则可以将特性的 <xref:System.ObsoleteAttribute.IsError%2A> 属性设置为 `True` 或 `False`。</span><span class="sxs-lookup"><span data-stu-id="874a1-108">If you do this, you can set the attribute's <xref:System.ObsoleteAttribute.IsError%2A> property to either `True` or `False`.</span></span> <span data-ttu-id="874a1-109">如果设置为 `True`，则编译器将尝试使用该元素的操作视为错误。</span><span class="sxs-lookup"><span data-stu-id="874a1-109">If you set it to `True`, the compiler treats an attempt to use the element as an error.</span></span> <span data-ttu-id="874a1-110">如果设置为 `False`，或让它默认为 `False`，则编译器会在尝试使用该元素时发出警告。</span><span class="sxs-lookup"><span data-stu-id="874a1-110">If you set it to `False`, or let it default to `False`, the compiler issues a warning if there is an attempt to use the element.</span></span>  
  
 <span data-ttu-id="874a1-111">**错误 ID:** BC30917</span><span class="sxs-lookup"><span data-stu-id="874a1-111">**Error ID:** BC30917</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="874a1-112">更正此错误</span><span class="sxs-lookup"><span data-stu-id="874a1-112">To correct this error</span></span>  
  
- <span data-ttu-id="874a1-113">使用 `Sub New` 在派生类中声明一个构造函数。</span><span class="sxs-lookup"><span data-stu-id="874a1-113">Use `Sub New` to declare a constructor in the derived class.</span></span>