---
title: "&#39;继续 &#39;后面必须跟 &#39; 执行 &#39;，&#39;有关 &#39;或 &#39;虽然 &#39;"
ms.date: 07/20/2015
ms.prod: .net
ms.technology: devlang-visual-basic
ms.topic: article
f1_keywords:
- bc30781
- vbc30781
helpviewer_keywords: BC30781
ms.assetid: a0d5854d-ca44-4c6b-9458-4fc50d29281a
caps.latest.revision: "9"
author: dotnet-bot
ms.author: dotnetcontent
ms.openlocfilehash: 02b43ac0a4b6db4786a20dc3e21558a4e1a87af3
ms.sourcegitcommit: bd1ef61f4bb794b25383d3d72e71041a5ced172e
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 10/18/2017
---
# <a name="39continue39-must-be-followed-by-39do39-39for39-or-39while39"></a><span data-ttu-id="fbf3d-102">&#39;继续 &#39;后面必须跟 &#39; 执行 &#39;，&#39;有关 &#39;或 &#39;虽然 &#39;</span><span class="sxs-lookup"><span data-stu-id="fbf3d-102">&#39;Continue&#39; must be followed by &#39;Do&#39;, &#39;For&#39; or &#39;While&#39;</span></span>
<span data-ttu-id="fbf3d-103">一个 `Continue` 语句后面必须接 `Do`、 `For`或 `While`，这取决于 `Continue` 语句是否出现在 `Do...Loop` 循环、 `For...Next` 循环或是 `While...End While` 循环中。</span><span class="sxs-lookup"><span data-stu-id="fbf3d-103">A `Continue` statement must be followed by `Do`, `For`, or `While`, depending on if the `Continue` statement appears within a `Do...Loop` loop, `For...Next` loop, or `While...End While` loop.</span></span>  
  
 <span data-ttu-id="fbf3d-104">**错误 ID：** BC30781</span><span class="sxs-lookup"><span data-stu-id="fbf3d-104">**Error ID:** BC30781</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="fbf3d-105">更正此错误</span><span class="sxs-lookup"><span data-stu-id="fbf3d-105">To correct this error</span></span>  
  
1.  <span data-ttu-id="fbf3d-106">如果 `Continue` 语句在 `Do...Loop` 循环中，请将该语句更改为 `Continue Do`。</span><span class="sxs-lookup"><span data-stu-id="fbf3d-106">If the `Continue` statement is in a `Do...Loop` loop, change the statement to `Continue Do`.</span></span>  
  
2.  <span data-ttu-id="fbf3d-107">如果 `Continue` 语句在 `For...Next` 循环中，请将该语句更改为 `Continue For`。</span><span class="sxs-lookup"><span data-stu-id="fbf3d-107">If the `Continue` statement is in a `For...Next` loop, change the statement to `Continue For`.</span></span>  
  
3.  <span data-ttu-id="fbf3d-108">如果 `Continue` 语句在 `While...End While` 循环中，请将该语句更改为 `Continue While`。</span><span class="sxs-lookup"><span data-stu-id="fbf3d-108">If the `Continue` statement is in a `While...End While` loop, change the statement to `Continue While`.</span></span>  
  
4.  <span data-ttu-id="fbf3d-109">否则，请删除 `Continue` 语句。</span><span class="sxs-lookup"><span data-stu-id="fbf3d-109">Otherwise, remove the `Continue` statement.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="fbf3d-110">另请参阅</span><span class="sxs-lookup"><span data-stu-id="fbf3d-110">See Also</span></span>  
 [<span data-ttu-id="fbf3d-111">Continue 语句</span><span class="sxs-lookup"><span data-stu-id="fbf3d-111">Continue Statement</span></span>](../../visual-basic/language-reference/statements/continue-statement.md)
