---
title: '&#39;继续&#39;后面必须跟&#39;执行&#39;，&#39;为&#39;或&#39;时&#39;'
ms.date: 07/20/2015
f1_keywords:
- bc30781
- vbc30781
helpviewer_keywords:
- BC30781
ms.assetid: a0d5854d-ca44-4c6b-9458-4fc50d29281a
ms.openlocfilehash: b5c20fc824db331357a08d069caff12ad02146d3
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 05/04/2018
ms.locfileid: "33617363"
---
# <a name="39continue39-must-be-followed-by-39do39-39for39-or-39while39"></a><span data-ttu-id="216a9-102">&#39;继续&#39;后面必须跟&#39;执行&#39;，&#39;为&#39;或&#39;时&#39;</span><span class="sxs-lookup"><span data-stu-id="216a9-102">&#39;Continue&#39; must be followed by &#39;Do&#39;, &#39;For&#39; or &#39;While&#39;</span></span>
<span data-ttu-id="216a9-103">一个 `Continue` 语句后面必须接 `Do`、 `For`或 `While`，这取决于 `Continue` 语句是否出现在 `Do...Loop` 循环、 `For...Next` 循环或是 `While...End While` 循环中。</span><span class="sxs-lookup"><span data-stu-id="216a9-103">A `Continue` statement must be followed by `Do`, `For`, or `While`, depending on if the `Continue` statement appears within a `Do...Loop` loop, `For...Next` loop, or `While...End While` loop.</span></span>  
  
 <span data-ttu-id="216a9-104">**错误 ID：** BC30781</span><span class="sxs-lookup"><span data-stu-id="216a9-104">**Error ID:** BC30781</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="216a9-105">更正此错误</span><span class="sxs-lookup"><span data-stu-id="216a9-105">To correct this error</span></span>  
  
1.  <span data-ttu-id="216a9-106">如果 `Continue` 语句在 `Do...Loop` 循环中，请将该语句更改为 `Continue Do`。</span><span class="sxs-lookup"><span data-stu-id="216a9-106">If the `Continue` statement is in a `Do...Loop` loop, change the statement to `Continue Do`.</span></span>  
  
2.  <span data-ttu-id="216a9-107">如果 `Continue` 语句在 `For...Next` 循环中，请将该语句更改为 `Continue For`。</span><span class="sxs-lookup"><span data-stu-id="216a9-107">If the `Continue` statement is in a `For...Next` loop, change the statement to `Continue For`.</span></span>  
  
3.  <span data-ttu-id="216a9-108">如果 `Continue` 语句在 `While...End While` 循环中，请将该语句更改为 `Continue While`。</span><span class="sxs-lookup"><span data-stu-id="216a9-108">If the `Continue` statement is in a `While...End While` loop, change the statement to `Continue While`.</span></span>  
  
4.  <span data-ttu-id="216a9-109">否则，请删除 `Continue` 语句。</span><span class="sxs-lookup"><span data-stu-id="216a9-109">Otherwise, remove the `Continue` statement.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="216a9-110">请参阅</span><span class="sxs-lookup"><span data-stu-id="216a9-110">See Also</span></span>  
 [<span data-ttu-id="216a9-111">Continue 语句</span><span class="sxs-lookup"><span data-stu-id="216a9-111">Continue Statement</span></span>](../../visual-basic/language-reference/statements/continue-statement.md)
