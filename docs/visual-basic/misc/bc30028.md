---
title: "&#39; #else 和 #39;前面必须是匹配的 #39; #If &#39;或 &#39; #ElseIf &#39;"
ms.date: 07/20/2015
ms.prod: .net
ms.technology: devlang-visual-basic
ms.topic: article
f1_keywords:
- vbc30028
- bc30028
helpviewer_keywords: BC30028
ms.assetid: c6ed34de-d6ed-4227-9880-538055aff20a
caps.latest.revision: "12"
author: dotnet-bot
ms.author: dotnetcontent
ms.openlocfilehash: a64a06505996df3c7f244c617c5f4744504ae6af
ms.sourcegitcommit: bd1ef61f4bb794b25383d3d72e71041a5ced172e
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 10/18/2017
---
# <a name="39else39-must-be-preceded-by-a-matching-39if39-or-39elseif39"></a><span data-ttu-id="2e137-102">&#39; #else 和 #39;前面必须是匹配的 #39; #If &#39;或 &#39; #ElseIf &#39;</span><span class="sxs-lookup"><span data-stu-id="2e137-102">&#39;#Else&#39; must be preceded by a matching &#39;#If&#39; or &#39;#ElseIf&#39;</span></span>
<span data-ttu-id="2e137-103">`#Else` 是条件编译指令。</span><span class="sxs-lookup"><span data-stu-id="2e137-103">`#Else` is a conditional compilation directive.</span></span> <span data-ttu-id="2e137-104">`#Else` 指令前面没有相应的 `#If` 或 `#ElseIf` 指令。</span><span class="sxs-lookup"><span data-stu-id="2e137-104">An `#Else` directive is not preceded by a corresponding `#If` or `#ElseIf` directive.</span></span>  
  
 <span data-ttu-id="2e137-105">**错误 ID:** BC30028</span><span class="sxs-lookup"><span data-stu-id="2e137-105">**Error ID:** BC30028</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="2e137-106">更正此错误</span><span class="sxs-lookup"><span data-stu-id="2e137-106">To correct this error</span></span>  
  
1.  <span data-ttu-id="2e137-107">检查插入型条件编译块或放置有误的 `#If` 是否未从此 `#ElseIf` 中分隔出前置 `#Else` 或 `#End If`。</span><span class="sxs-lookup"><span data-stu-id="2e137-107">Check that a preceding `#If` or `#ElseIf` is not separated from this `#Else` by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
2.  <span data-ttu-id="2e137-108">检查 `#Else` 前面是否有另一条 `#Else` 指令。</span><span class="sxs-lookup"><span data-stu-id="2e137-108">Check that `#Else` is preceded by another `#Else` directive.</span></span> <span data-ttu-id="2e137-109">如果有，请删除 `#Else` 或将其更改为 `#ElseIf`。</span><span class="sxs-lookup"><span data-stu-id="2e137-109">If it is, either remove `#Else` or change it to an `#ElseIf`.</span></span>  
  
3.  <span data-ttu-id="2e137-110">如果其他一切正常，请将 `#If` 指令添加到条件编译块的开头。</span><span class="sxs-lookup"><span data-stu-id="2e137-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="2e137-111">另请参阅</span><span class="sxs-lookup"><span data-stu-id="2e137-111">See Also</span></span>  
 [<span data-ttu-id="2e137-112">#If...Then...#Else 指令</span><span class="sxs-lookup"><span data-stu-id="2e137-112">#If...Then...#Else Directives</span></span>](../../visual-basic/language-reference/directives/if-then-else-directives.md)
