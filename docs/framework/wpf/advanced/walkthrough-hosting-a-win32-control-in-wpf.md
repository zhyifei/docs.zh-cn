---
title: 演练：在 WPF 中承载 Win32 控件
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
helpviewer_keywords:
- hosting Win32 control in WPF [WPF]
- Win32 code [WPF], WPF interoperation
ms.assetid: a676b1eb-fc55-4355-93ab-df840c41cea0
ms.openlocfilehash: cc27189afd2185d5f0a1eeacccf4c537dd3d9c2e
ms.sourcegitcommit: 68653db98c5ea7744fd438710248935f70020dfb
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 08/22/2019
ms.locfileid: "69917546"
---
# <a name="walkthrough-hosting-a-win32-control-in-wpf"></a><span data-ttu-id="5a3ed-102">演练：在 WPF 中承载 Win32 控件</span><span class="sxs-lookup"><span data-stu-id="5a3ed-102">Walkthrough: Hosting a Win32 Control in WPF</span></span>
<span data-ttu-id="5a3ed-103">Windows Presentation Foundation (WPF) 提供了一个用于创建应用程序的丰富环境。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-103">Windows Presentation Foundation (WPF) provides a rich environment for creating applications.</span></span> <span data-ttu-id="5a3ed-104">但是, 当你在 Win32 代码中有大量投资时, 在 WPF 应用程序中重复使用至少一些代码, 而不是完全重新编写它, 这可能更有效。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-104">However, when you have a substantial investment in Win32 code, it may be more effective to reuse at least some of that code in your WPF application rather than rewrite it completely.</span></span> <span data-ttu-id="5a3ed-105">WPF 提供了一种用于在 WPF 页上承载 Win32 窗口的简单机制。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-105">WPF provides a straightforward mechanism for hosting a Win32 window, on a WPF page.</span></span>  
  
 <span data-ttu-id="5a3ed-106">本主题将指导你完成承载 Win32 列表框控件的应用程序, 该应用程序[在 WPF 示例中承载 Win32 ListBox 控件](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control)。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-106">This topic walks you through an application, [Hosting a Win32 ListBox Control in WPF Sample](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control), that hosts a Win32 list box control.</span></span> <span data-ttu-id="5a3ed-107">可以扩展此常规过程以承载任何 Win32 窗口。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-107">This general procedure can be extended to hosting any Win32 window.</span></span>  

<a name="requirements"></a>   
## <a name="requirements"></a><span data-ttu-id="5a3ed-108">要求</span><span class="sxs-lookup"><span data-stu-id="5a3ed-108">Requirements</span></span>  
 <span data-ttu-id="5a3ed-109">本主题假定基本熟悉 WPF 和 Windows API 编程。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-109">This topic assumes a basic familiarity with both WPF and Windows API programming.</span></span> <span data-ttu-id="5a3ed-110">有关 WPF 编程的基本简介, 请参阅[入门](../getting-started/index.md)。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-110">For a basic introduction to WPF programming, see [Getting Started](../getting-started/index.md).</span></span> <span data-ttu-id="5a3ed-111">有关 Windows API 编程的简介, 请参阅主题中的任意一本书, 该主题中的 Charles Petzold。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-111">For an introduction to Windows API programming, see any of the numerous books on the subject, in particular *Programming Windows* by Charles Petzold.</span></span>  
  
 <span data-ttu-id="5a3ed-112">由于本主题附带的示例是在中C#实现的, 因此它使用平台调用服务 (PInvoke) 来访问 Windows API。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-112">Because the sample that accompanies this topic is implemented in C#, it makes use of Platform Invocation Services (PInvoke) to access the Windows API.</span></span> <span data-ttu-id="5a3ed-113">某些对 PInvoke 的熟悉非常有用, 但并不重要。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-113">Some familiarity with PInvoke is helpful but not essential.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="5a3ed-114">本主题包括来自相关示例的一些代码示例。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-114">This topic includes a number of code examples from the associated sample.</span></span> <span data-ttu-id="5a3ed-115">但是，出于可读性考虑，不包括完整的示例代码。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-115">However, for readability, it does not include the complete sample code.</span></span> <span data-ttu-id="5a3ed-116">你可以从[在 WPF 示例中承载 Win32 ListBox 控件](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control)获取或查看完整的代码。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-116">You can obtain or view complete code from [Hosting a Win32 ListBox Control in WPF Sample](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control).</span></span>  
  
<a name="basic_procedure"></a>   
## <a name="the-basic-procedure"></a><span data-ttu-id="5a3ed-117">基本过程</span><span class="sxs-lookup"><span data-stu-id="5a3ed-117">The Basic Procedure</span></span>  
 <span data-ttu-id="5a3ed-118">本部分概述了在 WPF 页上承载 Win32 窗口的基本过程。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-118">This section outlines the basic procedure for hosting a Win32 window on a WPF page.</span></span> <span data-ttu-id="5a3ed-119">其余各节介绍了每个步骤的详细内容。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-119">The remaining sections go through the details of each step.</span></span>  
  
 <span data-ttu-id="5a3ed-120">基本的承载步骤如下：</span><span class="sxs-lookup"><span data-stu-id="5a3ed-120">The basic hosting procedure is:</span></span>  
  
1. <span data-ttu-id="5a3ed-121">实现 WPF 页以承载窗口。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-121">Implement a WPF page to host the window.</span></span> <span data-ttu-id="5a3ed-122">一种方法是创建一个<xref:System.Windows.Controls.Border>元素, 以便为所承载的窗口保留部分页面。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-122">One technique is to create a <xref:System.Windows.Controls.Border> element to reserve a section of the page for the hosted window.</span></span>  
  
2. <span data-ttu-id="5a3ed-123">实现一个类以承载从<xref:System.Windows.Interop.HwndHost>继承的控件。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-123">Implement a class to host the control that inherits from <xref:System.Windows.Interop.HwndHost>.</span></span>  
  
3. <span data-ttu-id="5a3ed-124">在该类中, 重写<xref:System.Windows.Interop.HwndHost>类成员<xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-124">In that class, override the <xref:System.Windows.Interop.HwndHost> class member <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>.</span></span>  
  
4. <span data-ttu-id="5a3ed-125">将所承载的窗口创建为包含 WPF 页的窗口的子级。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-125">Create the hosted window as a child of the window that contains the WPF page.</span></span> <span data-ttu-id="5a3ed-126">尽管传统的 WPF 编程不需要显式使用它, 但宿主页是一个带有句柄 (HWND) 的窗口。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-126">Although conventional WPF programming does not need to explicitly make use of it, the hosting page is a window with a handle (HWND).</span></span> <span data-ttu-id="5a3ed-127">通过`hwndParent` <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>方法的参数接收页 HWND。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-127">You receive the page HWND through the `hwndParent` parameter of the <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A> method.</span></span> <span data-ttu-id="5a3ed-128">应将所承载的窗口创建为此 HWND 的子窗口。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-128">The hosted window should be created as a child of this HWND.</span></span>  
  
5. <span data-ttu-id="5a3ed-129">在创建宿主窗口之后，返回所承载的窗口的 HWND。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-129">Once you have created the host window, return the HWND of the hosted window.</span></span> <span data-ttu-id="5a3ed-130">如果要承载一个或多个 Win32 控件, 通常需要将宿主窗口创建为 HWND 的子级, 并使控件成为该宿主窗口的子级。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-130">If you want to host one or more Win32 controls, you typically create a host window as a child of the HWND and make the controls children of that host window.</span></span> <span data-ttu-id="5a3ed-131">在宿主窗口中包装控件提供了一种简单的方法, 使您的 WPF 页可以从控件接收通知, 这些控件处理跨 HWND 边界的通知的某些特定 Win32 问题。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-131">Wrapping the controls in a host window provides a simple way for your WPF page to receive notifications from the controls, which deals with some particular Win32 issues with notifications across the HWND boundary.</span></span>  
  
6. <span data-ttu-id="5a3ed-132">处理发送到宿主窗口的选定消息，例如，来自子控件的通知。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-132">Handle selected messages sent to the host window, such as notifications from child controls.</span></span> <span data-ttu-id="5a3ed-133">有两种方法可以实现此目的。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-133">There are two ways to do this.</span></span>  
  
    - <span data-ttu-id="5a3ed-134">如果更喜欢处理宿主类中的消息, 请重写<xref:System.Windows.Interop.HwndHost.WndProc%2A> <xref:System.Windows.Interop.HwndHost>类的方法。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-134">If you prefer to handle messages in your hosting class, override the <xref:System.Windows.Interop.HwndHost.WndProc%2A> method of the <xref:System.Windows.Interop.HwndHost> class.</span></span>  
  
    - <span data-ttu-id="5a3ed-135">如果想让 WPF 处理消息, 请在代码隐藏中处理<xref:System.Windows.Interop.HwndHost>类<xref:System.Windows.Interop.HwndHost.MessageHook>事件。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-135">If you prefer to have the WPF handle the messages, handle the <xref:System.Windows.Interop.HwndHost> class <xref:System.Windows.Interop.HwndHost.MessageHook> event in your code-behind.</span></span> <span data-ttu-id="5a3ed-136">对于所承载的窗口收到的每条消息，都将发生此事件。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-136">This event occurs for every message that is received by the hosted window.</span></span> <span data-ttu-id="5a3ed-137">如果选择此选项, 则仍必须重写<xref:System.Windows.Interop.HwndHost.WndProc%2A>, 但只需要最小实现。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-137">If you choose this option, you must still override <xref:System.Windows.Interop.HwndHost.WndProc%2A>, but you only need a minimal implementation.</span></span>  
  
7. <span data-ttu-id="5a3ed-138">重写的<xref:System.Windows.Interop.HwndHost.WndProc%2A> <xref:System.Windows.Interop.HwndHost>和方法<xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> 。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-138">Override the <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> and <xref:System.Windows.Interop.HwndHost.WndProc%2A> methods of <xref:System.Windows.Interop.HwndHost>.</span></span> <span data-ttu-id="5a3ed-139">你必须重写这些方法以满足<xref:System.Windows.Interop.HwndHost>协定, 但你可能只需要提供最小实现。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-139">You must override these methods to satisfy the <xref:System.Windows.Interop.HwndHost> contract, but you may only need to provide a minimal implementation.</span></span>  
  
8. <span data-ttu-id="5a3ed-140">在代码隐藏文件中, 创建控件宿主类的实例, 并使其成为用于承载窗口的<xref:System.Windows.Controls.Border>元素的子元素。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-140">In your code-behind file, create an instance of the control hosting class and make it a child of the <xref:System.Windows.Controls.Border> element that is intended to host the window.</span></span>  
  
9. <span data-ttu-id="5a3ed-141">通过从其子窗口发送[!INCLUDE[TLA#tla_win](../../../../includes/tlasharptla-win-md.md)]消息并处理消息 (例如, 由控件发送的通知), 与承载的窗口通信。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-141">Communicate with the hosted window by sending it [!INCLUDE[TLA#tla_win](../../../../includes/tlasharptla-win-md.md)] messages and handling messages from its child windows, such as notifications sent by controls.</span></span>  
  
<a name="page_layout"></a>   
## <a name="implement-the-page-layout"></a><span data-ttu-id="5a3ed-142">实现页面布局</span><span class="sxs-lookup"><span data-stu-id="5a3ed-142">Implement the Page Layout</span></span>  
 <span data-ttu-id="5a3ed-143">承载 ListBox 控件的 WPF 页的布局由两个区域组成。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-143">The layout for the WPF page that hosts the ListBox Control consists of two regions.</span></span> <span data-ttu-id="5a3ed-144">页的左侧承载多个 WPF 控件, 这些控件提供[!INCLUDE[TLA#tla_ui](../../../../includes/tlasharptla-ui-md.md)]的可用于操作 Win32 控件。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-144">The left side of the page hosts several WPF controls that provide a [!INCLUDE[TLA#tla_ui](../../../../includes/tlasharptla-ui-md.md)] that allows you to manipulate the Win32 control.</span></span> <span data-ttu-id="5a3ed-145">页面右上角具有一个正方形区域，用于放置所承载的 ListBox 控件。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-145">The upper right corner of the page has a square region for the hosted ListBox Control.</span></span>  
  
 <span data-ttu-id="5a3ed-146">用于实现此布局的代码非常简单。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-146">The code to implement this layout is quite simple.</span></span> <span data-ttu-id="5a3ed-147">根元素是<xref:System.Windows.Controls.DockPanel>具有两个子元素的。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-147">The root element is a <xref:System.Windows.Controls.DockPanel> that has two child elements.</span></span> <span data-ttu-id="5a3ed-148">第一个<xref:System.Windows.Controls.Border>元素是承载 ListBox 控件的元素。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-148">The first is a <xref:System.Windows.Controls.Border> element that hosts the ListBox Control.</span></span> <span data-ttu-id="5a3ed-149">它在该页的右上角占据了一个大小为 200x200 的正方形。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-149">It occupies a 200x200 square in the upper right corner of the page.</span></span> <span data-ttu-id="5a3ed-150">第二个<xref:System.Windows.Controls.StackPanel>元素是包含一组 WPF 控件的元素, 这些控件显示信息, 并允许您通过设置公开的互操作属性来操作 ListBox 控件。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-150">The second is a <xref:System.Windows.Controls.StackPanel> element that contains a set of WPF controls that display information and allow you to manipulate the ListBox Control by setting exposed interoperation properties.</span></span> <span data-ttu-id="5a3ed-151">对于作为子级<xref:System.Windows.Controls.StackPanel>的每个元素, 请参阅下面的示例代码中所用的各种元素的参考资料, 这些元素将在下面的示例代码中列出, 但不会在此处介绍 (基本互操作模型不需要任何一个, 而是提供它们以将一些交互性添加到示例中)。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-151">For each of the elements that are children of the <xref:System.Windows.Controls.StackPanel>, see the reference material for the various elements used for details on what these elements are or what they do, these are listed in the example code below but will not be explained here (the basic interoperation model does not require any of them, they are provided to add some interactivity to the sample).</span></span>  
  
 [!code-xaml[WPFHostingWin32Control#WPFUI](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml#wpfui)]  
  
<a name="host_class"></a>   
## <a name="implement-a-class-to-host-the-microsoft-win32-control"></a><span data-ttu-id="5a3ed-152">实现类以承载 Microsoft Win32 控件</span><span class="sxs-lookup"><span data-stu-id="5a3ed-152">Implement a Class to Host the Microsoft Win32 Control</span></span>  
 <span data-ttu-id="5a3ed-153">此示例的核心是实际承载控件的类，即 ControlHost.cs。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-153">The core of this sample is the class that actually hosts the control, ControlHost.cs.</span></span> <span data-ttu-id="5a3ed-154">它继承自 <xref:System.Windows.Interop.HwndHost>。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-154">It inherits from <xref:System.Windows.Interop.HwndHost>.</span></span> <span data-ttu-id="5a3ed-155">构造函数采用两个参数: height 和 width, 它们对应于承载 ListBox 控件的<xref:System.Windows.Controls.Border>元素的高度和宽度。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-155">The constructor takes two parameters, height and width, which correspond to the height and width of the <xref:System.Windows.Controls.Border> element that hosts the ListBox control.</span></span> <span data-ttu-id="5a3ed-156">稍后将使用这些值来确保控件的大小与<xref:System.Windows.Controls.Border>元素匹配。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-156">These values are used later to ensure that the size of the control matches the <xref:System.Windows.Controls.Border> element.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#ControlHostClass](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#controlhostclass)]
 [!code-vb[WPFHostingWin32Control#ControlHostClass](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#controlhostclass)]  
  
 <span data-ttu-id="5a3ed-157">还有一组常量。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-157">There is also a set of constants.</span></span> <span data-ttu-id="5a3ed-158">大多数情况下, 这些常量都是从 Winuser.h 中提取的, 并允许在调用 Win32 函数时使用传统名称。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-158">These constants are largely taken from Winuser.h, and allow you to use conventional names when calling Win32 functions.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#ControlHostConstants](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#controlhostconstants)]
 [!code-vb[WPFHostingWin32Control#ControlHostConstants](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#controlhostconstants)]  
  
<a name="buildwindowcore"></a>   
### <a name="override-buildwindowcore-to-create-the-microsoft-win32-window"></a><span data-ttu-id="5a3ed-159">替代 BuildWindowCore 以创建 Microsoft Win32 窗口</span><span class="sxs-lookup"><span data-stu-id="5a3ed-159">Override BuildWindowCore to Create the Microsoft Win32 Window</span></span>  
 <span data-ttu-id="5a3ed-160">重写此方法以创建将由页面承载的 Win32 窗口, 并在窗口和页面之间建立连接。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-160">You override this method to create the Win32 window that will be hosted by the page, and make the connection between the window and the page.</span></span> <span data-ttu-id="5a3ed-161">由于此示例涉及承载 ListBox 控件，因此将创建两个窗口。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-161">Because this sample involves hosting a ListBox Control, two windows are created.</span></span> <span data-ttu-id="5a3ed-162">第一种是由 WPF 页实际承载的窗口。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-162">The first is the window that is actually hosted by the WPF page.</span></span> <span data-ttu-id="5a3ed-163">ListBox 控件被创建为该窗口的子窗口。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-163">The ListBox Control is created as a child of that window.</span></span>  
  
 <span data-ttu-id="5a3ed-164">采用此方法的原因是为了简化接收来自控件的通知的过程。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-164">The reason for this approach is to simplify the process of receiving notifications from the control.</span></span> <span data-ttu-id="5a3ed-165"><xref:System.Windows.Interop.HwndHost>类允许处理发送到它所承载的窗口的消息。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-165">The <xref:System.Windows.Interop.HwndHost> class allows you to process messages sent to the window that it is hosting.</span></span> <span data-ttu-id="5a3ed-166">如果直接托管 Win32 控件, 则会收到发送到控件内部消息循环的消息。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-166">If you host a Win32 control directly, you receive the messages sent to the internal message loop of the control.</span></span> <span data-ttu-id="5a3ed-167">你可以显示控件并向其发送消息，但不会收到该控件发送到其父窗口的通知。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-167">You can display the control and send it messages, but you do not receive the notifications that the control sends to its parent window.</span></span> <span data-ttu-id="5a3ed-168">这意味你没有办法检测用户何时与该控件进行交互，以及其他更多方面。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-168">This means, among other things, that you have no way of detecting when the user interacts with the control.</span></span> <span data-ttu-id="5a3ed-169">可改为创建一个宿主窗口，并使控件成为该窗口的子窗口。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-169">Instead, create a host window and make the control a child of that window.</span></span> <span data-ttu-id="5a3ed-170">这样，你便能够处理宿主窗口的消息，包括由该控件发送给它的通知。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-170">This allows you to process the messages for the host window including the notifications sent to it by the control.</span></span> <span data-ttu-id="5a3ed-171">由于宿主窗口只是控件的一个简单包装器而已，为了方便起见，以后将把该包称为 ListBox 控件。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-171">For convenience, since the host window is little more than a simple wrapper for the control, the package will be referred to as a ListBox control.</span></span>  
  
<a name="create_the_window_and_listbox"></a>   
#### <a name="create-the-host-window-and-listbox-control"></a><span data-ttu-id="5a3ed-172">创建宿主窗口和 ListBox 控件</span><span class="sxs-lookup"><span data-stu-id="5a3ed-172">Create the Host Window and ListBox Control</span></span>  
 <span data-ttu-id="5a3ed-173">可以通过创建和注册窗口类等, 使用 PInvoke 为控件创建宿主窗口。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-173">You can use PInvoke to create a host window for the control by creating and registering a window class, and so on.</span></span> <span data-ttu-id="5a3ed-174">但是，更简单的一种方法是使用预定义的“静态”窗口类创建窗口。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-174">However, a much simpler approach is to create a window with the predefined "static" window class.</span></span> <span data-ttu-id="5a3ed-175">这将为你提供所需窗口步骤，以接收来自控件的通知，但需要完成最少的编码工作。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-175">This provides you with the window procedure you need in order to receive notifications from the control, and requires minimal coding.</span></span>  
  
 <span data-ttu-id="5a3ed-176">控件的 HWND 通过一个只读属性公开，以便宿主页面可使用它向控件发送消息。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-176">The HWND of the control is exposed through a read-only property, such that the host page can use it to send messages to the control.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#IntPtrProperty](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#intptrproperty)]
 [!code-vb[WPFHostingWin32Control#IntPtrProperty](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#intptrproperty)]  
  
 <span data-ttu-id="5a3ed-177">ListBox 控件被创建为宿主窗口的子窗口。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-177">The ListBox control is created as a child of the host window.</span></span> <span data-ttu-id="5a3ed-178">两个窗口的高度和宽度被设置为传递给构造函数的值（前面已讨论）。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-178">The height and width of both windows are set to the values passed to the constructor, discussed above.</span></span> <span data-ttu-id="5a3ed-179">这可确保宿主窗口和控件的大小与页面上的保留区域相同。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-179">This ensures that the size of the host window and control is identical to the reserved area on the page.</span></span>  <span data-ttu-id="5a3ed-180">创建 windows 后, 此示例将返回一个<xref:System.Runtime.InteropServices.HandleRef>对象, 其中包含宿主窗口的 HWND。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-180">After the windows are created, the sample returns a <xref:System.Runtime.InteropServices.HandleRef> object that contains the HWND of the host window.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#BuildWindowCore](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#buildwindowcore)]
 [!code-vb[WPFHostingWin32Control#BuildWindowCore](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#buildwindowcore)]  
  
 [!code-csharp[WPFHostingWin32Control#BuildWindowCoreHelper](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#buildwindowcorehelper)]
 [!code-vb[WPFHostingWin32Control#BuildWindowCoreHelper](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#buildwindowcorehelper)]  
  
<a name="destroywindow_wndproc"></a>   
### <a name="implement-destroywindow-and-wndproc"></a><span data-ttu-id="5a3ed-181">实现 DestroyWindow 和 WndProc</span><span class="sxs-lookup"><span data-stu-id="5a3ed-181">Implement DestroyWindow and WndProc</span></span>  
 <span data-ttu-id="5a3ed-182">除了之外<xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>, 还必须<xref:System.Windows.Interop.HwndHost.WndProc%2A>重写的和<xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A>方法<xref:System.Windows.Interop.HwndHost>。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-182">In addition to <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>, you must also override the <xref:System.Windows.Interop.HwndHost.WndProc%2A> and <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> methods of the <xref:System.Windows.Interop.HwndHost>.</span></span> <span data-ttu-id="5a3ed-183">在此示例中, 控件的消息由<xref:System.Windows.Interop.HwndHost.MessageHook>处理程序处理, 因此<xref:System.Windows.Interop.HwndHost.WndProc%2A>和<xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A>的实现是最少的。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-183">In this example, the messages for the control are handled by the <xref:System.Windows.Interop.HwndHost.MessageHook> handler, thus the implementation of <xref:System.Windows.Interop.HwndHost.WndProc%2A> and <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> is minimal.</span></span> <span data-ttu-id="5a3ed-184">对于<xref:System.Windows.Interop.HwndHost.WndProc%2A>, 将设置`handled`为`false`以指示消息未处理并且返回0。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-184">In the case of <xref:System.Windows.Interop.HwndHost.WndProc%2A>, set `handled` to `false` to indicate that the  message was not handled and return 0.</span></span> <span data-ttu-id="5a3ed-185">对于<xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A>, 只需销毁窗口。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-185">For <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A>, simply destroy the window.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#WndProcDestroy](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#wndprocdestroy)]
 [!code-vb[WPFHostingWin32Control#WndProcDestroy](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#wndprocdestroy)]  
  
 [!code-csharp[WPFHostingWin32Control#WndProcDestroyHelper](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#wndprocdestroyhelper)]
 [!code-vb[WPFHostingWin32Control#WndProcDestroyHelper](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#wndprocdestroyhelper)]  
  
<a name="host_the_control"></a>   
## <a name="host-the-control-on-the-page"></a><span data-ttu-id="5a3ed-186">在页面上承载控件</span><span class="sxs-lookup"><span data-stu-id="5a3ed-186">Host the Control on the Page</span></span>  
 <span data-ttu-id="5a3ed-187">若要在页面上承载控件, 请首先创建`ControlHost`类的新实例。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-187">To host the control on the page, you first create a new instance of the `ControlHost` class.</span></span> <span data-ttu-id="5a3ed-188">向构造函数传递包含控件 (`ControlHostElement`) 的 border 元素的高度和宽度。 `ControlHost`</span><span class="sxs-lookup"><span data-stu-id="5a3ed-188">Pass the height and width of the border element that contains the control (`ControlHostElement`) to the `ControlHost` constructor.</span></span> <span data-ttu-id="5a3ed-189">这可确保 ListBox 具有正确的大小。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-189">This ensures that the ListBox is sized correctly.</span></span> <span data-ttu-id="5a3ed-190">然后, 通过将`ControlHost`对象分配给宿主<xref:System.Windows.Controls.Border>的<xref:System.Windows.Controls.Decorator.Child%2A>属性, 在页面上承载控件。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-190">You then host the control on the page by assigning the `ControlHost` object to the <xref:System.Windows.Controls.Decorator.Child%2A> property of the host <xref:System.Windows.Controls.Border>.</span></span>  
  
 <span data-ttu-id="5a3ed-191">该示例将一个处理程序附加<xref:System.Windows.Interop.HwndHost.MessageHook>到的`ControlHost`事件, 以接收来自控件的消息。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-191">The sample attaches a handler to the <xref:System.Windows.Interop.HwndHost.MessageHook> event of the `ControlHost` to receive messages from the control.</span></span> <span data-ttu-id="5a3ed-192">对于每条发送到宿主窗口的消息，都将引发此事件。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-192">This event is raised for every message sent to the hosted window.</span></span> <span data-ttu-id="5a3ed-193">在这种情况下，这些消息是发送到包装实际 ListBox 控件的窗口的消息，其中包括来自控件的通知。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-193">In this case, these are the messages sent to window that wraps the actual ListBox control, including notifications from the control.</span></span> <span data-ttu-id="5a3ed-194">此示例将调用 SendMessage 以从控件获取信息并修改其内容。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-194">The sample calls SendMessage to obtain information from the control and modify its contents.</span></span> <span data-ttu-id="5a3ed-195">下一节将详细介绍页面如何与控件通信。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-195">The details of how the page communicates with the control are discussed in the next section.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="5a3ed-196">请注意, SendMessage 有两个 PInvoke 声明。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-196">Notice that there are two PInvoke declarations for SendMessage.</span></span> <span data-ttu-id="5a3ed-197">这是必需的`wParam` , 因为一个使用参数传递字符串, 另一个参数用于传递整数。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-197">This is necessary because one uses the `wParam` parameter to pass a string and the other uses it to pass an integer.</span></span> <span data-ttu-id="5a3ed-198">对于每个签名，都需要一个单独的声明，以确保正确封送数据。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-198">You need a separate declaration for each signature to ensure that the data is marshaled correctly.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#HostWindowClass](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml.cs#hostwindowclass)]
 [!code-vb[WPFHostingWin32Control#HostWindowClass](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/Page1.xaml.vb#hostwindowclass)]  
  
 [!code-csharp[WPFHostingWin32Control#ControlMsgFilterSendMessage](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml.cs#controlmsgfiltersendmessage)]
 [!code-vb[WPFHostingWin32Control#ControlMsgFilterSendMessage](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/Page1.xaml.vb#controlmsgfiltersendmessage)]  
  
<a name="communication"></a>   
## <a name="implement-communication-between-the-control-and-the-page"></a><span data-ttu-id="5a3ed-199">在控件和页面之间的实现通信</span><span class="sxs-lookup"><span data-stu-id="5a3ed-199">Implement Communication Between the Control and the Page</span></span>  
 <span data-ttu-id="5a3ed-200">通过向控件发送 Windows 消息来操作控件。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-200">You manipulate the control by sending it Windows messages.</span></span> <span data-ttu-id="5a3ed-201">当用户与控件交互时，控件会通过向其宿主窗口发送通知来通知你。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-201">The control notifies you when the user interacts with it by sending notifications to its host window.</span></span> <span data-ttu-id="5a3ed-202">[在 WPF 中承载 Win32 ListBox 控件](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control)示例包含一个 UI, 该 UI 提供了如何实现此操作的几个示例:</span><span class="sxs-lookup"><span data-stu-id="5a3ed-202">The [Hosting a Win32 ListBox Control in WPF](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control) sample includes a UI that provides several examples of how this works:</span></span>  
  
- <span data-ttu-id="5a3ed-203">向列表中追加项。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-203">Append an item to the list.</span></span>  
  
- <span data-ttu-id="5a3ed-204">从列表中删除选定项</span><span class="sxs-lookup"><span data-stu-id="5a3ed-204">Delete the selected item from the list</span></span>  
  
- <span data-ttu-id="5a3ed-205">显示当前选定项的文本。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-205">Display the text of the currently selected item.</span></span>  
  
- <span data-ttu-id="5a3ed-206">显示列表中的项数。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-206">Display the number of items in the list.</span></span>  
  
 <span data-ttu-id="5a3ed-207">用户还可以通过单击列表框中的项来选择该项, 就像在传统的 Win32 应用程序中一样。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-207">The user can also select an item in the list box by clicking on it, just as they would for a conventional Win32 application.</span></span> <span data-ttu-id="5a3ed-208">每当用户通过选择、添加或追加项来更改列表框的状态时，都将更新所显示的数据。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-208">The displayed data is updated each time the user changes the state of the list box by selecting, adding, or appending an item.</span></span>  
  
 <span data-ttu-id="5a3ed-209">若要追加项, 请发送列表框一[ `LB_ADDSTRING`条消息](/windows/desktop/Controls/lb-addstring)。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-209">To append items, send the list box an [`LB_ADDSTRING` message](/windows/desktop/Controls/lb-addstring).</span></span> <span data-ttu-id="5a3ed-210">若要删除项目, [`LB_GETCURSEL`](/windows/desktop/Controls/lb-getcursel)请发送以获取当前所选内容的索引[`LB_DELETESTRING`](/windows/desktop/Controls/lb-deletestring) , 然后删除该项目。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-210">To delete items, send [`LB_GETCURSEL`](/windows/desktop/Controls/lb-getcursel) to get the index of the current selection and then [`LB_DELETESTRING`](/windows/desktop/Controls/lb-deletestring) to delete the item.</span></span> <span data-ttu-id="5a3ed-211">该示例还发送[`LB_GETCOUNT`](/windows/desktop/Controls/lb-getcount), 并使用返回的值更新显示项数的显示内容。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-211">The sample also sends [`LB_GETCOUNT`](/windows/desktop/Controls/lb-getcount), and uses the returned value to update the display that shows the number of items.</span></span> <span data-ttu-id="5a3ed-212">这两个实例[`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage)都使用上一节中讨论的某个 PInvoke 声明。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-212">Both these instances of [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) use one of the PInvoke declarations discussed in the previous section.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#AppendDeleteText](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml.cs#appenddeletetext)]
 [!code-vb[WPFHostingWin32Control#AppendDeleteText](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/Page1.xaml.vb#appenddeletetext)]  
  
 <span data-ttu-id="5a3ed-213">当用户选择某一项或更改其选择时, 该控件将通过向<xref:System.Windows.Interop.HwndHost.MessageHook> [ `WM_COMMAND` ](/windows/desktop/menurc/wm-command)宿主窗口发送消息来通知宿主窗口, 这会引发该页的事件。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-213">When the user selects an item or changes their selection, the control notifies the host window by sending it a [`WM_COMMAND` message](/windows/desktop/menurc/wm-command), which raises the <xref:System.Windows.Interop.HwndHost.MessageHook> event for the page.</span></span> <span data-ttu-id="5a3ed-214">处理程序将接收到与宿主窗口的主窗口过程相同的信息。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-214">The handler receives the same information as the main window procedure of the host window.</span></span> <span data-ttu-id="5a3ed-215">它还传递对布尔值`handled`的引用。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-215">It also passes a reference to a Boolean value, `handled`.</span></span> <span data-ttu-id="5a3ed-216">您将`handled`设置`true`为, 以指示您已经处理了该消息, 并且不需要进一步的处理。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-216">You set `handled` to `true` to indicate that you have handled the message and no further processing is needed.</span></span>  
  
 <span data-ttu-id="5a3ed-217">[`WM_COMMAND`](/windows/desktop/menurc/wm-command)出于多种原因而发送, 因此您必须检查通知 ID 以确定它是否是您要处理的事件。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-217">[`WM_COMMAND`](/windows/desktop/menurc/wm-command) is sent for a variety of reasons, so you must examine the notification ID to determine whether it is an event that you wish to handle.</span></span> <span data-ttu-id="5a3ed-218">此 ID 包含在`wParam`参数的高位字中。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-218">The ID is contained in the high word of the `wParam` parameter.</span></span> <span data-ttu-id="5a3ed-219">该示例使用按位运算符提取 ID。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-219">The sample uses bitwise operators to extract the ID.</span></span> <span data-ttu-id="5a3ed-220">如果用户已做出或更改了其选择, 则 ID 将为[`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange)。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-220">If the user has made or changed their selection, the ID will be [`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange).</span></span>  
  
 <span data-ttu-id="5a3ed-221">接收到后, 该示例通过向[ `LB_GETCURSEL` ](/windows/desktop/Controls/lb-getcursel)控件发送消息来获取选定项的索引。 [`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange)</span><span class="sxs-lookup"><span data-stu-id="5a3ed-221">When [`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange) is received, the sample gets the index of the selected item by sending the control a [`LB_GETCURSEL` message](/windows/desktop/Controls/lb-getcursel).</span></span> <span data-ttu-id="5a3ed-222">若要获取文本, 首先要创建一个<xref:System.Text.StringBuilder>。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-222">To get the text, you first create a <xref:System.Text.StringBuilder>.</span></span> <span data-ttu-id="5a3ed-223">然后向控件[ `LB_GETTEXT`发送消息](/windows/desktop/Controls/lb-gettext)。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-223">You then send the control an [`LB_GETTEXT` message](/windows/desktop/Controls/lb-gettext).</span></span> <span data-ttu-id="5a3ed-224">将空<xref:System.Text.StringBuilder>对象`wParam`作为参数传递。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-224">Pass the empty <xref:System.Text.StringBuilder> object as the `wParam` parameter.</span></span> <span data-ttu-id="5a3ed-225">当[`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage)返回时<xref:System.Text.StringBuilder> , 将包含选定项的文本。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-225">When [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) returns, the <xref:System.Text.StringBuilder> will contain the text of the selected item.</span></span> <span data-ttu-id="5a3ed-226">的这种[`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage)用法还需要另一个 PInvoke 声明。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-226">This use of [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) requires yet another PInvoke declaration.</span></span>  
  
 <span data-ttu-id="5a3ed-227">最后, 将`handled`设置`true`为以指示消息已处理。</span><span class="sxs-lookup"><span data-stu-id="5a3ed-227">Finally, set `handled` to `true` to indicate that the message has been handled.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="5a3ed-228">请参阅</span><span class="sxs-lookup"><span data-stu-id="5a3ed-228">See also</span></span>

- <xref:System.Windows.Interop.HwndHost>
- [<span data-ttu-id="5a3ed-229">WPF 和 Win32 互操作</span><span class="sxs-lookup"><span data-stu-id="5a3ed-229">WPF and Win32 Interoperation</span></span>](wpf-and-win32-interoperation.md)
- [<span data-ttu-id="5a3ed-230">演练：我的第一个 WPF 桌面应用程序</span><span class="sxs-lookup"><span data-stu-id="5a3ed-230">Walkthrough: My first WPF desktop application</span></span>](../getting-started/walkthrough-my-first-wpf-desktop-application.md)
