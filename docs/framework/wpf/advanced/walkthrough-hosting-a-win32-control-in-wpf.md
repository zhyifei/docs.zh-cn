---
title: 在 WPF 中承载 Win32 控件
titleSuffix: ''
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
helpviewer_keywords:
- hosting Win32 control in WPF [WPF]
- Win32 code [WPF], WPF interoperation
ms.assetid: a676b1eb-fc55-4355-93ab-df840c41cea0
ms.openlocfilehash: eb497a88c119dece85d61d6a32e7b86fb03b44b5
ms.sourcegitcommit: de17a7a0a37042f0d4406f5ae5393531caeb25ba
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 01/24/2020
ms.locfileid: "76744930"
---
# <a name="walkthrough-host-a-win32-control-in-wpf"></a><span data-ttu-id="befad-102">演练：在 WPF 中承载 Win32 控件</span><span class="sxs-lookup"><span data-stu-id="befad-102">Walkthrough: Host a Win32 Control in WPF</span></span>
<span data-ttu-id="befad-103">Windows Presentation Foundation （WPF）提供了一个用于创建应用程序的丰富环境。</span><span class="sxs-lookup"><span data-stu-id="befad-103">Windows Presentation Foundation (WPF) provides a rich environment for creating applications.</span></span> <span data-ttu-id="befad-104">但是，当你在 Win32 代码中有大量投资时，在 WPF 应用程序中重复使用至少一些代码，而不是完全重新编写它，这可能更有效。</span><span class="sxs-lookup"><span data-stu-id="befad-104">However, when you have a substantial investment in Win32 code, it may be more effective to reuse at least some of that code in your WPF application rather than rewrite it completely.</span></span> <span data-ttu-id="befad-105">WPF 提供了一种用于在 WPF 页上承载 Win32 窗口的简单机制。</span><span class="sxs-lookup"><span data-stu-id="befad-105">WPF provides a straightforward mechanism for hosting a Win32 window, on a WPF page.</span></span>  
  
 <span data-ttu-id="befad-106">本主题将指导你完成承载 Win32 列表框控件的应用程序，该应用程序[在 WPF 示例中承载 Win32 ListBox 控件](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control)。</span><span class="sxs-lookup"><span data-stu-id="befad-106">This topic walks you through an application, [Hosting a Win32 ListBox Control in WPF Sample](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control), that hosts a Win32 list box control.</span></span> <span data-ttu-id="befad-107">可以扩展此常规过程以承载任何 Win32 窗口。</span><span class="sxs-lookup"><span data-stu-id="befad-107">This general procedure can be extended to hosting any Win32 window.</span></span>  

<a name="requirements"></a>   
## <a name="requirements"></a><span data-ttu-id="befad-108">요구 사항</span><span class="sxs-lookup"><span data-stu-id="befad-108">Requirements</span></span>  
 <span data-ttu-id="befad-109">本主题假定基本熟悉 WPF 和 Windows API 编程。</span><span class="sxs-lookup"><span data-stu-id="befad-109">This topic assumes a basic familiarity with both WPF and Windows API programming.</span></span> <span data-ttu-id="befad-110">有关 WPF 编程的基本简介，请参阅[入门](../getting-started/index.md)。</span><span class="sxs-lookup"><span data-stu-id="befad-110">For a basic introduction to WPF programming, see [Getting Started](../getting-started/index.md).</span></span> <span data-ttu-id="befad-111">有关 Windows API 编程的简介，请参阅主题中的任意一本书，该主题*中的 Charles* Petzold。</span><span class="sxs-lookup"><span data-stu-id="befad-111">For an introduction to Windows API programming, see any of the numerous books on the subject, in particular *Programming Windows* by Charles Petzold.</span></span>  
  
 <span data-ttu-id="befad-112">由于本主题附带的示例是在中C#实现的，因此它使用平台调用服务（PInvoke）来访问 Windows API。</span><span class="sxs-lookup"><span data-stu-id="befad-112">Because the sample that accompanies this topic is implemented in C#, it makes use of Platform Invocation Services (PInvoke) to access the Windows API.</span></span> <span data-ttu-id="befad-113">某些对 PInvoke 的熟悉非常有用，但并不重要。</span><span class="sxs-lookup"><span data-stu-id="befad-113">Some familiarity with PInvoke is helpful but not essential.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="befad-114">이 항목에는 관련 샘플의 많은 코드 예제가 포함되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="befad-114">This topic includes a number of code examples from the associated sample.</span></span> <span data-ttu-id="befad-115">그러나 가독성을 위해 전체 샘플 코드를 포함하지는 않습니다.</span><span class="sxs-lookup"><span data-stu-id="befad-115">However, for readability, it does not include the complete sample code.</span></span> <span data-ttu-id="befad-116">你可以从[在 WPF 示例中承载 Win32 ListBox 控件](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control)获取或查看完整的代码。</span><span class="sxs-lookup"><span data-stu-id="befad-116">You can obtain or view complete code from [Hosting a Win32 ListBox Control in WPF Sample](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control).</span></span>  
  
<a name="basic_procedure"></a>   
## <a name="the-basic-procedure"></a><span data-ttu-id="befad-117">기본 절차</span><span class="sxs-lookup"><span data-stu-id="befad-117">The Basic Procedure</span></span>  
 <span data-ttu-id="befad-118">本部分概述了在 WPF 页上承载 Win32 窗口的基本过程。</span><span class="sxs-lookup"><span data-stu-id="befad-118">This section outlines the basic procedure for hosting a Win32 window on a WPF page.</span></span> <span data-ttu-id="befad-119">나머지 섹션에서는 각 단계를 자세히 설명합니다.</span><span class="sxs-lookup"><span data-stu-id="befad-119">The remaining sections go through the details of each step.</span></span>  
  
 <span data-ttu-id="befad-120">기본 호스팅 절차는 다음과 같습니다.</span><span class="sxs-lookup"><span data-stu-id="befad-120">The basic hosting procedure is:</span></span>  
  
1. <span data-ttu-id="befad-121">实现 WPF 页以承载窗口。</span><span class="sxs-lookup"><span data-stu-id="befad-121">Implement a WPF page to host the window.</span></span> <span data-ttu-id="befad-122">一种方法是创建一个 <xref:System.Windows.Controls.Border> 元素，为承载的窗口保留部分页面。</span><span class="sxs-lookup"><span data-stu-id="befad-122">One technique is to create a <xref:System.Windows.Controls.Border> element to reserve a section of the page for the hosted window.</span></span>  
  
2. <span data-ttu-id="befad-123">实现一个类以承载继承自 <xref:System.Windows.Interop.HwndHost>的控件。</span><span class="sxs-lookup"><span data-stu-id="befad-123">Implement a class to host the control that inherits from <xref:System.Windows.Interop.HwndHost>.</span></span>  
  
3. <span data-ttu-id="befad-124">在该类中，重写 <xref:System.Windows.Interop.HwndHost> 类成员 <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>。</span><span class="sxs-lookup"><span data-stu-id="befad-124">In that class, override the <xref:System.Windows.Interop.HwndHost> class member <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>.</span></span>  
  
4. <span data-ttu-id="befad-125">将所承载的窗口创建为包含 WPF 页的窗口的子级。</span><span class="sxs-lookup"><span data-stu-id="befad-125">Create the hosted window as a child of the window that contains the WPF page.</span></span> <span data-ttu-id="befad-126">尽管传统的 WPF 编程不需要显式使用它，但宿主页是一个带有句柄（HWND）的窗口。</span><span class="sxs-lookup"><span data-stu-id="befad-126">Although conventional WPF programming does not need to explicitly make use of it, the hosting page is a window with a handle (HWND).</span></span> <span data-ttu-id="befad-127">通过 <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A> 方法的 `hwndParent` 参数接收页 HWND。</span><span class="sxs-lookup"><span data-stu-id="befad-127">You receive the page HWND through the `hwndParent` parameter of the <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A> method.</span></span> <span data-ttu-id="befad-128">호스트된 창은 이 HWND의 자식으로 만들어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="befad-128">The hosted window should be created as a child of this HWND.</span></span>  
  
5. <span data-ttu-id="befad-129">호스트 창을 만들었으면 호스트된 창의 HWND를 반환합니다.</span><span class="sxs-lookup"><span data-stu-id="befad-129">Once you have created the host window, return the HWND of the hosted window.</span></span> <span data-ttu-id="befad-130">如果要承载一个或多个 Win32 控件，通常需要将宿主窗口创建为 HWND 的子级，并使控件成为该宿主窗口的子级。</span><span class="sxs-lookup"><span data-stu-id="befad-130">If you want to host one or more Win32 controls, you typically create a host window as a child of the HWND and make the controls children of that host window.</span></span> <span data-ttu-id="befad-131">在宿主窗口中包装控件提供了一种简单的方法，使您的 WPF 页可以从控件接收通知，这些控件处理跨 HWND 边界的通知的某些特定 Win32 问题。</span><span class="sxs-lookup"><span data-stu-id="befad-131">Wrapping the controls in a host window provides a simple way for your WPF page to receive notifications from the controls, which deals with some particular Win32 issues with notifications across the HWND boundary.</span></span>  
  
6. <span data-ttu-id="befad-132">자식 컨트롤의 알림과 같이 호스트 창으로 전송되는 선택한 메시지를 처리합니다.</span><span class="sxs-lookup"><span data-stu-id="befad-132">Handle selected messages sent to the host window, such as notifications from child controls.</span></span> <span data-ttu-id="befad-133">이렇게 하는 방법은 두 가지가 있습니다.</span><span class="sxs-lookup"><span data-stu-id="befad-133">There are two ways to do this.</span></span>  
  
    - <span data-ttu-id="befad-134">如果你更喜欢处理宿主类中的消息，请重写 <xref:System.Windows.Interop.HwndHost> 类的 <xref:System.Windows.Interop.HwndHost.WndProc%2A> 方法。</span><span class="sxs-lookup"><span data-stu-id="befad-134">If you prefer to handle messages in your hosting class, override the <xref:System.Windows.Interop.HwndHost.WndProc%2A> method of the <xref:System.Windows.Interop.HwndHost> class.</span></span>  
  
    - <span data-ttu-id="befad-135">如果想让 WPF 处理消息，请在代码隐藏中处理 <xref:System.Windows.Interop.HwndHost> 类 <xref:System.Windows.Interop.HwndHost.MessageHook> 事件。</span><span class="sxs-lookup"><span data-stu-id="befad-135">If you prefer to have the WPF handle the messages, handle the <xref:System.Windows.Interop.HwndHost> class <xref:System.Windows.Interop.HwndHost.MessageHook> event in your code-behind.</span></span> <span data-ttu-id="befad-136">이 이벤트는 호스트된 창에서 받은 모든 메시지에 대해 발생합니다.</span><span class="sxs-lookup"><span data-stu-id="befad-136">This event occurs for every message that is received by the hosted window.</span></span> <span data-ttu-id="befad-137">如果选择此选项，则仍必须重写 <xref:System.Windows.Interop.HwndHost.WndProc%2A>，但你只需要最小实现。</span><span class="sxs-lookup"><span data-stu-id="befad-137">If you choose this option, you must still override <xref:System.Windows.Interop.HwndHost.WndProc%2A>, but you only need a minimal implementation.</span></span>  
  
7. <span data-ttu-id="befad-138">重写 <xref:System.Windows.Interop.HwndHost>的 <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> 和 <xref:System.Windows.Interop.HwndHost.WndProc%2A> 方法。</span><span class="sxs-lookup"><span data-stu-id="befad-138">Override the <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> and <xref:System.Windows.Interop.HwndHost.WndProc%2A> methods of <xref:System.Windows.Interop.HwndHost>.</span></span> <span data-ttu-id="befad-139">你必须重写这些方法以满足 <xref:System.Windows.Interop.HwndHost> 协定，但你可能只需要提供最小实现。</span><span class="sxs-lookup"><span data-stu-id="befad-139">You must override these methods to satisfy the <xref:System.Windows.Interop.HwndHost> contract, but you may only need to provide a minimal implementation.</span></span>  
  
8. <span data-ttu-id="befad-140">在代码隐藏文件中，创建控制宿主类的实例，并使其成为用于承载窗口的 <xref:System.Windows.Controls.Border> 元素的子元素。</span><span class="sxs-lookup"><span data-stu-id="befad-140">In your code-behind file, create an instance of the control hosting class and make it a child of the <xref:System.Windows.Controls.Border> element that is intended to host the window.</span></span>  
  
9. <span data-ttu-id="befad-141">通过从其子窗口发送 Microsoft Windows 消息并处理消息（例如，由控件发送的通知），与承载的窗口通信。</span><span class="sxs-lookup"><span data-stu-id="befad-141">Communicate with the hosted window by sending it Microsoft Windows messages and handling messages from its child windows, such as notifications sent by controls.</span></span>  
  
<a name="page_layout"></a>   
## <a name="implement-the-page-layout"></a><span data-ttu-id="befad-142">페이지 레이아웃 구현</span><span class="sxs-lookup"><span data-stu-id="befad-142">Implement the Page Layout</span></span>  
 <span data-ttu-id="befad-143">承载 ListBox 控件的 WPF 页的布局由两个区域组成。</span><span class="sxs-lookup"><span data-stu-id="befad-143">The layout for the WPF page that hosts the ListBox Control consists of two regions.</span></span> <span data-ttu-id="befad-144">页的左侧承载多个 WPF 控件，这些控件提供了允许操作 Win32 控件的 [!INCLUDE[TLA#tla_ui](../../../../includes/tlasharptla-ui-md.md)]。</span><span class="sxs-lookup"><span data-stu-id="befad-144">The left side of the page hosts several WPF controls that provide a [!INCLUDE[TLA#tla_ui](../../../../includes/tlasharptla-ui-md.md)] that allows you to manipulate the Win32 control.</span></span> <span data-ttu-id="befad-145">페이지의 오른쪽 위에는 호스트된 ListBox 컨트롤에 대한 사각형 영역이 있습니다.</span><span class="sxs-lookup"><span data-stu-id="befad-145">The upper right corner of the page has a square region for the hosted ListBox Control.</span></span>  
  
 <span data-ttu-id="befad-146">用于实现此布局的代码非常简单。</span><span class="sxs-lookup"><span data-stu-id="befad-146">The code to implement this layout is quite simple.</span></span> <span data-ttu-id="befad-147">根元素是具有两个子元素的 <xref:System.Windows.Controls.DockPanel>。</span><span class="sxs-lookup"><span data-stu-id="befad-147">The root element is a <xref:System.Windows.Controls.DockPanel> that has two child elements.</span></span> <span data-ttu-id="befad-148">第一个是承载 ListBox 控件的 <xref:System.Windows.Controls.Border> 元素。</span><span class="sxs-lookup"><span data-stu-id="befad-148">The first is a <xref:System.Windows.Controls.Border> element that hosts the ListBox Control.</span></span> <span data-ttu-id="befad-149">이 요소는 페이지의 오른쪽 위에 있는 200x200 정사각형을 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="befad-149">It occupies a 200x200 square in the upper right corner of the page.</span></span> <span data-ttu-id="befad-150">第二个是包含一组 WPF 控件的 <xref:System.Windows.Controls.StackPanel> 元素，这些控件显示信息，并允许您通过设置公开的互操作属性来操作 ListBox 控件。</span><span class="sxs-lookup"><span data-stu-id="befad-150">The second is a <xref:System.Windows.Controls.StackPanel> element that contains a set of WPF controls that display information and allow you to manipulate the ListBox Control by setting exposed interoperation properties.</span></span> <span data-ttu-id="befad-151">对于每个作为 <xref:System.Windows.Controls.StackPanel>的子级的元素，请参阅下面的示例代码中所用的各种元素的参考资料，这些元素将在下面的示例代码中列出，但不会在此处介绍（基本互操作性模型不需要任何这些元素，而是提供它们来向示例添加一些交互性）。</span><span class="sxs-lookup"><span data-stu-id="befad-151">For each of the elements that are children of the <xref:System.Windows.Controls.StackPanel>, see the reference material for the various elements used for details on what these elements are or what they do, these are listed in the example code below but will not be explained here (the basic interoperation model does not require any of them, they are provided to add some interactivity to the sample).</span></span>  
  
 [!code-xaml[WPFHostingWin32Control#WPFUI](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml#wpfui)]  
  
<a name="host_class"></a>   
## <a name="implement-a-class-to-host-the-microsoft-win32-control"></a><span data-ttu-id="befad-152">Microsoft Win32 컨트롤을 호스트하는 클래스 구현</span><span class="sxs-lookup"><span data-stu-id="befad-152">Implement a Class to Host the Microsoft Win32 Control</span></span>  
 <span data-ttu-id="befad-153">이 샘플의 핵심은 ControlHost.cs 컨트롤을 실제로 호스트하는 클래스입니다.</span><span class="sxs-lookup"><span data-stu-id="befad-153">The core of this sample is the class that actually hosts the control, ControlHost.cs.</span></span> <span data-ttu-id="befad-154"><xref:System.Windows.Interop.HwndHost>에서 상속받습니다.</span><span class="sxs-lookup"><span data-stu-id="befad-154">It inherits from <xref:System.Windows.Interop.HwndHost>.</span></span> <span data-ttu-id="befad-155">构造函数采用两个参数： height 和 width，它们对应于承载 ListBox 控件的 <xref:System.Windows.Controls.Border> 元素的高度和宽度。</span><span class="sxs-lookup"><span data-stu-id="befad-155">The constructor takes two parameters, height and width, which correspond to the height and width of the <xref:System.Windows.Controls.Border> element that hosts the ListBox control.</span></span> <span data-ttu-id="befad-156">稍后将使用这些值来确保控件大小与 <xref:System.Windows.Controls.Border> 元素匹配。</span><span class="sxs-lookup"><span data-stu-id="befad-156">These values are used later to ensure that the size of the control matches the <xref:System.Windows.Controls.Border> element.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#ControlHostClass](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#controlhostclass)]
 [!code-vb[WPFHostingWin32Control#ControlHostClass](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#controlhostclass)]  
  
 <span data-ttu-id="befad-157">또한 상수 집합도 있습니다.</span><span class="sxs-lookup"><span data-stu-id="befad-157">There is also a set of constants.</span></span> <span data-ttu-id="befad-158">大多数情况下，这些常量都是从 Winuser.h 中提取的，并允许在调用 Win32 函数时使用传统名称。</span><span class="sxs-lookup"><span data-stu-id="befad-158">These constants are largely taken from Winuser.h, and allow you to use conventional names when calling Win32 functions.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#ControlHostConstants](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#controlhostconstants)]
 [!code-vb[WPFHostingWin32Control#ControlHostConstants](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#controlhostconstants)]  
  
<a name="buildwindowcore"></a>   
### <a name="override-buildwindowcore-to-create-the-microsoft-win32-window"></a><span data-ttu-id="befad-159">BuildWindowCore를 재정의하여 Microsoft Win32 창 만들기</span><span class="sxs-lookup"><span data-stu-id="befad-159">Override BuildWindowCore to Create the Microsoft Win32 Window</span></span>  
 <span data-ttu-id="befad-160">重写此方法以创建将由页面承载的 Win32 窗口，并在窗口和页面之间建立连接。</span><span class="sxs-lookup"><span data-stu-id="befad-160">You override this method to create the Win32 window that will be hosted by the page, and make the connection between the window and the page.</span></span> <span data-ttu-id="befad-161">이 샘플에서는 ListBox 컨트롤을 호스트하므로 두 개의 창을 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="befad-161">Because this sample involves hosting a ListBox Control, two windows are created.</span></span> <span data-ttu-id="befad-162">第一种是由 WPF 页实际承载的窗口。</span><span class="sxs-lookup"><span data-stu-id="befad-162">The first is the window that is actually hosted by the WPF page.</span></span> <span data-ttu-id="befad-163">ListBox 컨트롤은 해당 창의 자식으로 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="befad-163">The ListBox Control is created as a child of that window.</span></span>  
  
 <span data-ttu-id="befad-164">이 방법은 컨트롤에서 알림을 받는 프로세스를 간소화하기 위해서 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="befad-164">The reason for this approach is to simplify the process of receiving notifications from the control.</span></span> <span data-ttu-id="befad-165"><xref:System.Windows.Interop.HwndHost> 类允许处理发送到它所承载的窗口的消息。</span><span class="sxs-lookup"><span data-stu-id="befad-165">The <xref:System.Windows.Interop.HwndHost> class allows you to process messages sent to the window that it is hosting.</span></span> <span data-ttu-id="befad-166">如果直接托管 Win32 控件，则会收到发送到控件内部消息循环的消息。</span><span class="sxs-lookup"><span data-stu-id="befad-166">If you host a Win32 control directly, you receive the messages sent to the internal message loop of the control.</span></span> <span data-ttu-id="befad-167">컨트롤을 표시하고 메시지를 전송할 수 있지만 컨트롤이 해당 부모 창으로 전송된다는 알림은 받지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="befad-167">You can display the control and send it messages, but you do not receive the notifications that the control sends to its parent window.</span></span> <span data-ttu-id="befad-168">즉, 사용자가 컨트롤과 상호 작용하는 경우를 검색할 방법이 없습니다.</span><span class="sxs-lookup"><span data-stu-id="befad-168">This means, among other things, that you have no way of detecting when the user interacts with the control.</span></span> <span data-ttu-id="befad-169">대신 호스트 창을 만들고 컨트롤을 해당 창의 자식으로 설정합니다.</span><span class="sxs-lookup"><span data-stu-id="befad-169">Instead, create a host window and make the control a child of that window.</span></span> <span data-ttu-id="befad-170">이렇게 하면 컨트롤에서 전송한 알림을 포함하여 호스트 창에 대한 메시지를 처리할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="befad-170">This allows you to process the messages for the host window including the notifications sent to it by the control.</span></span> <span data-ttu-id="befad-171">호스트 창은 컨트롤에 대한 간단한 래퍼에 불과하므로 편의상 패키지를 ListBox 컨트롤이라고 합니다.</span><span class="sxs-lookup"><span data-stu-id="befad-171">For convenience, since the host window is little more than a simple wrapper for the control, the package will be referred to as a ListBox control.</span></span>  
  
<a name="create_the_window_and_listbox"></a>   
#### <a name="create-the-host-window-and-listbox-control"></a><span data-ttu-id="befad-172">호스트 창 및 ListBox 컨트롤 만들기</span><span class="sxs-lookup"><span data-stu-id="befad-172">Create the Host Window and ListBox Control</span></span>  
 <span data-ttu-id="befad-173">可以通过创建和注册窗口类等，使用 PInvoke 为控件创建宿主窗口。</span><span class="sxs-lookup"><span data-stu-id="befad-173">You can use PInvoke to create a host window for the control by creating and registering a window class, and so on.</span></span> <span data-ttu-id="befad-174">그러나 미리 정의된 "정적" 창 클래스를 사용하여 창을 만드는 방법이 훨씬 더 간단합니다.</span><span class="sxs-lookup"><span data-stu-id="befad-174">However, a much simpler approach is to create a window with the predefined "static" window class.</span></span> <span data-ttu-id="befad-175">이 방법은 컨트롤에서 알림을 받기 위해 필요한 창 프로시저를 제공하며 최소한의 코딩이 필요합니다.</span><span class="sxs-lookup"><span data-stu-id="befad-175">This provides you with the window procedure you need in order to receive notifications from the control, and requires minimal coding.</span></span>  
  
 <span data-ttu-id="befad-176">컨트롤의 HWND는 읽기 전용 속성을 통해 노출되므로 호스트 페이지에서 컨트롤로 메시지를 전송하는 데 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="befad-176">The HWND of the control is exposed through a read-only property, such that the host page can use it to send messages to the control.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#IntPtrProperty](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#intptrproperty)]
 [!code-vb[WPFHostingWin32Control#IntPtrProperty](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#intptrproperty)]  
  
 <span data-ttu-id="befad-177">ListBox 컨트롤은 호스트 창의 자식으로 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="befad-177">The ListBox control is created as a child of the host window.</span></span> <span data-ttu-id="befad-178">두 창의 너비와 높이는 위에서 설명한 대로 생성자에 전달된 값으로 설정됩니다.</span><span class="sxs-lookup"><span data-stu-id="befad-178">The height and width of both windows are set to the values passed to the constructor, discussed above.</span></span> <span data-ttu-id="befad-179">이렇게 하면 호스트 창과 컨트롤의 크기가 페이지에서 예약된 영역과 동일해집니다.</span><span class="sxs-lookup"><span data-stu-id="befad-179">This ensures that the size of the host window and control is identical to the reserved area on the page.</span></span>  <span data-ttu-id="befad-180">创建 windows 后，此示例将返回一个 <xref:System.Runtime.InteropServices.HandleRef> 对象，该对象包含宿主窗口的 HWND。</span><span class="sxs-lookup"><span data-stu-id="befad-180">After the windows are created, the sample returns a <xref:System.Runtime.InteropServices.HandleRef> object that contains the HWND of the host window.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#BuildWindowCore](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#buildwindowcore)]
 [!code-vb[WPFHostingWin32Control#BuildWindowCore](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#buildwindowcore)]  
  
 [!code-csharp[WPFHostingWin32Control#BuildWindowCoreHelper](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#buildwindowcorehelper)]
 [!code-vb[WPFHostingWin32Control#BuildWindowCoreHelper](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#buildwindowcorehelper)]  
  
<a name="destroywindow_wndproc"></a>   
### <a name="implement-destroywindow-and-wndproc"></a><span data-ttu-id="befad-181">DestroyWindow 및 WndProc 구현</span><span class="sxs-lookup"><span data-stu-id="befad-181">Implement DestroyWindow and WndProc</span></span>  
 <span data-ttu-id="befad-182">除了 <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>之外，还必须重写 <xref:System.Windows.Interop.HwndHost>的 <xref:System.Windows.Interop.HwndHost.WndProc%2A> 和 <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> 方法。</span><span class="sxs-lookup"><span data-stu-id="befad-182">In addition to <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>, you must also override the <xref:System.Windows.Interop.HwndHost.WndProc%2A> and <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> methods of the <xref:System.Windows.Interop.HwndHost>.</span></span> <span data-ttu-id="befad-183">在此示例中，控件的消息由 <xref:System.Windows.Interop.HwndHost.MessageHook> 处理程序处理，因此 <xref:System.Windows.Interop.HwndHost.WndProc%2A> 和 <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> 的实现最小。</span><span class="sxs-lookup"><span data-stu-id="befad-183">In this example, the messages for the control are handled by the <xref:System.Windows.Interop.HwndHost.MessageHook> handler, thus the implementation of <xref:System.Windows.Interop.HwndHost.WndProc%2A> and <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> is minimal.</span></span> <span data-ttu-id="befad-184">如果是 <xref:System.Windows.Interop.HwndHost.WndProc%2A>，请将 `handled` 设置为 `false`，以指示消息未处理并且返回0。</span><span class="sxs-lookup"><span data-stu-id="befad-184">In the case of <xref:System.Windows.Interop.HwndHost.WndProc%2A>, set `handled` to `false` to indicate that the  message was not handled and return 0.</span></span> <span data-ttu-id="befad-185">对于 <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A>，只需销毁窗口。</span><span class="sxs-lookup"><span data-stu-id="befad-185">For <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A>, simply destroy the window.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#WndProcDestroy](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#wndprocdestroy)]
 [!code-vb[WPFHostingWin32Control#WndProcDestroy](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#wndprocdestroy)]  
  
 [!code-csharp[WPFHostingWin32Control#WndProcDestroyHelper](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#wndprocdestroyhelper)]
 [!code-vb[WPFHostingWin32Control#WndProcDestroyHelper](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#wndprocdestroyhelper)]  
  
<a name="host_the_control"></a>   
## <a name="host-the-control-on-the-page"></a><span data-ttu-id="befad-186">페이지에서 컨트롤 호스트</span><span class="sxs-lookup"><span data-stu-id="befad-186">Host the Control on the Page</span></span>  
 <span data-ttu-id="befad-187">若要在页面上承载控件，请首先创建 `ControlHost` 类的新实例。</span><span class="sxs-lookup"><span data-stu-id="befad-187">To host the control on the page, you first create a new instance of the `ControlHost` class.</span></span> <span data-ttu-id="befad-188">将包含控件的 border 元素的高度和宽度（`ControlHostElement`）传递到 `ControlHost` 构造函数。</span><span class="sxs-lookup"><span data-stu-id="befad-188">Pass the height and width of the border element that contains the control (`ControlHostElement`) to the `ControlHost` constructor.</span></span> <span data-ttu-id="befad-189">이렇게 하면 ListBox 크기가 정확하게 조정됩니다.</span><span class="sxs-lookup"><span data-stu-id="befad-189">This ensures that the ListBox is sized correctly.</span></span> <span data-ttu-id="befad-190">然后，通过将 `ControlHost` 对象分配给主机 <xref:System.Windows.Controls.Border>的 <xref:System.Windows.Controls.Decorator.Child%2A> 属性，在页面上承载控件。</span><span class="sxs-lookup"><span data-stu-id="befad-190">You then host the control on the page by assigning the `ControlHost` object to the <xref:System.Windows.Controls.Decorator.Child%2A> property of the host <xref:System.Windows.Controls.Border>.</span></span>  
  
 <span data-ttu-id="befad-191">该示例将一个处理程序附加到 `ControlHost` 的 <xref:System.Windows.Interop.HwndHost.MessageHook> 事件，以接收来自控件的消息。</span><span class="sxs-lookup"><span data-stu-id="befad-191">The sample attaches a handler to the <xref:System.Windows.Interop.HwndHost.MessageHook> event of the `ControlHost` to receive messages from the control.</span></span> <span data-ttu-id="befad-192">이 이벤트는 호스트된 창으로 전송된 모든 메시지에 대해 발생됩니다.</span><span class="sxs-lookup"><span data-stu-id="befad-192">This event is raised for every message sent to the hosted window.</span></span> <span data-ttu-id="befad-193">이 경우 컨트롤의 알림을 포함하여 실제 ListBox 컨트롤을 래핑하는 창에 전송된 메시지입니다.</span><span class="sxs-lookup"><span data-stu-id="befad-193">In this case, these are the messages sent to window that wraps the actual ListBox control, including notifications from the control.</span></span> <span data-ttu-id="befad-194">샘플에서는 SendMessage를 호출하여 컨트롤에서 정보를 가져오고 해당 내용을 수정합니다.</span><span class="sxs-lookup"><span data-stu-id="befad-194">The sample calls SendMessage to obtain information from the control and modify its contents.</span></span> <span data-ttu-id="befad-195">페이지가 컨트롤과 통신하는 방법에 대한 자세한 내용은 다음 섹션에서 설명합니다.</span><span class="sxs-lookup"><span data-stu-id="befad-195">The details of how the page communicates with the control are discussed in the next section.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="befad-196">请注意，SendMessage 有两个 PInvoke 声明。</span><span class="sxs-lookup"><span data-stu-id="befad-196">Notice that there are two PInvoke declarations for SendMessage.</span></span> <span data-ttu-id="befad-197">这是必需的，因为一个参数使用 `wParam` 参数传递字符串，另一个参数用于传递整数。</span><span class="sxs-lookup"><span data-stu-id="befad-197">This is necessary because one uses the `wParam` parameter to pass a string and the other uses it to pass an integer.</span></span> <span data-ttu-id="befad-198">데이터가 올바르게 마샬링되도록 하려면 각 시그니처에 대해 별도의 선언이 필요합니다.</span><span class="sxs-lookup"><span data-stu-id="befad-198">You need a separate declaration for each signature to ensure that the data is marshaled correctly.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#HostWindowClass](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml.cs#hostwindowclass)]
 [!code-vb[WPFHostingWin32Control#HostWindowClass](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/Page1.xaml.vb#hostwindowclass)]  
  
 [!code-csharp[WPFHostingWin32Control#ControlMsgFilterSendMessage](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml.cs#controlmsgfiltersendmessage)]
 [!code-vb[WPFHostingWin32Control#ControlMsgFilterSendMessage](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/Page1.xaml.vb#controlmsgfiltersendmessage)]  
  
<a name="communication"></a>   
## <a name="implement-communication-between-the-control-and-the-page"></a><span data-ttu-id="befad-199">컨트롤과 페이지 간의 통신 구현</span><span class="sxs-lookup"><span data-stu-id="befad-199">Implement Communication Between the Control and the Page</span></span>  
 <span data-ttu-id="befad-200">通过向控件发送 Windows 消息来操作控件。</span><span class="sxs-lookup"><span data-stu-id="befad-200">You manipulate the control by sending it Windows messages.</span></span> <span data-ttu-id="befad-201">컨트롤은 사용자가 호스트 창에 알림을 보내 상호 작용할 때 알려 줍니다.</span><span class="sxs-lookup"><span data-stu-id="befad-201">The control notifies you when the user interacts with it by sending notifications to its host window.</span></span> <span data-ttu-id="befad-202">[在 WPF 中承载 Win32 ListBox 控件](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control)示例包含一个 UI，该 UI 提供了如何实现此操作的几个示例：</span><span class="sxs-lookup"><span data-stu-id="befad-202">The [Hosting a Win32 ListBox Control in WPF](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control) sample includes a UI that provides several examples of how this works:</span></span>  
  
- <span data-ttu-id="befad-203">목록에 항목을 추가합니다.</span><span class="sxs-lookup"><span data-stu-id="befad-203">Append an item to the list.</span></span>  
  
- <span data-ttu-id="befad-204">선택한 항목을 목록에서 삭제합니다.</span><span class="sxs-lookup"><span data-stu-id="befad-204">Delete the selected item from the list</span></span>  
  
- <span data-ttu-id="befad-205">현재 선택한 항목의 텍스트를 표시합니다.</span><span class="sxs-lookup"><span data-stu-id="befad-205">Display the text of the currently selected item.</span></span>  
  
- <span data-ttu-id="befad-206">목록의 항목 수를 표시합니다.</span><span class="sxs-lookup"><span data-stu-id="befad-206">Display the number of items in the list.</span></span>  
  
 <span data-ttu-id="befad-207">用户还可以通过单击列表框中的项来选择该项，就像在传统的 Win32 应用程序中一样。</span><span class="sxs-lookup"><span data-stu-id="befad-207">The user can also select an item in the list box by clicking on it, just as they would for a conventional Win32 application.</span></span> <span data-ttu-id="befad-208">표시된 데이터는 사용자가 항목을 선택 또는 추가하여 목록 상자의 상태를 변경할 때마다 업데이트됩니다.</span><span class="sxs-lookup"><span data-stu-id="befad-208">The displayed data is updated each time the user changes the state of the list box by selecting, adding, or appending an item.</span></span>  
  
 <span data-ttu-id="befad-209">若要追加项，请将列表框发送[`LB_ADDSTRING` 消息](/windows/desktop/Controls/lb-addstring)。</span><span class="sxs-lookup"><span data-stu-id="befad-209">To append items, send the list box an [`LB_ADDSTRING` message](/windows/desktop/Controls/lb-addstring).</span></span> <span data-ttu-id="befad-210">若要删除项目，请发送[`LB_GETCURSEL`](/windows/desktop/Controls/lb-getcursel)以获取当前所选内容的索引，然后[`LB_DELETESTRING`](/windows/desktop/Controls/lb-deletestring)删除该项目。</span><span class="sxs-lookup"><span data-stu-id="befad-210">To delete items, send [`LB_GETCURSEL`](/windows/desktop/Controls/lb-getcursel) to get the index of the current selection and then [`LB_DELETESTRING`](/windows/desktop/Controls/lb-deletestring) to delete the item.</span></span> <span data-ttu-id="befad-211">该示例还发送[`LB_GETCOUNT`](/windows/desktop/Controls/lb-getcount)，并使用返回的值更新显示项数的显示内容。</span><span class="sxs-lookup"><span data-stu-id="befad-211">The sample also sends [`LB_GETCOUNT`](/windows/desktop/Controls/lb-getcount), and uses the returned value to update the display that shows the number of items.</span></span> <span data-ttu-id="befad-212">[`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage)的两个实例都使用上一节中讨论的某个 PInvoke 声明。</span><span class="sxs-lookup"><span data-stu-id="befad-212">Both these instances of [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) use one of the PInvoke declarations discussed in the previous section.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#AppendDeleteText](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml.cs#appenddeletetext)]
 [!code-vb[WPFHostingWin32Control#AppendDeleteText](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/Page1.xaml.vb#appenddeletetext)]  
  
 <span data-ttu-id="befad-213">当用户选择某一项或更改其选择时，该控件将通过向宿主窗口发送[`WM_COMMAND` 消息](/windows/desktop/menurc/wm-command)来通知宿主窗口，这会引发该页的 <xref:System.Windows.Interop.HwndHost.MessageHook> 事件。</span><span class="sxs-lookup"><span data-stu-id="befad-213">When the user selects an item or changes their selection, the control notifies the host window by sending it a [`WM_COMMAND` message](/windows/desktop/menurc/wm-command), which raises the <xref:System.Windows.Interop.HwndHost.MessageHook> event for the page.</span></span> <span data-ttu-id="befad-214">처리기는 호스트 창의 주 창 프로시저와 같은 정보를 수신합니다.</span><span class="sxs-lookup"><span data-stu-id="befad-214">The handler receives the same information as the main window procedure of the host window.</span></span> <span data-ttu-id="befad-215">它还传递对布尔值的引用，`handled`。</span><span class="sxs-lookup"><span data-stu-id="befad-215">It also passes a reference to a Boolean value, `handled`.</span></span> <span data-ttu-id="befad-216">您将 `handled` 设置为 `true`，以指示您已经处理了该消息，并且不需要进一步的处理。</span><span class="sxs-lookup"><span data-stu-id="befad-216">You set `handled` to `true` to indicate that you have handled the message and no further processing is needed.</span></span>  
  
 <span data-ttu-id="befad-217">由于各种原因，会发送[`WM_COMMAND`](/windows/desktop/menurc/wm-command) ，因此必须检查通知 ID 以确定它是否为要处理的事件。</span><span class="sxs-lookup"><span data-stu-id="befad-217">[`WM_COMMAND`](/windows/desktop/menurc/wm-command) is sent for a variety of reasons, so you must examine the notification ID to determine whether it is an event that you wish to handle.</span></span> <span data-ttu-id="befad-218">ID 包含在 `wParam` 参数的高位字中。</span><span class="sxs-lookup"><span data-stu-id="befad-218">The ID is contained in the high word of the `wParam` parameter.</span></span> <span data-ttu-id="befad-219">该示例使用按位运算符提取 ID。</span><span class="sxs-lookup"><span data-stu-id="befad-219">The sample uses bitwise operators to extract the ID.</span></span> <span data-ttu-id="befad-220">如果用户已选择或更改了其选择，则将[`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange)ID。</span><span class="sxs-lookup"><span data-stu-id="befad-220">If the user has made or changed their selection, the ID will be [`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange).</span></span>  
  
 <span data-ttu-id="befad-221">接收到[`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange)时，此示例通过将控件发送[`LB_GETCURSEL` 消息](/windows/desktop/Controls/lb-getcursel)来获取选定项的索引。</span><span class="sxs-lookup"><span data-stu-id="befad-221">When [`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange) is received, the sample gets the index of the selected item by sending the control a [`LB_GETCURSEL` message](/windows/desktop/Controls/lb-getcursel).</span></span> <span data-ttu-id="befad-222">若要获取文本，请先创建一个 <xref:System.Text.StringBuilder>。</span><span class="sxs-lookup"><span data-stu-id="befad-222">To get the text, you first create a <xref:System.Text.StringBuilder>.</span></span> <span data-ttu-id="befad-223">然后，将控件发送[`LB_GETTEXT` 消息](/windows/desktop/Controls/lb-gettext)。</span><span class="sxs-lookup"><span data-stu-id="befad-223">You then send the control an [`LB_GETTEXT` message](/windows/desktop/Controls/lb-gettext).</span></span> <span data-ttu-id="befad-224">向 `wParam` 参数传递空的 <xref:System.Text.StringBuilder> 对象。</span><span class="sxs-lookup"><span data-stu-id="befad-224">Pass the empty <xref:System.Text.StringBuilder> object as the `wParam` parameter.</span></span> <span data-ttu-id="befad-225">当[`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage)返回时，<xref:System.Text.StringBuilder> 将包含选定项的文本。</span><span class="sxs-lookup"><span data-stu-id="befad-225">When [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) returns, the <xref:System.Text.StringBuilder> will contain the text of the selected item.</span></span> <span data-ttu-id="befad-226">[`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage)的这种用法还需要另一个 PInvoke 声明。</span><span class="sxs-lookup"><span data-stu-id="befad-226">This use of [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) requires yet another PInvoke declaration.</span></span>  
  
 <span data-ttu-id="befad-227">最后，将 `handled` 设置为 `true` 以指示消息已处理。</span><span class="sxs-lookup"><span data-stu-id="befad-227">Finally, set `handled` to `true` to indicate that the message has been handled.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="befad-228">另请参阅</span><span class="sxs-lookup"><span data-stu-id="befad-228">See also</span></span>

- <xref:System.Windows.Interop.HwndHost>
- [<span data-ttu-id="befad-229">WPF 및 Win32 상호 운용성</span><span class="sxs-lookup"><span data-stu-id="befad-229">WPF and Win32 Interoperation</span></span>](wpf-and-win32-interoperation.md)
- [<span data-ttu-id="befad-230">연습: 내 첫 WPF 데스크톱 애플리케이션</span><span class="sxs-lookup"><span data-stu-id="befad-230">Walkthrough: My first WPF desktop application</span></span>](../getting-started/walkthrough-my-first-wpf-desktop-application.md)
