---
title: 重写服务标识以便进行身份验证
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
ms.assetid: d613a22b-07d7-41a4-bada-1adc653b9b5d
ms.openlocfilehash: 5649ef4cc05c9c16b1f8f626ba5e2e584b0e52eb
ms.sourcegitcommit: 7980a91f90ae5eca859db7e6bfa03e23e76a1a50
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 04/14/2020
ms.locfileid: "81278907"
---
# <a name="override-the-identity-of-a-service-for-authentication"></a><span data-ttu-id="01937-102">重写服务标识以进行身份验证</span><span class="sxs-lookup"><span data-stu-id="01937-102">Override the identity of a service for authentication</span></span>

<span data-ttu-id="01937-103">通常情况下不需要设置服务上的标识，因为客户端凭据类型的选择即规定了服务元数据中公开的标识的类型。</span><span class="sxs-lookup"><span data-stu-id="01937-103">Typically, you do not have to set the identity on a service because the selection of a client credential type dictates the type of identity exposed in the service metadata.</span></span> <span data-ttu-id="01937-104">例如，以下配置代码使用[\<wsHttpBinding>](../../configure-apps/file-schema/wcf/wshttpbinding.md)元素并将`clientCredentialType`属性设置到 Windows。</span><span class="sxs-lookup"><span data-stu-id="01937-104">For example, the following configuration code uses the [\<wsHttpBinding>](../../configure-apps/file-schema/wcf/wshttpbinding.md) element and sets the `clientCredentialType` attribute to Windows.</span></span>  

 <span data-ttu-id="01937-105">下面的 Web 服务描述语言 (WSDL) 片断演示前面定义的终结点的标识。</span><span class="sxs-lookup"><span data-stu-id="01937-105">The following Web Services Description Language (WSDL) fragment shows the identity for the endpoint previously defined.</span></span> <span data-ttu-id="01937-106">在此示例中，服务作为自托管服务在特定用户帐户 （username@contoso.com） 下运行，因此用户主体名称 （UPN） 标识包含帐户名称。</span><span class="sxs-lookup"><span data-stu-id="01937-106">In this example, the service is running as a self-hosted service under a particular user account (username@contoso.com) and therefore the user principal name (UPN) identity contains the account name.</span></span> <span data-ttu-id="01937-107">UPN 也称为 Windows 域中的用户登录名称。</span><span class="sxs-lookup"><span data-stu-id="01937-107">The UPN is also known as the user sign-in name in a Windows domain.</span></span>  

 <span data-ttu-id="01937-108">有关演示标识设置的示例应用程序，请参阅[服务标识示例](../samples/service-identity-sample.md)。</span><span class="sxs-lookup"><span data-stu-id="01937-108">For a sample application that demonstrates identity setting, see [Service Identity Sample](../samples/service-identity-sample.md).</span></span> <span data-ttu-id="01937-109">有关服务标识的详细信息，请参阅[服务标识和身份验证](../feature-details/service-identity-and-authentication.md)。</span><span class="sxs-lookup"><span data-stu-id="01937-109">For more information about service identity, see [Service Identity and Authentication](../feature-details/service-identity-and-authentication.md).</span></span>  
  
## <a name="kerberos-authentication-and-identity"></a><span data-ttu-id="01937-110">Kerberos 身份验证和标识</span><span class="sxs-lookup"><span data-stu-id="01937-110">Kerberos Authentication and Identity</span></span>  
 <span data-ttu-id="01937-111">默认情况下，当服务配置为使用 Windows 凭据时，在 WSDL 中生成包含[\<用户主体名称>](../../configure-apps/file-schema/wcf/userprincipalname.md)[\<或服务主体名称>](../../configure-apps/file-schema/wcf/serviceprincipalname.md)元素的[\<标识>](../../configure-apps/file-schema/wcf/identity.md)元素。</span><span class="sxs-lookup"><span data-stu-id="01937-111">By default, when a service is configured to use a Windows credential, an [\<identity>](../../configure-apps/file-schema/wcf/identity.md) element that contains a [\<userPrincipalName>](../../configure-apps/file-schema/wcf/userprincipalname.md) or [\<servicePrincipalName>](../../configure-apps/file-schema/wcf/serviceprincipalname.md) element is generated in the WSDL.</span></span> <span data-ttu-id="01937-112">如果服务`LocalSystem`在 、`LocalService`或`NetworkService`帐户下运行，则默认情况下以`host/`\<*主机名*>形式生成服务主体名称 （SPN），因为这些帐户有权访问计算机的 SPN 数据。</span><span class="sxs-lookup"><span data-stu-id="01937-112">If the service is running under the `LocalSystem`, `LocalService`, or `NetworkService` account, a service principal name (SPN) is generated by default in the form of `host/`\<*hostname*> because those accounts have access to the computer's SPN data.</span></span> <span data-ttu-id="01937-113">如果服务在不同的帐户下运行，Windows 通信基金会 （WCF） 会以\<*用户名*>@<*域名*`>`的形式生成 UPN。</span><span class="sxs-lookup"><span data-stu-id="01937-113">If the service is running under a different account, Windows Communication Foundation (WCF) generates a UPN in the form of \<*username*>@<*domainName*`>`.</span></span> <span data-ttu-id="01937-114">发生这种情况的原因是 Kerberos 身份验证要求向客户端提供 UPN 或 SPN，以便对服务进行身份验证。</span><span class="sxs-lookup"><span data-stu-id="01937-114">This occurs because Kerberos authentication requires that a UPN or SPN be supplied to the client to authenticate the service.</span></span>  
  
 <span data-ttu-id="01937-115">您还可以使用[Setpn](https://docs.microsoft.com/previous-versions/windows/it-pro/windows-server-2008-R2-and-2008/cc731241(v=ws.10)?redirectedfrom=MSDN)工具在域中使用服务的帐户注册其他 SPN。</span><span class="sxs-lookup"><span data-stu-id="01937-115">You can also use the [Setspn](https://docs.microsoft.com/previous-versions/windows/it-pro/windows-server-2008-R2-and-2008/cc731241(v=ws.10)?redirectedfrom=MSDN) tool to register an additional SPN with a service's account in a domain.</span></span> <span data-ttu-id="01937-116">然后，可以使用该 SPN 作为服务的标识。</span><span class="sxs-lookup"><span data-stu-id="01937-116">You can then use the SPN as the identity of the service.</span></span> <span data-ttu-id="01937-117">有关该工具的详细信息，请参阅[Setpn 概述](https://docs.microsoft.com/previous-versions/windows/it-pro/windows-server-2003/cc773257(v=ws.10))。</span><span class="sxs-lookup"><span data-stu-id="01937-117">For more information about the tool, see [Setspn Overview](https://docs.microsoft.com/previous-versions/windows/it-pro/windows-server-2003/cc773257(v=ws.10)).</span></span>  
  
> [!NOTE]
> <span data-ttu-id="01937-118">若要使用不带协商的 Windows 凭据类型，服务的用户帐户必须有权访问向 Active Directory 域注册的 SPN。</span><span class="sxs-lookup"><span data-stu-id="01937-118">To use the Windows credential type without negotiation, the service's user account must have access to the SPN that is registered with the Active Directory domain.</span></span> <span data-ttu-id="01937-119">可以使用下列方式来实现：</span><span class="sxs-lookup"><span data-stu-id="01937-119">You can do this in the following ways:</span></span>  
  
- <span data-ttu-id="01937-120">使用 NetworkService 或 LocalSystem 帐户运行服务。</span><span class="sxs-lookup"><span data-stu-id="01937-120">Use the NetworkService or LocalSystem account to run your service.</span></span> <span data-ttu-id="01937-121">由于这些帐户有权访问计算机加入 Active Directory 域时建立的计算机 SPN，因此 WCF 会自动在服务的元数据 （WSDL） 中的服务终结点内生成正确的 SPN 元素。</span><span class="sxs-lookup"><span data-stu-id="01937-121">Because those accounts have access to the machine SPN that is established when the machine joins the Active Directory domain, WCF automatically generates the proper SPN element inside the service's endpoint in the service's metadata (WSDL).</span></span>  
  
- <span data-ttu-id="01937-122">使用任意 Active Directory 域帐户运行服务。</span><span class="sxs-lookup"><span data-stu-id="01937-122">Use an arbitrary Active Directory domain account to run your service.</span></span> <span data-ttu-id="01937-123">在这种情况下，需要为该域帐户建立一个 SPN，这可以使用 Setspn.exe 实用工具来完成。</span><span class="sxs-lookup"><span data-stu-id="01937-123">In this case, establish an SPN for that domain account, which you can do by using the Setspn.exe utility tool.</span></span> <span data-ttu-id="01937-124">为服务的帐户创建 SPN 后，将 WCF 配置为通过其元数据 （WSDL） 将该 SPN 发布到服务的客户端。</span><span class="sxs-lookup"><span data-stu-id="01937-124">Once you create the SPN for the service's account, configure WCF to publish that SPN to the service's clients through its metadata (WSDL).</span></span> <span data-ttu-id="01937-125">这可以通过为公开的终结点设置终结点标识（借助于应用程序配置文件或代码）来完成。</span><span class="sxs-lookup"><span data-stu-id="01937-125">This is done by setting the endpoint identity for the exposed endpoint, either through an application configuration file or code.</span></span>  
  
 <span data-ttu-id="01937-126">有关 SPN、Kerberos 协议和活动目录的详细信息，请参阅[Windows 的 Kerberos 技术补充](https://docs.microsoft.com/previous-versions/msp-n-p/ff649429(v=pandp.10))。</span><span class="sxs-lookup"><span data-stu-id="01937-126">For more information about SPNs, the Kerberos protocol, and Active Directory, see [Kerberos Technical Supplement for Windows](https://docs.microsoft.com/previous-versions/msp-n-p/ff649429(v=pandp.10)).</span></span>  
  
### <a name="when-spn-or-upn-equals-the-empty-string"></a><span data-ttu-id="01937-127">当 SPN 或 UPN 等于空字符串时</span><span class="sxs-lookup"><span data-stu-id="01937-127">When SPN or UPN Equals the Empty String</span></span>  
 <span data-ttu-id="01937-128">如果将 SPN 或 UPN 设置为等于空字符串，将出现多种不同的情况，具体取决于所使用的安全级别和身份验证模式：</span><span class="sxs-lookup"><span data-stu-id="01937-128">If you set the SPN or UPN equal to an empty string, a number of different things happen, depending on the security level and authentication mode being used:</span></span>  
  
- <span data-ttu-id="01937-129">如果使用传输级安全，则会选择 NT LanMan (NTLM) 身份验证。</span><span class="sxs-lookup"><span data-stu-id="01937-129">If you are using transport level security, NT LanMan (NTLM) authentication is chosen.</span></span>  
  
- <span data-ttu-id="01937-130">如果使用消息级安全，则根据身份验证模式的不同，身份验证可能会失败：</span><span class="sxs-lookup"><span data-stu-id="01937-130">If you are using message level security, authentication may fail, depending on the authentication mode:</span></span>  
  
- <span data-ttu-id="01937-131">如果使用`spnego`模式，并且`AllowNtlm`属性设置为`false`，身份验证将失败。</span><span class="sxs-lookup"><span data-stu-id="01937-131">If you are using `spnego` mode and the `AllowNtlm` attribute is set to `false`, authentication fails.</span></span>  
  
- <span data-ttu-id="01937-132">如果使用`spnego`模式，并且`AllowNtlm`属性设置为`true`，如果 UPN 为空，则身份验证将失败，但如果 SPN 为空，则身份验证将成功。</span><span class="sxs-lookup"><span data-stu-id="01937-132">If you are using `spnego` mode and the `AllowNtlm` attribute is set to `true`, authentication fails if the UPN is empty but succeeds if the SPN is empty.</span></span>  
  
- <span data-ttu-id="01937-133">如果使用 Kerberos direct（也称为“一次完成”），则身份验证将失败。</span><span class="sxs-lookup"><span data-stu-id="01937-133">If you are using Kerberos direct (also known as "one-shot"), authentication fails.</span></span>  
  
### <a name="use-the-identity-element-in-configuration"></a><span data-ttu-id="01937-134">在\<配置中使用标识>元素</span><span class="sxs-lookup"><span data-stu-id="01937-134">Use the \<identity> Element in Configuration</span></span>  
 <span data-ttu-id="01937-135">如果在以前显示的绑定中更改客户端凭据类型`Certificate`，则生成的 WSDL 包含标识值的 Base64 序列化 X.509 证书，如下代码所示。</span><span class="sxs-lookup"><span data-stu-id="01937-135">If you change the client credential type in the binding previously shown to `Certificate`, then the generated WSDL contains a Base64 serialized X.509 certificate for the identity value as shown in the following code.</span></span> <span data-ttu-id="01937-136">这是除 Windows 之外的所有客户端凭据类型的默认值。</span><span class="sxs-lookup"><span data-stu-id="01937-136">This is the default for all client credential types other than Windows.</span></span>  

 <span data-ttu-id="01937-137">您可以通过在配置中使用<>`identity`元素或在代码中设置标识来更改默认服务标识的值或更改标识的类型。</span><span class="sxs-lookup"><span data-stu-id="01937-137">You can change the value of the default service identity or change the type of the identity by using the <`identity`> element in configuration or by setting the identity in code.</span></span> <span data-ttu-id="01937-138">下面的配置代码使用值 `contoso.com` 设置域名系统 (DNS) 标识。</span><span class="sxs-lookup"><span data-stu-id="01937-138">The following configuration code sets a domain name system (DNS) identity with the value `contoso.com`.</span></span>  

### <a name="set-identity-programmatically"></a><span data-ttu-id="01937-139">以编程方式设置标识</span><span class="sxs-lookup"><span data-stu-id="01937-139">Set Identity Programmatically</span></span>  
 <span data-ttu-id="01937-140">服务不必显式指定标识，因为 WCF 会自动确定它。</span><span class="sxs-lookup"><span data-stu-id="01937-140">Your service does not have to explicitly specify an identity, because WCF automatically determines it.</span></span> <span data-ttu-id="01937-141">但是，如有必要，WCF 允许您在终结点上指定标识。</span><span class="sxs-lookup"><span data-stu-id="01937-141">However, WCF allows you to specify an identity on an endpoint, if necessary.</span></span> <span data-ttu-id="01937-142">下面的代码使用特定的 DNS 标识添加了一个新的服务终结点。</span><span class="sxs-lookup"><span data-stu-id="01937-142">The following code adds a new service endpoint with a specific DNS identity.</span></span>  
  
 [!code-csharp[C_Identity#5](../../../../samples/snippets/csharp/VS_Snippets_CFX/c_identity/cs/source.cs#5)]
 [!code-vb[C_Identity#5](../../../../samples/snippets/visualbasic/VS_Snippets_CFX/c_identity/vb/source.vb#5)]  
  
## <a name="see-also"></a><span data-ttu-id="01937-143">另请参阅</span><span class="sxs-lookup"><span data-stu-id="01937-143">See also</span></span>

- [<span data-ttu-id="01937-144">如何：创建自定义客户端标识验证工具</span><span class="sxs-lookup"><span data-stu-id="01937-144">How to: Create a Custom Client Identity Verifier</span></span>](how-to-create-a-custom-client-identity-verifier.md)
- [<span data-ttu-id="01937-145">服务标识和身份验证</span><span class="sxs-lookup"><span data-stu-id="01937-145">Service Identity and Authentication</span></span>](../feature-details/service-identity-and-authentication.md)
