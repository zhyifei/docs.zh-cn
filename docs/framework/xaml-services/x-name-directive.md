---
title: x:Name 指令
ms.date: 03/30/2017
f1_keywords:
- x:Name
- xName
- Name
helpviewer_keywords:
- x:Name attribute [XAML Services]
- XAML [XAML Services], x:Name attribute
- Name attribute in XAML [XAML Services]
ms.assetid: b7e61222-e8cf-48d2-acd0-6df3b7685d48
ms.openlocfilehash: d17d977384962980839fbe2b2c0a4708412d403d
ms.sourcegitcommit: a4f9b754059f0210e29ae0578363a27b9ba84b64
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 12/05/2019
ms.locfileid: "74837215"
---
# <a name="xname-directive"></a><span data-ttu-id="dab55-102">x:Name 指令</span><span class="sxs-lookup"><span data-stu-id="dab55-102">x:Name Directive</span></span>
<span data-ttu-id="dab55-103">在 XAML 名称范围中唯一标识 XAML 定义的元素。</span><span class="sxs-lookup"><span data-stu-id="dab55-103">Uniquely identifies XAML-defined elements in a XAML namescope.</span></span> <span data-ttu-id="dab55-104">当框架提供 Api 或实现在运行时访问 XAML 创建的对象图的行为时，可以将 XAML 名称范围及其唯一性模型应用于实例化的对象。</span><span class="sxs-lookup"><span data-stu-id="dab55-104">XAML namescopes and their uniqueness models can be applied to the instantiated objects, when frameworks provide APIs or implement behaviors that access the XAML-created object graph at run time.</span></span>  
  
## <a name="xaml-attribute-usage"></a><span data-ttu-id="dab55-105">XAML 特性用法</span><span class="sxs-lookup"><span data-stu-id="dab55-105">XAML Attribute Usage</span></span>  
  
```xaml  
<object x:Name="XAMLNameValue".../>  
```  
  
## <a name="xaml-values"></a><span data-ttu-id="dab55-106">XAML 值</span><span class="sxs-lookup"><span data-stu-id="dab55-106">XAML Values</span></span>  
  
|||  
|-|-|  
|`XAMLNameValue`|<span data-ttu-id="dab55-107">符合[XamlName 语法](xamlname-grammar.md)限制的字符串。</span><span class="sxs-lookup"><span data-stu-id="dab55-107">A string that conforms to the restrictions of the [XamlName Grammar](xamlname-grammar.md).</span></span>|  
  
## <a name="remarks"></a><span data-ttu-id="dab55-108">备注</span><span class="sxs-lookup"><span data-stu-id="dab55-108">Remarks</span></span>  
 <span data-ttu-id="dab55-109">将 `x:Name` 应用于框架的后备编程模型后，该名称等效于包含构造函数返回的对象引用或实例的变量。</span><span class="sxs-lookup"><span data-stu-id="dab55-109">After `x:Name` is applied to a framework's backing programming model, the name is equivalent to the variable that holds an object reference or an instance as returned by a constructor.</span></span>  
  
 <span data-ttu-id="dab55-110">`x:Name` 指令用法的值在 XAML 名称范围内必须是唯一的。</span><span class="sxs-lookup"><span data-stu-id="dab55-110">The value of an `x:Name` directive usage must be unique within a XAML namescope.</span></span> <span data-ttu-id="dab55-111">默认情况下，当由 .NET Framework XAML 服务 API 使用时，主 XAML 名称范围将在单个 XAML 生产的 XAML 根元素中定义，并且包含该 XAML 生产中包含的元素。</span><span class="sxs-lookup"><span data-stu-id="dab55-111">By default when used by .NET Framework XAML Services API, the primary XAML namescope is defined at the XAML root element of a single XAML production, and encompasses the elements that are contained in that XAML production.</span></span> <span data-ttu-id="dab55-112">可能出现在单个 XAML 生产内的其他离散 XAML 名称范围可以由框架定义，以解决特定方案。</span><span class="sxs-lookup"><span data-stu-id="dab55-112">Additional discrete XAML namescopes that might occur within a single XAML production can be defined by frameworks to address specific scenarios.</span></span> <span data-ttu-id="dab55-113">例如，在 WPF 中，新的 XAML 名称范围由也在该 XAML 生产环境中定义的任何模板来定义和创建。</span><span class="sxs-lookup"><span data-stu-id="dab55-113">For example, in WPF, new XAML namescopes are defined and created by any template that is also defined on that XAML production.</span></span> <span data-ttu-id="dab55-114">有关 XAML 名称范围（针对 WPF 编写但与许多 XAML 名称范围概念相关的详细信息），请参阅[WPF XAML 名称范围](../wpf/advanced/wpf-xaml-namescopes.md)。</span><span class="sxs-lookup"><span data-stu-id="dab55-114">For more information about XAML namescopes (written for WPF but relevant for many XAML namescope concepts), see [WPF XAML Namescopes](../wpf/advanced/wpf-xaml-namescopes.md).</span></span>  
  
 <span data-ttu-id="dab55-115">通常，不应在也使用 `x:Key`的情况下应用 `x:Name`。</span><span class="sxs-lookup"><span data-stu-id="dab55-115">In general, `x:Name` should not be applied in situations that also use `x:Key`.</span></span> <span data-ttu-id="dab55-116">特定现有框架的 XAML 实现在 `x:Key` 和 `x:Name`之间引入了替代概念，但这不是建议的做法。</span><span class="sxs-lookup"><span data-stu-id="dab55-116">XAML implementations by specific existing frameworks have introduced substitution concepts between `x:Key` and `x:Name`, but that is not a recommended practice.</span></span> <span data-ttu-id="dab55-117">在处理名称/键信息（如 <xref:System.Windows.Markup.INameScope> 或 <xref:System.Windows.Markup.DictionaryKeyPropertyAttribute>）时，.NET Framework XAML 服务不支持这种替换概念。</span><span class="sxs-lookup"><span data-stu-id="dab55-117">.NET Framework XAML Services does not support such substitution concepts when handling name/key information such as <xref:System.Windows.Markup.INameScope> or <xref:System.Windows.Markup.DictionaryKeyPropertyAttribute>.</span></span>  
  
 <span data-ttu-id="dab55-118">`x:Name` 的 permittance 和名称唯一性强制的规则可能由特定的实现框架定义。</span><span class="sxs-lookup"><span data-stu-id="dab55-118">Rules for permittance of `x:Name` as well as the name uniqueness enforcement are potentially defined by specific implementing frameworks.</span></span> <span data-ttu-id="dab55-119">但是，若要与 .NET Framework XAML 服务一起使用，XAML 名称范围唯一性的框架定义应该与本文档中 <xref:System.Windows.Markup.INameScope> 信息的定义一致，并且应使用与信息应用位置相同的规则。</span><span class="sxs-lookup"><span data-stu-id="dab55-119">However, to be usable with .NET Framework XAML Services, the framework definitions of XAML namescope uniqueness should be consistent with the definition of <xref:System.Windows.Markup.INameScope> information in this documentation, and should use the same rules regarding where the information is applied.</span></span> <span data-ttu-id="dab55-120">例如，[!INCLUDE[TLA#tla_winclient](../../../includes/tlasharptla-winclient-md.md)] 实现将各种标记元素分成不同的 <xref:System.Windows.NameScope> 范围，如资源字典、页面级别 XAML 创建的逻辑树、模板和其他延迟的内容，然后在每个 XAML 名称范围内强制实施 XAML 名称唯一性。</span><span class="sxs-lookup"><span data-stu-id="dab55-120">For example, the [!INCLUDE[TLA#tla_winclient](../../../includes/tlasharptla-winclient-md.md)] implementation divides various markup elements into separate <xref:System.Windows.NameScope> ranges, such as resource dictionaries, the logical tree created by the page-level XAML, templates, and other deferred content, and then enforces XAML name uniqueness within each of those XAML namescopes.</span></span>  
  
 <span data-ttu-id="dab55-121">对于使用 .NET Framework XAML 服务 XAML 对象编写器的自定义类型，可以建立或更改映射到类型的 `x:Name` 的属性。</span><span class="sxs-lookup"><span data-stu-id="dab55-121">For custom types that use .NET Framework XAML Services XAML object writers, a property that maps to `x:Name` on a type can be established or changed.</span></span> <span data-ttu-id="dab55-122">通过在类型定义代码中引用要 <xref:System.Windows.Markup.RuntimeNamePropertyAttribute> 映射的属性的名称来定义此行为。</span><span class="sxs-lookup"><span data-stu-id="dab55-122">You define this behavior by referencing the name of the property to map with the <xref:System.Windows.Markup.RuntimeNamePropertyAttribute> in the type definition code.</span></span>  <span data-ttu-id="dab55-123"><xref:System.Windows.Markup.RuntimeNamePropertyAttribute> 是一个类型级特性。</span><span class="sxs-lookup"><span data-stu-id="dab55-123"><xref:System.Windows.Markup.RuntimeNamePropertyAttribute> is a type-level attribute.</span></span>  
  
 <span data-ttu-id="dab55-124">Using.NET Framework XAML 服务，可通过实现 <xref:System.Windows.Markup.INameScope> 接口以非特定于框架的方式定义 XAML 名称范围支持的支持逻辑。</span><span class="sxs-lookup"><span data-stu-id="dab55-124">Using.NET Framework XAML Services, the backing logic for XAML namescope support can be defined in a framework-neutral way by implementing the <xref:System.Windows.Markup.INameScope> interface.</span></span>  
  
## <a name="wpf-usage-notes"></a><span data-ttu-id="dab55-125">WPF 使用说明</span><span class="sxs-lookup"><span data-stu-id="dab55-125">WPF Usage Notes</span></span>  
 <span data-ttu-id="dab55-126">在使用 XAML、分部类和代码隐藏的 [!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] 应用程序的标准生成配置下，指定的 `x:Name` 成为在标记编译生成任务处理 [!INCLUDE[TLA2#tla_xaml](../../../includes/tla2sharptla-xaml-md.md)] 时在基础代码中创建的字段的名称，并且该字段包含对该对象的引用。</span><span class="sxs-lookup"><span data-stu-id="dab55-126">Under the standard build configuration for a [!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] application that uses XAML, partial classes, and code-behind, the specified `x:Name` becomes the name of a field that is created in the underlying code when [!INCLUDE[TLA2#tla_xaml](../../../includes/tla2sharptla-xaml-md.md)] is processed by a markup compilation build task, and that field holds a reference to the object.</span></span> <span data-ttu-id="dab55-127">默认情况下，创建的字段为内部字段。</span><span class="sxs-lookup"><span data-stu-id="dab55-127">By default, the created field is internal.</span></span> <span data-ttu-id="dab55-128">可以通过指定[x:FieldModifier 属性](x-fieldmodifier-directive.md)来更改字段访问权限。</span><span class="sxs-lookup"><span data-stu-id="dab55-128">You can change field access by specifying the [x:FieldModifier attribute](x-fieldmodifier-directive.md).</span></span> <span data-ttu-id="dab55-129">在 WPF 和 Silverlight 中，序列是标记编译定义并命名分部类中的字段，但该值最初为空。</span><span class="sxs-lookup"><span data-stu-id="dab55-129">In WPF and Silverlight, the sequence is that the markup compile defines and names the field in a partial class, but the value is initially empty.</span></span> <span data-ttu-id="dab55-130">然后，从类构造函数中调用名为 `InitializeComponent` 的生成方法。</span><span class="sxs-lookup"><span data-stu-id="dab55-130">Then, a generated method named `InitializeComponent` is called from within the class constructor.</span></span> <span data-ttu-id="dab55-131">`InitializeComponent` 包含 `FindName` 调用，它们使用在分部类的 XAML 定义部分中存在的每个 `x:Name` 值作为输入字符串。</span><span class="sxs-lookup"><span data-stu-id="dab55-131">`InitializeComponent` consists of `FindName` calls using each of the `x:Name` values that exist in the XAML-defined part of the partial class as input strings.</span></span> <span data-ttu-id="dab55-132">然后，将返回值分配给赞命名字段引用，以用通过 XAML 分析创建的对象填充字段值。</span><span class="sxs-lookup"><span data-stu-id="dab55-132">The return values are then assigned to the like-named field reference to fill the field values with objects that were created from XAML parsing.</span></span> <span data-ttu-id="dab55-133">执行 `InitializeComponent` 使你可以直接使用 `x:Name`/字段名称引用运行时对象关系图，而不必在需要引用 XAML 定义的对象时显式调用 `FindName`。</span><span class="sxs-lookup"><span data-stu-id="dab55-133">The execution of `InitializeComponent` make it possible to reference the run time object graph using the `x:Name` / field name directly, rather than having to call `FindName` explicitly any time you need a reference to a XAML-defined object.</span></span>  
  
 <span data-ttu-id="dab55-134">对于使用 Microsoft Visual Basic 目标的 WPF 应用程序，并包含具有 `Page` 生成操作的 XAML 文件，在编译过程中将创建一个单独的引用属性，该属性将 `WithEvents` 关键字添加到具有 `x:Name`的所有元素，以支持事件处理程序委托的 `Handles` 语法。</span><span class="sxs-lookup"><span data-stu-id="dab55-134">For a WPF application that uses the Microsoft Visual Basic targets and includes XAML files with `Page` build action, a separate reference property is created during compilation that adds the `WithEvents` keyword to all elements that have an `x:Name`, to support `Handles` syntax for event handler delegates.</span></span> <span data-ttu-id="dab55-135">此属性始终是公共的。</span><span class="sxs-lookup"><span data-stu-id="dab55-135">This property is always public.</span></span> <span data-ttu-id="dab55-136">有关详细信息，请参阅 [Visual Basic 和 WPF 事件处理](../wpf/advanced/visual-basic-and-wpf-event-handling.md)。</span><span class="sxs-lookup"><span data-stu-id="dab55-136">For more information, see [Visual Basic and WPF Event Handling](../wpf/advanced/visual-basic-and-wpf-event-handling.md).</span></span>  
  
 <span data-ttu-id="dab55-137">WPF XAML 处理器使用 `x:Name` 在加载时将名称注册到 XAML 名称范围，即使在页面未通过生成操作进行标记编译（例如，资源字典的松散 XAML）时也是如此。</span><span class="sxs-lookup"><span data-stu-id="dab55-137">`x:Name` is used by the WPF XAML processor to register a name into a XAML namescope at load time, even for cases where the page is not markup-compiled by build actions (for example, loose XAML of a resource dictionary).</span></span> <span data-ttu-id="dab55-138">此行为的一个原因是因为 `x:Name` 可能需要 <xref:System.Windows.Data.Binding.ElementName%2A> 绑定。</span><span class="sxs-lookup"><span data-stu-id="dab55-138">One reason for this behavior is because the `x:Name` is potentially needed for <xref:System.Windows.Data.Binding.ElementName%2A> binding.</span></span> <span data-ttu-id="dab55-139">有关详细信息，请参阅[数据绑定概述](../../desktop-wpf/data/data-binding-overview.md)。</span><span class="sxs-lookup"><span data-stu-id="dab55-139">For details, see [Data Binding Overview](../../desktop-wpf/data/data-binding-overview.md).</span></span>  
  
 <span data-ttu-id="dab55-140">如前所述，在也使用 `x:Key`的情况下，不应应用 `x:Name` （或 `Name`）。</span><span class="sxs-lookup"><span data-stu-id="dab55-140">As mentioned previously, `x:Name` (or `Name`) should not be applied in situations that also use `x:Key`.</span></span> <span data-ttu-id="dab55-141">[!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] <xref:System.Windows.ResourceDictionary> 具有将自身定义为 XAML 名称范围的特殊行为，但不会为 <xref:System.Windows.Markup.INameScope> Api 返回值或 null 值，以实现此行为。</span><span class="sxs-lookup"><span data-stu-id="dab55-141">The [!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] <xref:System.Windows.ResourceDictionary> has a special behavior of defining itself as a XAML namescope but returning Not Implemented or null values for <xref:System.Windows.Markup.INameScope> APIs as a way to enforce this behavior.</span></span> <span data-ttu-id="dab55-142">如果 WPF XAML 分析器在 XAML 定义的 <xref:System.Windows.ResourceDictionary>中遇到 `Name` 或 `x:Name`，则不会将该名称添加到任何 XAML 名称范围。</span><span class="sxs-lookup"><span data-stu-id="dab55-142">If the WPF XAML parser encounters `Name` or `x:Name` in a XAML-defined <xref:System.Windows.ResourceDictionary>, the name is not added to any XAML namescope.</span></span> <span data-ttu-id="dab55-143">尝试从任何 XAML 名称范围和 `FindName` 方法中查找该名称将不会返回有效的结果。</span><span class="sxs-lookup"><span data-stu-id="dab55-143">Attempting to find that name from any XAML namescope and the `FindName` methods will not return valid results.</span></span>  
  
### <a name="xname-and-name"></a><span data-ttu-id="dab55-144">X：Name 和名称</span><span class="sxs-lookup"><span data-stu-id="dab55-144">x:Name and Name</span></span>  
 <span data-ttu-id="dab55-145">很多 WPF 应用程序方案都可以避免任何使用 `x:Name` 特性，因为在默认 XAML 命名空间中为多个重要基类（如 <xref:System.Windows.FrameworkElement> 和 <xref:System.Windows.FrameworkContentElement>）指定的 `Name` 依赖项属性将满足此相同目的。</span><span class="sxs-lookup"><span data-stu-id="dab55-145">Many WPF application scenarios can avoid any use of the `x:Name` attribute, because the `Name` dependency property as specified in the default XAML namespace for several of the important base classes such as <xref:System.Windows.FrameworkElement> and <xref:System.Windows.FrameworkContentElement> satisfies this same purpose.</span></span> <span data-ttu-id="dab55-146">还有一些常见 XAML 和 WPF 方案，在这些方案中，代码访问框架级别上没有 `Name` 属性的元素是非常重要的。</span><span class="sxs-lookup"><span data-stu-id="dab55-146">There are still some common XAML and WPF scenarios where code access to an element with no `Name` property at the framework level is important.</span></span> <span data-ttu-id="dab55-147">例如，某些动画和情节提要支持类不支持 `Name` 属性，但通常需要在代码中引用这些类才能控制动画。</span><span class="sxs-lookup"><span data-stu-id="dab55-147">For example, certain animation and storyboard support classes do not support a `Name` property, but they often need to be referenced in code in order to control the animation.</span></span> <span data-ttu-id="dab55-148">如果你打算稍后从代码中引用它们，则应将 `x:Name` 指定为在 XAML 中创建的时间线和转换的属性。</span><span class="sxs-lookup"><span data-stu-id="dab55-148">You should specify `x:Name` as an attribute on timelines and transforms that are created in XAML, if you intend to reference them from code later.</span></span>  
  
 <span data-ttu-id="dab55-149">如果 <xref:System.Windows.FrameworkElement.Name%2A> 作为类的属性提供，则可以将 <xref:System.Windows.FrameworkElement.Name%2A> 和 `x:Name` 交换为属性，但如果两者都在同一个元素上指定，则将导致分析异常。</span><span class="sxs-lookup"><span data-stu-id="dab55-149">If <xref:System.Windows.FrameworkElement.Name%2A> is available as a property on the class, <xref:System.Windows.FrameworkElement.Name%2A> and `x:Name` can be used interchangeably as attributes, but a parse exception will result if both are specified on the same element.</span></span> <span data-ttu-id="dab55-150">如果 XAML 是标记编译的，则异常将在标记编译中发生，否则会在加载时发生。</span><span class="sxs-lookup"><span data-stu-id="dab55-150">If the XAML is markup compiled, the exception will occur on the markup compile, otherwise it occurs on load.</span></span>  
  
 <span data-ttu-id="dab55-151">可以使用 XAML 特性语法和使用 <xref:System.Windows.DependencyObject.SetValue%2A>的代码来设置 <xref:System.Windows.FrameworkElement.Name%2A>;但请注意，在代码中设置 <xref:System.Windows.FrameworkElement.Name%2A> 属性不会在已加载 XAML 的大多数情况下在 XAML 名称范围内创建代表字段引用。</span><span class="sxs-lookup"><span data-stu-id="dab55-151"><xref:System.Windows.FrameworkElement.Name%2A> can be set using XAML attribute syntax, and in code using <xref:System.Windows.DependencyObject.SetValue%2A>; note however that setting the <xref:System.Windows.FrameworkElement.Name%2A> property in code does not create the representative field reference within the XAML namescope in most circumstances where the XAML is already loaded.</span></span> <span data-ttu-id="dab55-152">不要尝试在代码中设置 <xref:System.Windows.FrameworkElement.Name%2A>，而是根据相应的名称范围使用代码中的 <xref:System.Windows.NameScope> 方法。</span><span class="sxs-lookup"><span data-stu-id="dab55-152">Instead of attempting to set <xref:System.Windows.FrameworkElement.Name%2A> in code, use <xref:System.Windows.NameScope> methods from code, against the appropriate namescope.</span></span>  
  
 <span data-ttu-id="dab55-153">还可以通过对内部文本使用属性元素语法来设置 <xref:System.Windows.FrameworkElement.Name%2A>，但这种情况并不常见。</span><span class="sxs-lookup"><span data-stu-id="dab55-153"><xref:System.Windows.FrameworkElement.Name%2A> can also be set using property element syntax with inner text, but that is uncommon.</span></span> <span data-ttu-id="dab55-154">相反，不能在 XAML 属性元素语法中或使用 <xref:System.Windows.DependencyObject.SetValue%2A>的代码中设置 `x:Name`;它只能在对象上使用特性语法来设置，因为它是一个指令。</span><span class="sxs-lookup"><span data-stu-id="dab55-154">In contrast, `x:Name` cannot be set in XAML property element syntax, or in code using <xref:System.Windows.DependencyObject.SetValue%2A>; it can only be set using attribute syntax on objects because it is a directive.</span></span>  
  
## <a name="silverlight-usage-notes"></a><span data-ttu-id="dab55-155">Silverlight Usage 备注。</span><span class="sxs-lookup"><span data-stu-id="dab55-155">Silverlight Usage Notes</span></span>  
 <span data-ttu-id="dab55-156">单独说明了 `x:Name` for Silverlight。</span><span class="sxs-lookup"><span data-stu-id="dab55-156">`x:Name` for Silverlight is documented separately.</span></span> <span data-ttu-id="dab55-157">有关详细信息，请参阅[XAML 命名空间（x：）语言功能（Silverlight）](https://docs.microsoft.com/previous-versions/windows/silverlight/dotnet-windows-silverlight/cc188995(v=vs.95))。</span><span class="sxs-lookup"><span data-stu-id="dab55-157">For more information, see [XAML Namespace (x:) Language Features (Silverlight)](https://docs.microsoft.com/previous-versions/windows/silverlight/dotnet-windows-silverlight/cc188995(v=vs.95)).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="dab55-158">另请参阅</span><span class="sxs-lookup"><span data-stu-id="dab55-158">See also</span></span>

- <xref:System.Windows.FrameworkElement.Name%2A?displayProperty=nameWithType>
- <xref:System.Windows.FrameworkContentElement.Name%2A?displayProperty=nameWithType>
- [<span data-ttu-id="dab55-159">WPF 中的树</span><span class="sxs-lookup"><span data-stu-id="dab55-159">Trees in WPF</span></span>](../wpf/advanced/trees-in-wpf.md)
