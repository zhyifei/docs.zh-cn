---
title: .NET Framework XAML 服务的 XAML 命名空间
ms.date: 03/30/2017
ms.assetid: e4f15f13-c420-4c1e-aeab-9b6f50212047
ms.openlocfilehash: 11bf5d112c64fb0b942decf7635f02b90a98bdeb
ms.sourcegitcommit: a4f9b754059f0210e29ae0578363a27b9ba84b64
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 12/05/2019
ms.locfileid: "74837163"
---
# <a name="xaml-namespaces-for-net-framework-xaml-services"></a><span data-ttu-id="b9d7e-102">.NET Framework XAML 服务的 XAML 命名空间</span><span class="sxs-lookup"><span data-stu-id="b9d7e-102">XAML Namespaces for .NET Framework XAML Services</span></span>
<span data-ttu-id="b9d7e-103">XAML 命名空间是扩展 XML 命名空间的定义的概念。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-103">A XAML namespace is a concept that expands on the definition of an XML namespace.</span></span> <span data-ttu-id="b9d7e-104">与 XML 命名空间类似，你可以在标记中使用 `xmlns` 特性定义 XAML 命名空间。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-104">Similar to an XML namespace, you can define a XAML namespace using an `xmlns` attribute in markup.</span></span> <span data-ttu-id="b9d7e-105">Xaml 命名空间还在 XAML 节点流和其他 XAML 服务 Api 中表示。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-105">XAML namespaces are also represented in the XAML node stream and other XAML Services APIs.</span></span> <span data-ttu-id="b9d7e-106">本主题定义了 XAML 命名空间概念，并介绍了 xaml 命名空间的定义，以及 xaml 架构上下文和 .NET Framework XAML 服务的其他方面的使用方法。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-106">This topic defines the XAML namespace concept, and describes how XAML namespaces can be defined and are used by XAML schema contexts and other aspects of .NET Framework XAML Services.</span></span>  
  
## <a name="xml-namespace-and-xaml-namespace"></a><span data-ttu-id="b9d7e-107">XML 命名空间和 XAML 命名空间</span><span class="sxs-lookup"><span data-stu-id="b9d7e-107">XML Namespace and XAML Namespace</span></span>  
 <span data-ttu-id="b9d7e-108">XAML 命名空间是专用 XML 命名空间，正如 XAML 是一种专用形式的 XML，并为其标记使用基本 XML 窗体。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-108">A XAML namespace is a specialized XML namespace, just as XAML is a specialized form of XML and uses the basic XML form for its markup.</span></span> <span data-ttu-id="b9d7e-109">在标记中，通过应用到元素的 `xmlns` 特性来声明 XAML 命名空间及其映射。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-109">In markup, you declare a XAML namespace and its mapping through an `xmlns` attribute applied to an element.</span></span> <span data-ttu-id="b9d7e-110">可以对在其中声明 XAML 命名空间的同一元素进行 `xmlns` 声明。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-110">The `xmlns` declaration can be made to the same element that the XAML namespace is declared in.</span></span> <span data-ttu-id="b9d7e-111">对元素所做的 XAML 命名空间声明对该元素、该元素的所有属性以及该元素的所有子级都有效。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-111">A XAML namespace declaration made to an element is valid for that element, all attributes of that element, and all children of that element.</span></span> <span data-ttu-id="b9d7e-112">特性可以使用与包含特性的元素不同的 XAML 命名空间，只要特性名称本身在标记中引用前缀作为其特性名称的一部分。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-112">Attributes can use a XAML namespaces that is not the same as the element that contains the attribute, so long as the attribute name itself references the prefix as part of its attribute name in markup.</span></span>  
  
 <span data-ttu-id="b9d7e-113">XAML 命名空间与 XML 命名空间的区别在于，XML 命名空间可以用于引用架构，也可以用来仅区分实体。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-113">The distinction of a XAML namespace versus an XML namespace is that an XML namespace might be used to reference a schema, or might be used to simply differentiate entities.</span></span> <span data-ttu-id="b9d7e-114">对于 XAML，XAML 中使用的类型和成员最终必须解析为支持类型，并且 XML 架构概念不适用于此功能。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-114">For XAML, the types and members as used in XAML must ultimately be resolved to backing types, and XML schema concepts do not apply well to this capability.</span></span> <span data-ttu-id="b9d7e-115">XAML 命名空间包含 XAML 架构上下文必须提供的信息，以便执行此类型映射。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-115">The XAML namespace contains information that the XAML schema context must have available in order to perform this type mapping.</span></span>  
  
## <a name="xaml-namespace-components"></a><span data-ttu-id="b9d7e-116">XAML 命名空间组件</span><span class="sxs-lookup"><span data-stu-id="b9d7e-116">XAML Namespace Components</span></span>  
 <span data-ttu-id="b9d7e-117">XAML 命名空间定义包含两个组件：前缀和标识符。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-117">The XAML namespace definition has two components: a prefix, and an identifier.</span></span> <span data-ttu-id="b9d7e-118">如果在标记中声明了 XAML 命名空间，或在 XAML 类型系统中定义了这些组件，则会出现上述每个组件。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-118">Each of these components are present when a XAML namespace is declared in markup, or defined in the XAML type system.</span></span>  
  
 <span data-ttu-id="b9d7e-119">前缀可以是[XML 1.0 规范中 W3C 命名空间](https://www.w3.org/TR/REC-xml-names/)允许的任意字符串。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-119">The prefix can be any string as allowed by the [W3C Namespaces in XML 1.0 specification](https://www.w3.org/TR/REC-xml-names/) .</span></span> <span data-ttu-id="b9d7e-120">按照约定，前缀通常是非常短的字符串，因为前缀在典型的标记文件中重复多次。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-120">By convention, the prefixes are typically very short strings, because the prefix is repeated many times in a typical markup file.</span></span> <span data-ttu-id="b9d7e-121">打算在多个 XAML 实现中使用的某些 XAML 命名空间使用特定的传统前缀。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-121">Certain XAML namespaces that are intended to be used in multiple XAML implementations use particular conventional prefixes.</span></span> <span data-ttu-id="b9d7e-122">例如，通常使用前缀 `x`映射 XAML 语言 XAML 命名空间。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-122">For example, the XAML language XAML namespace is typically mapped using the prefix `x`.</span></span> <span data-ttu-id="b9d7e-123">您可以定义默认的 XAML 命名空间，其中不在定义中提供前缀，而是在定义或查询 by.NET Framework XAML 服务 API 时表示为空字符串。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-123">You can define a default XAML namespace, where the prefix is not given in the definition but is represented as an empty string if defined or queried by.NET Framework XAML Services API.</span></span> <span data-ttu-id="b9d7e-124">通常，特意选择默认的 XAML 命名空间，以便通过 XAML 实现技术及其方案和词汇来提升最大的前缀省略标记量。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-124">Typically, the default XAML namespace is deliberately chosen in order to promote a maximized amount of prefix-omitting markup by a XAML-implementing technology and its scenarios and vocabularies.</span></span>  
  
 <span data-ttu-id="b9d7e-125">标识符可以是[XML 1.0 规范中 W3C 命名空间](https://www.w3.org/TR/REC-xml-names/)允许的任意字符串。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-125">The identifier can be any string as allowed by the [W3C Namespaces in XML 1.0 specification](https://www.w3.org/TR/REC-xml-names/).</span></span> <span data-ttu-id="b9d7e-126">按照约定，XML 命名空间或 XAML 命名空间的标识符通常以 URI 形式提供，通常作为协议限定的绝对 URI。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-126">By convention, identifiers for either XML namespaces or XAML namespaces are often given in URI form, typically as a protocol-qualified absolute URI.</span></span> <span data-ttu-id="b9d7e-127">通常，定义特定 XAML 词汇的版本信息隐含为路径字符串的一部分。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-127">Often, version information that defines a particular XAML vocabulary is implied as part of the path string.</span></span> <span data-ttu-id="b9d7e-128">XAML 命名空间添加了除 XML URI 约定以外的其他标识符约定。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-128">XAML namespaces add an additional identifier convention beyond the XML URI convention.</span></span> <span data-ttu-id="b9d7e-129">对于 XAML 命名空间，标识符会传达 XAML 架构上下文所需的信息，以便解析指定为该 XAML 命名空间下的元素的类型，或解析成员的属性。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-129">For XAML namespaces, the identifier communicates information that is needed by a XAML schema context in order to resolve the types that are specified as elements under that XAML namespace, or to resolve attributes to members.</span></span>  
  
 <span data-ttu-id="b9d7e-130">为了将信息传递到 XAML 架构上下文，XAML 命名空间的标识符可能仍以 URI 形式提供。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-130">For purposes of communicating information to a XAML schema context, the identifier for a XAML namespace might still be in URI form.</span></span> <span data-ttu-id="b9d7e-131">但是，在这种情况下，URI 也被声明为特定程序集或程序集列表中的匹配标识符。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-131">However, in this case the URI is also declared as a matching identifier in a particular assembly or list of assemblies.</span></span> <span data-ttu-id="b9d7e-132">这是通过使用 <xref:System.Windows.Markup.XmlnsDefinitionAttribute>将程序集特性化来实现的。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-132">This is done in assemblies by attributing the assembly with <xref:System.Windows.Markup.XmlnsDefinitionAttribute>.</span></span> <span data-ttu-id="b9d7e-133">.NET Framework XAML 服务中的默认 XAML 架构上下文标识 XAML 命名空间并支持特性化程序集中的基于 CLR 的类型解析行为的这种方法。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-133">This method of identifying the XAML namespace and supporting a CLR-based type resolution behavior in the attributed assembly is supported by the default XAML schema context in .NET Framework XAML Services.</span></span> <span data-ttu-id="b9d7e-134">更常见的情况是，在 XAML 架构上下文包含 CLR 或基于默认 XAML 架构上下文的情况下使用此约定，这对于从 CLR 程序集读取 CLR 属性是必需的。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-134">More generally, this convention can be used for cases where the XAML schema context incorporates the CLR or is based on the default XAML schema context, which is necessary in order to read CLR attributes from CLR assemblies.</span></span>  
  
 <span data-ttu-id="b9d7e-135">XAML 命名空间还可以通过与 CLR 命名空间和类型定义程序集通信的约定进行标识。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-135">XAML namespaces also can be identified by a convention that communicates a CLR namespace and a type-defining assembly.</span></span> <span data-ttu-id="b9d7e-136">如果包含类型的程序集中不存在任何 <xref:System.Windows.Markup.XmlnsDefinitionAttribute> 归属，则使用此约定。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-136">This convention is used in cases where no <xref:System.Windows.Markup.XmlnsDefinitionAttribute> attribution exists in the assemblies that contain types.</span></span> <span data-ttu-id="b9d7e-137">此约定可能比 URI 约定更复杂，而且还可能存在歧义和重复，因为有多种方法可以引用程序集。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-137">This convention is potentially more complex than the URI convention, and also has the potential for ambiguity and duplication, because there are multiple ways of referring to an assembly.</span></span>  
  
 <span data-ttu-id="b9d7e-138">使用 CLR 命名空间和程序集约定的标识符的最基本形式如下：</span><span class="sxs-lookup"><span data-stu-id="b9d7e-138">The most basic form of an identifier that uses the CLR namespace and assembly convention is as follows:</span></span>  
  
 <span data-ttu-id="b9d7e-139">`clr-namespace:` *clrnsName* `; assembly=` *assemblyShortName*</span><span class="sxs-lookup"><span data-stu-id="b9d7e-139">`clr-namespace:` *clrnsName* `; assembly=` *assemblyShortName*</span></span>  
  
 <span data-ttu-id="b9d7e-140">`clr-namespace:` 和 `; assembly=` 是语法的文本组件。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-140">`clr-namespace:` and `; assembly=` are literal components of the syntax.</span></span>  
  
 <span data-ttu-id="b9d7e-141">*clrnsName*是标识 CLR 命名空间的字符串名称。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-141">*clrnsName* is the string name that identifies a CLR namespace.</span></span> <span data-ttu-id="b9d7e-142">此字符串名称包含的任何内部点字符（.）都提供有关 CLR 命名空间及其与其他 CLR 命名空间的关系的提示。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-142">This string name includes any internal dot characters (.) that provide hints about the CLR namespace and its relation to other CLR namespaces.</span></span>  
  
 <span data-ttu-id="b9d7e-143">*assemblyShortName*是定义在 XAML 中有用的类型的程序集的字符串名称。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-143">*assemblyShortName* is the string name of an assembly that defines types that are useful in XAML.</span></span> <span data-ttu-id="b9d7e-144">要通过声明的 XAML 命名空间访问的类型应由程序集定义，并在*clrnsName*指定的 CLR 命名空间中专门声明。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-144">The types to be accessed through the declared XAML namespace are expected to be defined by the assembly and to be specifically declared within the CLR namespace specified by *clrnsName*.</span></span> <span data-ttu-id="b9d7e-145">此字符串名称通常类似于 <xref:System.Reflection.AssemblyName.Name%2A?displayProperty=nameWithType>报告的信息。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-145">This string name typically parallels the information as reported by <xref:System.Reflection.AssemblyName.Name%2A?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="b9d7e-146">CLR 命名空间和程序集约定的更完整定义如下所示：</span><span class="sxs-lookup"><span data-stu-id="b9d7e-146">A more complete definition of the CLR namespace and assembly convention is as follows:</span></span>  
  
 <span data-ttu-id="b9d7e-147">`clr-namespace:` *clrnsName* `; assembly=` *assemblyName*</span><span class="sxs-lookup"><span data-stu-id="b9d7e-147">`clr-namespace:` *clrnsName* `; assembly=` *assemblyName*</span></span>  
  
 <span data-ttu-id="b9d7e-148">*assemblyName*表示作为 <xref:System.Reflection.Assembly.Load%28System.String%29?displayProperty=nameWithType> 输入合法的任何字符串。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-148">*assemblyName* represents any string that is legal as an <xref:System.Reflection.Assembly.Load%28System.String%29?displayProperty=nameWithType> input.</span></span> <span data-ttu-id="b9d7e-149">此字符串可以包括区域性、公钥或版本信息（这些概念的定义在 <xref:System.Reflection.Assembly>的参考主题中定义）。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-149">This string can include culture, public key, or version information (definitions of these concepts are defined in the reference topic for <xref:System.Reflection.Assembly>).</span></span> <span data-ttu-id="b9d7e-150">COFF 格式和证据（由 <xref:System.Reflection.Assembly.Load%2A>的其他重载使用）与 XAML 程序集加载用途无关;所有加载信息必须以字符串形式提供。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-150">COFF format and evidence (as used by other overloads of <xref:System.Reflection.Assembly.Load%2A>) are not relevant for XAML assembly loading purposes; all load information must be presented as a string.</span></span>  
  
 <span data-ttu-id="b9d7e-151">为程序集指定公钥是一种适用于 XAML 安全性的有用技术，或者用于删除在使用简单名称加载程序集或在缓存或应用程序域中预先存在的可能存在的多义性。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-151">Specifying a public key for the assembly is a useful technique for XAML security, or for removing possible ambiguity that can exist if assemblies are loaded by simple name, or pre-exist in a cache or application domain.</span></span> <span data-ttu-id="b9d7e-152">有关详细信息，请参阅[XAML 安全注意事项](xaml-security-considerations.md)。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-152">For more information, see [XAML Security Considerations](xaml-security-considerations.md).</span></span>  
  
## <a name="xaml-namespace-declarations-in-the-xaml-services-api"></a><span data-ttu-id="b9d7e-153">XAML 服务 API 中的 XAML 命名空间声明</span><span class="sxs-lookup"><span data-stu-id="b9d7e-153">XAML Namespace Declarations in the XAML Services API</span></span>  
 <span data-ttu-id="b9d7e-154">在 XAML 服务 API 中，XAML 命名空间声明由 <xref:System.Xaml.NamespaceDeclaration> 对象表示。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-154">In the XAML Services API, a XAML namespace declaration is represented by a <xref:System.Xaml.NamespaceDeclaration> object.</span></span> <span data-ttu-id="b9d7e-155">如果在代码中声明 XAML 命名空间，则调用 <xref:System.Xaml.NamespaceDeclaration.%23ctor%28System.String%2CSystem.String%29> 构造函数。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-155">If you are declaring a XAML namespace in code, you call the <xref:System.Xaml.NamespaceDeclaration.%23ctor%28System.String%2CSystem.String%29> constructor.</span></span> <span data-ttu-id="b9d7e-156">`ns` 和 `prefix` 参数被指定为字符串，要为这些参数提供的输入对应于本主题前面所述的 XAML 命名空间标识符和 XAML 命名空间前缀的定义。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-156">The `ns` and `prefix` parameters are specified as strings, and the input to provide for these parameters corresponds to the definition of XAML namespace identifier and XAML namespace prefix as provided previously in this topic.</span></span>  
  
 <span data-ttu-id="b9d7e-157">如果要将 XAML 命名空间信息作为 XAML 节点流的一部分进行检查或通过其他方式访问 XAML 类型系统，请 <xref:System.Xaml.NamespaceDeclaration.Namespace%2A?displayProperty=nameWithType> 报告 XAML 命名空间标识符，并且 <xref:System.Xaml.NamespaceDeclaration.Prefix%2A?displayProperty=nameWithType> 报告 XAML 命名空间前缀。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-157">If you are examining XAML namespace information as part of a XAML node stream or through other access to the XAML type system, <xref:System.Xaml.NamespaceDeclaration.Namespace%2A?displayProperty=nameWithType> reports the XAML namespace identifier, and <xref:System.Xaml.NamespaceDeclaration.Prefix%2A?displayProperty=nameWithType> reports the XAML namespace prefix.</span></span>  
  
 <span data-ttu-id="b9d7e-158">在 XAML 节点流中，XAML 命名空间信息可以作为 XAML 节点出现在它所应用到的实体之前。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-158">In a XAML node stream, the XAML namespace information can appear as a XAML node that precedes the entity to which it applies.</span></span> <span data-ttu-id="b9d7e-159">这包括 XAML 命名空间信息位于 XAML 根元素 `StartObject` 之前的情况。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-159">This includes cases where the XAML namespace information precedes the `StartObject` of the XAML root element.</span></span> <span data-ttu-id="b9d7e-160">有关更多信息，请参见 [Understanding XAML Node Stream Structures and Concepts](understanding-xaml-node-stream-structures-and-concepts.md)。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-160">For more information, see [Understanding XAML Node Stream Structures and Concepts](understanding-xaml-node-stream-structures-and-concepts.md).</span></span>  
  
 <span data-ttu-id="b9d7e-161">对于许多使用 .NET Framework XAML 服务 API 的方案，至少应有一个 XAML 命名空间声明存在，并且声明必须包含或引用 XAML 架构上下文所需的信息。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-161">For many scenarios that use .NET Framework XAML Services API, at least one XAML namespace declaration is expected to exist, and the declaration must either contain or refer to information that is required by a XAML schema context.</span></span> <span data-ttu-id="b9d7e-162">XAML 命名空间必须指定要加载的程序集，或帮助解析命名空间和 XAML 架构上下文已经加载或已知的程序集中的特定类型。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-162">The XAML namespaces must either specify assemblies to be loaded, or assist in resolving specific types within namespaces and assemblies that are already loaded or known by the XAML schema context.</span></span>  
  
 <span data-ttu-id="b9d7e-163">为了生成 XAML 节点流，XAML 类型信息必须可用于 XAML 架构上下文。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-163">In order to generate a XAML node stream, XAML type information must be available, through the XAML schema context.</span></span> <span data-ttu-id="b9d7e-164">如果没有首先为要创建的每个节点确定相关的 XAML 命名空间，则无法确定 XAML 类型信息。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-164">The XAML type information cannot be determined without first determining the relevant XAML namespace for each node to create.</span></span> <span data-ttu-id="b9d7e-165">此时，尚未创建类型的实例，但 XAML 架构上下文可能需要从定义程序集和后备类型中查找信息。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-165">At this point, no instances of types are created yet, but the XAML schema context may need to look up information from the defining assembly and backing type.</span></span> <span data-ttu-id="b9d7e-166">例如，为了处理标记 `<Party><PartyFavor/></Party>`，XAML 架构上下文必须能够确定 `Party`的 `ContentProperty` 的名称和类型，因此还必须知道 `Party` 和 `PartyFavor`的 XAML 命名空间信息。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-166">For example, in order to process the markup `<Party><PartyFavor/></Party>`, the XAML schema context must be able to determine the name and type of the `ContentProperty` of `Party`, and thus also must know the XAML namespace information for `Party` and `PartyFavor`.</span></span> <span data-ttu-id="b9d7e-167">对于默认的 XAML 架构上下文，静态反射会报告许多 XAML 类型系统信息，需要在节点流中生成 XAML 类型节点。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-167">In the case of the default XAML schema context, static reflection reports much of the XAML type system information that is needed to generate XAML type nodes in the node stream.</span></span>  
  
 <span data-ttu-id="b9d7e-168">为了从 XAML 节点流生成对象图，XAML 命名空间声明必须存在于原始标记中使用的每个 XAML 前缀，并记录在 XAML 节点流中。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-168">In order to generate an object graph from a XAML node stream, XAML namespace declarations must exist for each XAML prefix used in the original markup and recorded in the XAML node stream.</span></span> <span data-ttu-id="b9d7e-169">此时，将创建实例，并发生 true 类型映射行为。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-169">At this point, instances are being created, and true type-mapping behavior occurs.</span></span>  
  
 <span data-ttu-id="b9d7e-170">如果你需要预填充 XAML 命名空间信息，则在标记中未定义你打算使用 XAML 架构上下文的 XAML 命名空间的情况下，你可以使用的一种方法是在 <xref:System.Xml.XmlReader>中声明 <xref:System.Xml.XmlParserContext> 中的 XML 命名空间声明。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-170">If you need to prepopulate XAML namespace information, in cases where the XAML namespace you intend the XAML schema context to use is not defined in the markup, one technique you can use is to declare XML namespace declarations in the <xref:System.Xml.XmlParserContext> for an <xref:System.Xml.XmlReader>.</span></span> <span data-ttu-id="b9d7e-171">然后，将该 <xref:System.Xml.XmlReader> 用作 XAML 读取器构造函数的输入或 <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29?displayProperty=nameWithType>。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-171">Then use that <xref:System.Xml.XmlReader> as input for a XAML reader constructor, or <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="b9d7e-172">与 .NET Framework XAML 服务中的 XAML 命名空间处理相关的两个其他 API 是 <xref:System.Windows.Markup.XmlnsDefinitionAttribute> 和 <xref:System.Windows.Markup.XmlnsPrefixAttribute>属性。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-172">Two other API that are relevant for XAML namespace handling in .NET Framework XAML Services are the attributes <xref:System.Windows.Markup.XmlnsDefinitionAttribute> and <xref:System.Windows.Markup.XmlnsPrefixAttribute>.</span></span> <span data-ttu-id="b9d7e-173">这些属性适用于程序集。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-173">These attributes apply to assemblies.</span></span> <span data-ttu-id="b9d7e-174">XAML 架构上下文使用 <xref:System.Windows.Markup.XmlnsDefinitionAttribute> 来解释包含 URI 的任何 XAML 命名空间声明。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-174"><xref:System.Windows.Markup.XmlnsDefinitionAttribute> is used by a XAML schema context to interpret any XAML namespace declaration that includes a URI.</span></span> <span data-ttu-id="b9d7e-175"><xref:System.Windows.Markup.XmlnsPrefixAttribute> 由发出 XAML 的工具使用，以便可以使用可预测的前缀序列化特定的 XAML 命名空间。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-175"><xref:System.Windows.Markup.XmlnsPrefixAttribute> is used by tools that emit XAML so that a particular XAML namespace can be serialized with a predictable prefix.</span></span> <span data-ttu-id="b9d7e-176">有关详细信息，请参阅[自定义类型和库的 XAML 相关 CLR 特性](xaml-related-clr-attributes-for-custom-types-and-libraries.md)。</span><span class="sxs-lookup"><span data-stu-id="b9d7e-176">For more information, see [XAML-Related CLR Attributes for Custom Types and Libraries](xaml-related-clr-attributes-for-custom-types-and-libraries.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="b9d7e-177">另请参阅</span><span class="sxs-lookup"><span data-stu-id="b9d7e-177">See also</span></span>

- [<span data-ttu-id="b9d7e-178">了解 XAML 节点流结构和概念</span><span class="sxs-lookup"><span data-stu-id="b9d7e-178">Understanding XAML Node Stream Structures and Concepts</span></span>](understanding-xaml-node-stream-structures-and-concepts.md)
