---
title: .NET Framework XAML 服务的 XAML 命名空间
ms.date: 03/30/2017
ms.assetid: e4f15f13-c420-4c1e-aeab-9b6f50212047
ms.openlocfilehash: ac6554cbdeb5bc6e0fe7fb96ea95d0143c293d22
ms.sourcegitcommit: 64f4baed249341e5bf64d1385bf48e3f2e1a0211
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 09/07/2018
ms.locfileid: "44137455"
---
# <a name="xaml-namespaces-for-net-framework-xaml-services"></a><span data-ttu-id="bcad4-102">.NET Framework XAML 服务的 XAML 命名空间</span><span class="sxs-lookup"><span data-stu-id="bcad4-102">XAML Namespaces for .NET Framework XAML Services</span></span>
<span data-ttu-id="bcad4-103">XAML 命名空间是扩展的定义中的 XML 命名空间的概念。</span><span class="sxs-lookup"><span data-stu-id="bcad4-103">A XAML namespace is a concept that expands on the definition of an XML namespace.</span></span> <span data-ttu-id="bcad4-104">类似于 XML 命名空间，您可以定义 XAML 命名空间使用`xmlns`标记中的属性。</span><span class="sxs-lookup"><span data-stu-id="bcad4-104">Similar to an XML namespace, you can define a XAML namespace using an `xmlns` attribute in markup.</span></span> <span data-ttu-id="bcad4-105">在 XAML 节点流和其他 XAML 服务 Api 中还表示 XAML 命名空间。</span><span class="sxs-lookup"><span data-stu-id="bcad4-105">XAML namespaces are also represented in the XAML node stream and other XAML Services APIs.</span></span> <span data-ttu-id="bcad4-106">本主题定义 XAML 命名空间概念，并说明如何 XAML 命名空间可以定义和使用的 XAML 架构上下文和.NET Framework XAML 服务的其他方面。</span><span class="sxs-lookup"><span data-stu-id="bcad4-106">This topic defines the XAML namespace concept, and describes how XAML namespaces can be defined and are used by XAML schema contexts and other aspects of .NET Framework XAML Services.</span></span>  
  
## <a name="xml-namespace-and-xaml-namespace"></a><span data-ttu-id="bcad4-107">XML Namespace 和 XAML Namespace</span><span class="sxs-lookup"><span data-stu-id="bcad4-107">XML Namespace and XAML Namespace</span></span>  
 <span data-ttu-id="bcad4-108">XAML 命名空间是一个专用的 XML 命名空间，正如 XAML 是一种特殊的形式的 XML 和其标记为使用基本的 XML 格式。</span><span class="sxs-lookup"><span data-stu-id="bcad4-108">A XAML namespace is a specialized XML namespace, just as XAML is a specialized form of XML and uses the basic XML form for its markup.</span></span> <span data-ttu-id="bcad4-109">在标记中，声明 XAML 命名空间和通过其映射`xmlns`应用于某个元素的属性。</span><span class="sxs-lookup"><span data-stu-id="bcad4-109">In markup, you declare a XAML namespace and its mapping through an `xmlns` attribute applied to an element.</span></span> <span data-ttu-id="bcad4-110">`xmlns`声明可为 XAML 命名空间声明中的同一元素。</span><span class="sxs-lookup"><span data-stu-id="bcad4-110">The `xmlns` declaration can be made to the same element that the XAML namespace is declared in.</span></span> <span data-ttu-id="bcad4-111">对元素的 XAML 命名空间声明是有效的该元素，该元素的所有属性和该元素的所有子文件夹。</span><span class="sxs-lookup"><span data-stu-id="bcad4-111">A XAML namespace declaration made to an element is valid for that element, all attributes of that element, and all children of that element.</span></span> <span data-ttu-id="bcad4-112">属性可以使用不包含属性的元素相同，只要本身的属性名称作为其标记中的属性名称的一部分引用前缀的 XAML 命名空间。</span><span class="sxs-lookup"><span data-stu-id="bcad4-112">Attributes can use a XAML namespaces that is not the same as the element that contains the attribute, so long as the attribute name itself references the prefix as part of its attribute name in markup.</span></span>  
  
 <span data-ttu-id="bcad4-113">XML 命名空间可用于引用架构，或可能用于只是区分实体，而不是 XML 命名空间的 XAML 命名空间的区别。</span><span class="sxs-lookup"><span data-stu-id="bcad4-113">The distinction of a XAML namespace versus an XML namespace is that an XML namespace might be used to reference a schema, or might be used to simply differentiate entities.</span></span> <span data-ttu-id="bcad4-114">为 XAML 的类型和成员，因为在 XAML 中使用最终必须解析为后备类型，并且很好地适应此功能不适用于 XML 架构概念。</span><span class="sxs-lookup"><span data-stu-id="bcad4-114">For XAML, the types and members as used in XAML must ultimately be resolved to backing types, and XML schema concepts do not apply well to this capability.</span></span> <span data-ttu-id="bcad4-115">XAML 命名空间包含的 XAML 架构上下文必须具有可用于执行此类型映射信息。</span><span class="sxs-lookup"><span data-stu-id="bcad4-115">The XAML namespace contains information that the XAML schema context must have available in order to perform this type mapping.</span></span>  
  
## <a name="xaml-namespace-components"></a><span data-ttu-id="bcad4-116">XAML Namespace 组件</span><span class="sxs-lookup"><span data-stu-id="bcad4-116">XAML Namespace Components</span></span>  
 <span data-ttu-id="bcad4-117">该 XAML 命名空间定义了两个组件： 前缀和标识符。</span><span class="sxs-lookup"><span data-stu-id="bcad4-117">The XAML namespace definition has two components: a prefix, and an identifier.</span></span> <span data-ttu-id="bcad4-118">每个组件都存在时在标记中，声明或 XAML 类型系统中定义的 XAML 命名空间。</span><span class="sxs-lookup"><span data-stu-id="bcad4-118">Each of these components are present when a XAML namespace is declared in markup, or defined in the XAML type system.</span></span>  
  
 <span data-ttu-id="bcad4-119">前缀可以是任意字符串，因为允许通过[W3C XML 1.0 规范中的命名空间](https://go.microsoft.com/fwlink/?LinkID=161735)。</span><span class="sxs-lookup"><span data-stu-id="bcad4-119">The prefix can be any string as allowed by the [W3C Namespaces in XML 1.0 specification](https://go.microsoft.com/fwlink/?LinkID=161735) .</span></span> <span data-ttu-id="bcad4-120">按照约定的前缀是通常很短的字符串，因为该前缀典型的标记文件中重复多次。</span><span class="sxs-lookup"><span data-stu-id="bcad4-120">By convention, the prefixes are typically very short strings, because the prefix is repeated many times in a typical markup file.</span></span> <span data-ttu-id="bcad4-121">用于在多个 XAML 实现中使用某些 XAML 命名空间使用特定的传统前缀。</span><span class="sxs-lookup"><span data-stu-id="bcad4-121">Certain XAML namespaces that are intended to be used in multiple XAML implementations use particular conventional prefixes.</span></span> <span data-ttu-id="bcad4-122">例如，在 XAML 语言 XAML 命名空间值通常映射使用前缀`x`。</span><span class="sxs-lookup"><span data-stu-id="bcad4-122">For example, the XAML language XAML namespace is typically mapped using the prefix `x`.</span></span> <span data-ttu-id="bcad4-123">您可以定义默认 XAML 命名空间，其中前缀定义中未提供，但如果定义，或者查询 by.NET Framework XAML 服务 API 都表示为一个空字符串。</span><span class="sxs-lookup"><span data-stu-id="bcad4-123">You can define a default XAML namespace, where the prefix is not given in the definition but is represented as an empty string if defined or queried by.NET Framework XAML Services API.</span></span> <span data-ttu-id="bcad4-124">通常情况下，为了最大的前缀省略有意选择默认 XAML 命名空间由 XAML 实现的技术及其方案和词汇标记。</span><span class="sxs-lookup"><span data-stu-id="bcad4-124">Typically, the default XAML namespace is deliberately chosen in order to promote a maximized amount of prefix-omitting markup by a XAML-implementing technology and its scenarios and vocabularies.</span></span>  
  
 <span data-ttu-id="bcad4-125">标识符可以是任何字符串，因为允许通过[W3C XML 1.0 规范中的命名空间](https://go.microsoft.com/fwlink/?LinkID=161735)。</span><span class="sxs-lookup"><span data-stu-id="bcad4-125">The identifier can be any string as allowed by the [W3C Namespaces in XML 1.0 specification](https://go.microsoft.com/fwlink/?LinkID=161735).</span></span> <span data-ttu-id="bcad4-126">按照惯例，XML 命名空间或 XAML 命名空间标识符通常给出了 URI 形式，通常作为协议限定的绝对 URI。</span><span class="sxs-lookup"><span data-stu-id="bcad4-126">By convention, identifiers for either XML namespaces or XAML namespaces are often given in URI form, typically as a protocol-qualified absolute URI.</span></span> <span data-ttu-id="bcad4-127">通常情况下，用于定义特定的 XAML 词汇的版本信息则暗指路径字符串的一部分。</span><span class="sxs-lookup"><span data-stu-id="bcad4-127">Often, version information that defines a particular XAML vocabulary is implied as part of the path string.</span></span> <span data-ttu-id="bcad4-128">XAML 命名空间添加一个附加标识符约定超出 XML URI 约定。</span><span class="sxs-lookup"><span data-stu-id="bcad4-128">XAML namespaces add an additional identifier convention beyond the XML URI convention.</span></span> <span data-ttu-id="bcad4-129">对于 XAML 命名空间标识符通信所需的 XAML 架构上下文来解析被指定为该 XAML 命名空间下的元素的类型，或将解析为成员的属性的信息。</span><span class="sxs-lookup"><span data-stu-id="bcad4-129">For XAML namespaces, the identifier communicates information that is needed by a XAML schema context in order to resolve the types that are specified as elements under that XAML namespace, or to resolve attributes to members.</span></span>  
  
 <span data-ttu-id="bcad4-130">对于信息传达给 XAML 架构上下文，XAML 命名空间的标识符仍可能采用 URI 形式。</span><span class="sxs-lookup"><span data-stu-id="bcad4-130">For purposes of communicating information to a XAML schema context, the identifier for a XAML namespace might still be in URI form.</span></span> <span data-ttu-id="bcad4-131">但是，在这种情况下 URI 也声明为一个特定的程序集或程序集的列表中的匹配标识符。</span><span class="sxs-lookup"><span data-stu-id="bcad4-131">However, in this case the URI is also declared as a matching identifier in a particular assembly or list of assemblies.</span></span> <span data-ttu-id="bcad4-132">这是在程序集通过使用程序集<xref:System.Windows.Markup.XmlnsDefinitionAttribute>。</span><span class="sxs-lookup"><span data-stu-id="bcad4-132">This is done in assemblies by attributing the assembly with <xref:System.Windows.Markup.XmlnsDefinitionAttribute>.</span></span> <span data-ttu-id="bcad4-133">此方法确定的 XAML 命名空间并支持基于 CLR 的类型解析行为中的特性化程序集的受.NET Framework XAML 服务中的默认 XAML 架构上下文。</span><span class="sxs-lookup"><span data-stu-id="bcad4-133">This method of identifying the XAML namespace and supporting a CLR-based type resolution behavior in the attributed assembly is supported by the default XAML schema context in .NET Framework XAML Services.</span></span> <span data-ttu-id="bcad4-134">一般来说，此约定可用于 XAML 架构上下文包含 CLR 或基于为 CLR 程序集从读取 CLR 属性是必需的默认 XAML 架构上下文的情况。</span><span class="sxs-lookup"><span data-stu-id="bcad4-134">More generally, this convention can be used for cases where the XAML schema context incorporates the CLR or is based on the default XAML schema context, which is necessary in order to read CLR attributes from CLR assemblies.</span></span>  
  
 <span data-ttu-id="bcad4-135">XAML 命名空间，也可以识别的 CLR 命名空间和类型定义的程序集进行通信的约定。</span><span class="sxs-lookup"><span data-stu-id="bcad4-135">XAML namespaces also can be identified by a convention that communicates a CLR namespace and a type-defining assembly.</span></span> <span data-ttu-id="bcad4-136">此约定用于在情况下，在不<xref:System.Windows.Markup.XmlnsDefinitionAttribute>归属存在于包含类型的程序集。</span><span class="sxs-lookup"><span data-stu-id="bcad4-136">This convention is used in cases where no <xref:System.Windows.Markup.XmlnsDefinitionAttribute> attribution exists in the assemblies that contain types.</span></span> <span data-ttu-id="bcad4-137">此约定可能比 URI 约定，更复杂而还有可能存在二义性和重复数据删除，因为有多个方法的引用程序集。</span><span class="sxs-lookup"><span data-stu-id="bcad4-137">This convention is potentially more complex than the URI convention, and also has the potential for ambiguity and duplication, because there are multiple ways of referring to an assembly.</span></span>  
  
 <span data-ttu-id="bcad4-138">使用 CLR 命名空间和程序集约定的标识符的最基本形式如下所示：</span><span class="sxs-lookup"><span data-stu-id="bcad4-138">The most basic form of an identifier that uses the CLR namespace and assembly convention is as follows:</span></span>  
  
 <span data-ttu-id="bcad4-139">`clr-namespace:` *clrnsName* `; assembly=` *程序集短名称*</span><span class="sxs-lookup"><span data-stu-id="bcad4-139">`clr-namespace:` *clrnsName* `; assembly=` *assemblyShortName*</span></span>  
  
 <span data-ttu-id="bcad4-140">`clr-namespace:` 和`; assembly=`是文本的语法组件。</span><span class="sxs-lookup"><span data-stu-id="bcad4-140">`clr-namespace:` and `; assembly=` are literal components of the syntax.</span></span>  
  
 <span data-ttu-id="bcad4-141">*clrnsName*是标识 CLR 命名空间的字符串名称。</span><span class="sxs-lookup"><span data-stu-id="bcad4-141">*clrnsName* is the string name that identifies a CLR namespace.</span></span> <span data-ttu-id="bcad4-142">此字符串名称包括提供有关 CLR 命名空间和其他 CLR 命名空间之间的关系的提示的任何内部圆点字符 （.）。</span><span class="sxs-lookup"><span data-stu-id="bcad4-142">This string name includes any internal dot characters (.) that provide hints about the CLR namespace and its relation to other CLR namespaces.</span></span>  
  
 <span data-ttu-id="bcad4-143">*程序集短名称*是定义可在 XAML 中的类型的程序集的字符串名称。</span><span class="sxs-lookup"><span data-stu-id="bcad4-143">*assemblyShortName* is the string name of an assembly that defines types that are useful in XAML.</span></span> <span data-ttu-id="bcad4-144">要通过声明 XAML 命名空间进行访问的类型定义的程序集，特别是在指定的 CLR 命名空间中声明预期*clrnsName*。</span><span class="sxs-lookup"><span data-stu-id="bcad4-144">The types to be accessed through the declared XAML namespace are expected to be defined by the assembly and to be specifically declared within the CLR namespace specified by *clrnsName*.</span></span> <span data-ttu-id="bcad4-145">此字符串名称通常与信息报告的<xref:System.Reflection.AssemblyName.Name%2A?displayProperty=nameWithType>。</span><span class="sxs-lookup"><span data-stu-id="bcad4-145">This string name typically parallels the information as reported by <xref:System.Reflection.AssemblyName.Name%2A?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="bcad4-146">更完整的 CLR 命名空间和程序集约定的定义如下所示：</span><span class="sxs-lookup"><span data-stu-id="bcad4-146">A more complete definition of the CLR namespace and assembly convention is as follows:</span></span>  
  
 <span data-ttu-id="bcad4-147">`clr-namespace:` *clrnsName* `; assembly=` *程序集名称*</span><span class="sxs-lookup"><span data-stu-id="bcad4-147">`clr-namespace:` *clrnsName* `; assembly=` *assemblyName*</span></span>  
  
 <span data-ttu-id="bcad4-148">*程序集名称*表示作为是合法的任何字符串<xref:System.Reflection.Assembly.Load%28System.String%29?displayProperty=nameWithType>输入。</span><span class="sxs-lookup"><span data-stu-id="bcad4-148">*assemblyName* represents any string that is legal as an <xref:System.Reflection.Assembly.Load%28System.String%29?displayProperty=nameWithType> input.</span></span> <span data-ttu-id="bcad4-149">此字符串可以包括区域性、 公钥或版本信息 (这些概念的定义中的参考主题定义<xref:System.Reflection.Assembly>)。</span><span class="sxs-lookup"><span data-stu-id="bcad4-149">This string can include culture, public key, or version information (definitions of these concepts are defined in the reference topic for <xref:System.Reflection.Assembly>).</span></span> <span data-ttu-id="bcad4-150">COFF 格式和证据 (由另一个重载<xref:System.Reflection.Assembly.Load%2A>) 不相关的 XAML 程序集加载目的; 所有负载信息必须都显示为字符串。</span><span class="sxs-lookup"><span data-stu-id="bcad4-150">COFF format and evidence (as used by other overloads of <xref:System.Reflection.Assembly.Load%2A>) are not relevant for XAML assembly loading purposes; all load information must be presented as a string.</span></span>  
  
 <span data-ttu-id="bcad4-151">指定程序集的公共密钥是为了 XAML 安全性，也可以消除可能的多义性，如果程序集加载的简单名称或预先缓存或应用程序域中存在可存在的有用技术。</span><span class="sxs-lookup"><span data-stu-id="bcad4-151">Specifying a public key for the assembly is a useful technique for XAML security, or for removing possible ambiguity that can exist if assemblies are loaded by simple name, or pre-exist in a cache or application domain.</span></span> <span data-ttu-id="bcad4-152">有关详细信息，请参阅[XAML 安全注意事项](../../../docs/framework/xaml-services/xaml-security-considerations.md)。</span><span class="sxs-lookup"><span data-stu-id="bcad4-152">For more information, see [XAML Security Considerations](../../../docs/framework/xaml-services/xaml-security-considerations.md).</span></span>  
  
## <a name="xaml-namespace-declarations-in-the-xaml-services-api"></a><span data-ttu-id="bcad4-153">XAML 服务 API 中的 XAML Namespace 声明</span><span class="sxs-lookup"><span data-stu-id="bcad4-153">XAML Namespace Declarations in the XAML Services API</span></span>  
 <span data-ttu-id="bcad4-154">在 XAML 服务 API 中，XAML 命名空间声明为由<xref:System.Xaml.NamespaceDeclaration>对象。</span><span class="sxs-lookup"><span data-stu-id="bcad4-154">In the XAML Services API, a XAML namespace declaration is represented by a <xref:System.Xaml.NamespaceDeclaration> object.</span></span> <span data-ttu-id="bcad4-155">如果你将声明代码中的 XAML 命名空间，则调用<xref:System.Xaml.NamespaceDeclaration.%23ctor%28System.String%2CSystem.String%29>构造函数。</span><span class="sxs-lookup"><span data-stu-id="bcad4-155">If you are declaring a XAML namespace in code, you call the <xref:System.Xaml.NamespaceDeclaration.%23ctor%28System.String%2CSystem.String%29> constructor.</span></span> <span data-ttu-id="bcad4-156">`ns`和`prefix`参数被指定为字符串，并如本主题中提供以前，要为这些参数提供的输入对应于 XAML 命名空间标识符和 XAML 命名空间前缀的定义。</span><span class="sxs-lookup"><span data-stu-id="bcad4-156">The `ns` and `prefix` parameters are specified as strings, and the input to provide for these parameters corresponds to the definition of XAML namespace identifier and XAML namespace prefix as provided previously in this topic.</span></span>  
  
 <span data-ttu-id="bcad4-157">如果你正在检查 XAML 节点流的一部分或通过其他访问 XAML 类型系统，为 XAML 命名空间信息<xref:System.Xaml.NamespaceDeclaration.Namespace%2A?displayProperty=nameWithType>报告的 XAML 命名空间标识符，和<xref:System.Xaml.NamespaceDeclaration.Prefix%2A?displayProperty=nameWithType>报告的 XAML 命名空间前缀。</span><span class="sxs-lookup"><span data-stu-id="bcad4-157">If you are examining XAML namespace information as part of a XAML node stream or through other access to the XAML type system, <xref:System.Xaml.NamespaceDeclaration.Namespace%2A?displayProperty=nameWithType> reports the XAML namespace identifier, and <xref:System.Xaml.NamespaceDeclaration.Prefix%2A?displayProperty=nameWithType> reports the XAML namespace prefix.</span></span>  
  
 <span data-ttu-id="bcad4-158">在 XAML 节点流中，XAML 命名空间信息可以显示为 XAML 节点之前它所应用于的实体。</span><span class="sxs-lookup"><span data-stu-id="bcad4-158">In a XAML node stream, the XAML namespace information can appear as a XAML node that precedes the entity to which it applies.</span></span> <span data-ttu-id="bcad4-159">这包括其中的 XAML 命名空间信息之前的情况下`StartObject`的 XAML 根元素。</span><span class="sxs-lookup"><span data-stu-id="bcad4-159">This includes cases where the XAML namespace information precedes the `StartObject` of the XAML root element.</span></span> <span data-ttu-id="bcad4-160">有关更多信息，请参见 [Understanding XAML Node Stream Structures and Concepts](../../../docs/framework/xaml-services/understanding-xaml-node-stream-structures-and-concepts.md)。</span><span class="sxs-lookup"><span data-stu-id="bcad4-160">For more information, see [Understanding XAML Node Stream Structures and Concepts](../../../docs/framework/xaml-services/understanding-xaml-node-stream-structures-and-concepts.md).</span></span>  
  
 <span data-ttu-id="bcad4-161">对于使用.NET Framework XAML 服务 API 的许多情况下，应至少一个 XAML 命名空间声明存在，并声明必须包含或指的所需的 XAML 架构上下文信息。</span><span class="sxs-lookup"><span data-stu-id="bcad4-161">For many scenarios that use .NET Framework XAML Services API, at least one XAML namespace declaration is expected to exist, and the declaration must either contain or refer to information that is required by a XAML schema context.</span></span> <span data-ttu-id="bcad4-162">XAML 命名空间必须指定要加载的或帮助解决命名空间和已加载或已知的 XAML 架构上下文的程序集内的特定类型的程序集。</span><span class="sxs-lookup"><span data-stu-id="bcad4-162">The XAML namespaces must either specify assemblies to be loaded, or assist in resolving specific types within namespaces and assemblies that are already loaded or known by the XAML schema context.</span></span>  
  
 <span data-ttu-id="bcad4-163">为了生成 XAML 节点流，必须通过 XAML 架构上下文提供 XAML 类型信息。</span><span class="sxs-lookup"><span data-stu-id="bcad4-163">In order to generate a XAML node stream, XAML type information must be available, through the XAML schema context.</span></span> <span data-ttu-id="bcad4-164">而无需首先确定要创建每个节点相关的 XAML 命名空间，无法确定的 XAML 类型信息。</span><span class="sxs-lookup"><span data-stu-id="bcad4-164">The XAML type information cannot be determined without first determining the relevant XAML namespace for each node to create.</span></span> <span data-ttu-id="bcad4-165">此时，尚未，创建的任何类型实例，但 XAML 架构上下文可能需要查找来自定义程序集和备份类型的信息。</span><span class="sxs-lookup"><span data-stu-id="bcad4-165">At this point, no instances of types are created yet, but the XAML schema context may need to look up information from the defining assembly and backing type.</span></span> <span data-ttu-id="bcad4-166">例如，若要处理标记`<Party><PartyFavor/></Party>`，必须能够确定的名称和类型的 XAML 架构上下文`ContentProperty`的`Party`，并因此还必须知道的 XAML 命名空间信息`Party`和`PartyFavor`。</span><span class="sxs-lookup"><span data-stu-id="bcad4-166">For example, in order to process the markup `<Party><PartyFavor/></Party>`, the XAML schema context must be able to determine the name and type of the `ContentProperty` of `Party`, and thus also must know the XAML namespace information for `Party` and `PartyFavor`.</span></span> <span data-ttu-id="bcad4-167">对于默认 XAML 架构上下文中，静态反射会报告很多在节点流中生成 XAML 类型的节点所需的 XAML 类型系统信息。</span><span class="sxs-lookup"><span data-stu-id="bcad4-167">In the case of the default XAML schema context, static reflection reports much of the XAML type system information that is needed to generate XAML type nodes in the node stream.</span></span>  
  
 <span data-ttu-id="bcad4-168">若要从 XAML 节点流生成对象图，请在原始标记中使用并记录在 XAML 节点流中的每个 XAML 前缀必须存在 XAML 命名空间声明。</span><span class="sxs-lookup"><span data-stu-id="bcad4-168">In order to generate an object graph from a XAML node stream, XAML namespace declarations must exist for each XAML prefix used in the original markup and recorded in the XAML node stream.</span></span> <span data-ttu-id="bcad4-169">此时，正在创建实例，并且会出现，则返回 true 的类型映射行为。</span><span class="sxs-lookup"><span data-stu-id="bcad4-169">At this point, instances are being created, and true type-mapping behavior occurs.</span></span>  
  
 <span data-ttu-id="bcad4-170">如果您需要用于预先填充 XAML 命名空间中的信息，其中在标记中，未定义你想要使用的 XAML 架构上下文的 XAML 命名空间的情况下可以使用一种方法是声明中的 XML 命名空间声明<xref:System.Xml.XmlParserContext>为<xref:System.Xml.XmlReader>.</span><span class="sxs-lookup"><span data-stu-id="bcad4-170">If you need to prepopulate XAML namespace information, in cases where the XAML namespace you intend the XAML schema context to use is not defined in the markup, one technique you can use is to declare XML namespace declarations in the <xref:System.Xml.XmlParserContext> for an <xref:System.Xml.XmlReader>.</span></span> <span data-ttu-id="bcad4-171">然后，使用该<xref:System.Xml.XmlReader>作为 XAML 读取器，构造函数的输入或<xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29?displayProperty=nameWithType>。</span><span class="sxs-lookup"><span data-stu-id="bcad4-171">Then use that <xref:System.Xml.XmlReader> as input for a XAML reader constructor, or <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="bcad4-172">相关的.NET Framework XAML 服务中处理的 XAML 命名空间的两个其他 API 是属性<xref:System.Windows.Markup.XmlnsDefinitionAttribute>和<xref:System.Windows.Markup.XmlnsPrefixAttribute>。</span><span class="sxs-lookup"><span data-stu-id="bcad4-172">Two other API that are relevant for XAML namespace handling in .NET Framework XAML Services are the attributes <xref:System.Windows.Markup.XmlnsDefinitionAttribute> and <xref:System.Windows.Markup.XmlnsPrefixAttribute>.</span></span> <span data-ttu-id="bcad4-173">这些属性适用于程序集。</span><span class="sxs-lookup"><span data-stu-id="bcad4-173">These attributes apply to assemblies.</span></span> <span data-ttu-id="bcad4-174"><xref:System.Windows.Markup.XmlnsDefinitionAttribute> 可供 XAML 架构上下文来解释包含 URI 的任何 XAML 命名空间声明。</span><span class="sxs-lookup"><span data-stu-id="bcad4-174"><xref:System.Windows.Markup.XmlnsDefinitionAttribute> is used by a XAML schema context to interpret any XAML namespace declaration that includes a URI.</span></span> <span data-ttu-id="bcad4-175"><xref:System.Windows.Markup.XmlnsPrefixAttribute> 使用发出 XAML，以便特定 XAML 命名空间可以使用可预测的前缀进行序列化的工具。</span><span class="sxs-lookup"><span data-stu-id="bcad4-175"><xref:System.Windows.Markup.XmlnsPrefixAttribute> is used by tools that emit XAML so that a particular XAML namespace can be serialized with a predictable prefix.</span></span> <span data-ttu-id="bcad4-176">有关详细信息，请参阅[XAML-Related CLR 特性自定义类型和库的](../../../docs/framework/xaml-services/xaml-related-clr-attributes-for-custom-types-and-libraries.md)。</span><span class="sxs-lookup"><span data-stu-id="bcad4-176">For more information, see [XAML-Related CLR Attributes for Custom Types and Libraries](../../../docs/framework/xaml-services/xaml-related-clr-attributes-for-custom-types-and-libraries.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="bcad4-177">请参阅</span><span class="sxs-lookup"><span data-stu-id="bcad4-177">See Also</span></span>  
 [<span data-ttu-id="bcad4-178">了解 XAML 节点流结构和概念</span><span class="sxs-lookup"><span data-stu-id="bcad4-178">Understanding XAML Node Stream Structures and Concepts</span></span>](../../../docs/framework/xaml-services/understanding-xaml-node-stream-structures-and-concepts.md)
