---
title: 数据并行和任务并行中的潜在缺陷
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- parallel programming, pitfalls
ms.assetid: 1e357177-e699-4b8f-9e49-56d3513ed128
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: 5613128950d53946d55050ba3fd77cf1f0bb048a
ms.sourcegitcommit: 6b308cf6d627d78ee36dbbae8972a310ac7fd6c8
ms.translationtype: HT
ms.contentlocale: zh-CN
ms.lasthandoff: 01/23/2019
ms.locfileid: "54513420"
---
# <a name="potential-pitfalls-in-data-and-task-parallelism"></a><span data-ttu-id="5d1a7-102">数据并行和任务并行中的潜在缺陷</span><span class="sxs-lookup"><span data-stu-id="5d1a7-102">Potential Pitfalls in Data and Task Parallelism</span></span>
<span data-ttu-id="5d1a7-103">在许多情况下，与普通的顺序循环相比，<xref:System.Threading.Tasks.Parallel.For%2A?displayProperty=nameWithType> 和 <xref:System.Threading.Tasks.Parallel.ForEach%2A?displayProperty=nameWithType> 可以显著提升性能。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-103">In many cases, <xref:System.Threading.Tasks.Parallel.For%2A?displayProperty=nameWithType> and <xref:System.Threading.Tasks.Parallel.ForEach%2A?displayProperty=nameWithType> can provide significant performance improvements over ordinary sequential loops.</span></span> <span data-ttu-id="5d1a7-104">但是，对循环进行并行化的工作增加了复杂性，可能会导致在顺序代码中出现不常见或根本不会遇到的问题。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-104">However, the work of parallelizing the loop introduces complexity that can lead to problems that, in sequential code, are not as common or are not encountered at all.</span></span> <span data-ttu-id="5d1a7-105">本主题列出了一些在编写并行循环时要避免的做法。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-105">This topic lists some practices to avoid when you write parallel loops.</span></span>  
  
## <a name="do-not-assume-that-parallel-is-always-faster"></a><span data-ttu-id="5d1a7-106">不要假定并行的速度始终更快</span><span class="sxs-lookup"><span data-stu-id="5d1a7-106">Do Not Assume That Parallel Is Always Faster</span></span>  
 <span data-ttu-id="5d1a7-107">在某些情况下，并行循环可能比它等效的顺序循环的运行速度更慢。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-107">In certain cases a parallel loop might run slower than its sequential equivalent.</span></span> <span data-ttu-id="5d1a7-108">基本的经验法则是具有较少迭代和快速用户委托的并行循环未必会快很多。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-108">The basic rule of thumb is that parallel loops that have few iterations and fast user delegates are unlikely to speedup much.</span></span> <span data-ttu-id="5d1a7-109">但是，由于性能会涉及到很多因素，因此我们建议始终衡量实际的结果。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-109">However, because many factors are involved in performance, we recommend that you always measure actual results.</span></span>  
  
## <a name="avoid-writing-to-shared-memory-locations"></a><span data-ttu-id="5d1a7-110">避免写入共享内存位置</span><span class="sxs-lookup"><span data-stu-id="5d1a7-110">Avoid Writing to Shared Memory Locations</span></span>  
 <span data-ttu-id="5d1a7-111">在顺序代码中，从静态变量或类字段中读取或写入静态变量或类字段的情况很常见。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-111">In sequential code, it is not uncommon to read from or write to static variables or class fields.</span></span> <span data-ttu-id="5d1a7-112">但是，每当多个线程同时访问此类变量时，则很有可能会出现争用条件。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-112">However, whenever multiple threads are accessing such variables concurrently, there is a big potential for race conditions.</span></span> <span data-ttu-id="5d1a7-113">即使可以使用锁来同步对变量的访问，但同步开销可能会对性能造成损害。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-113">Even though you can use locks to synchronize access to the variable, the cost of synchronization can hurt performance.</span></span> <span data-ttu-id="5d1a7-114">因此，我们建议尽可能地避免在一个并行循环中访问共享状态，或至少限制对共享状态的访问。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-114">Therefore, we recommend that you avoid, or at least limit, access to shared state in a parallel loop as much as possible.</span></span> <span data-ttu-id="5d1a7-115">为此，最好使用 <xref:System.Threading.Tasks.Parallel.For%2A?displayProperty=nameWithType> 和 <xref:System.Threading.Tasks.Parallel.ForEach%2A?displayProperty=nameWithType> 的重载，以便在循环执行期间使用 <xref:System.Threading.ThreadLocal%601?displayProperty=nameWithType> 变量存储线程本地状态。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-115">The best way to do this is to use the overloads of <xref:System.Threading.Tasks.Parallel.For%2A?displayProperty=nameWithType> and <xref:System.Threading.Tasks.Parallel.ForEach%2A?displayProperty=nameWithType> that use a <xref:System.Threading.ThreadLocal%601?displayProperty=nameWithType> variable to store thread-local state during loop execution.</span></span> <span data-ttu-id="5d1a7-116">有关详细信息，请参阅[如何：编写具有线程局部变量的 Parallel.For 循环](../../../docs/standard/parallel-programming/how-to-write-a-parallel-for-loop-with-thread-local-variables.md)和[如何：使用分区本地变量编写 Parallel.ForEach 循环](../../../docs/standard/parallel-programming/how-to-write-a-parallel-foreach-loop-with-partition-local-variables.md)。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-116">For more information, see [How to: Write a Parallel.For Loop with Thread-Local Variables](../../../docs/standard/parallel-programming/how-to-write-a-parallel-for-loop-with-thread-local-variables.md) and [How to: Write a Parallel.ForEach Loop with Partition-Local Variables](../../../docs/standard/parallel-programming/how-to-write-a-parallel-foreach-loop-with-partition-local-variables.md).</span></span>  
  
## <a name="avoid-over-parallelization"></a><span data-ttu-id="5d1a7-117">避免过度并行化</span><span class="sxs-lookup"><span data-stu-id="5d1a7-117">Avoid Over-Parallelization</span></span>  
 <span data-ttu-id="5d1a7-118">通过使用并行循环，将会产生对源集合进行分区和同步工作线程的开销成本。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-118">By using parallel loops, you incur the overhead costs of partitioning the source collection and synchronizing the worker threads.</span></span> <span data-ttu-id="5d1a7-119">计算机上的处理器数量进一步限制了并行化的优点。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-119">The benefits of parallelization are further limited by the number of processors on the computer.</span></span> <span data-ttu-id="5d1a7-120">仅在一个处理器上运行多个受计算限制的线程时，速度并不会得到提升。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-120">There is no speedup to be gained by running multiple compute-bound threads on just one processor.</span></span> <span data-ttu-id="5d1a7-121">因此，必须要小心，不要对循环进行过度并行化。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-121">Therefore, you must be careful not to over-parallelize a loop.</span></span>  
  
 <span data-ttu-id="5d1a7-122">在嵌套的循环中，最有可能发生过度并行化的情况。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-122">The most common scenario in which over-parallelization can occur is in nested loops.</span></span> <span data-ttu-id="5d1a7-123">在大多数情况下，除非满足以下一个或多个条件，否则最好仅对外部循环进行并行化：</span><span class="sxs-lookup"><span data-stu-id="5d1a7-123">In most cases, it is best to parallelize only the outer loop unless one or more of the following conditions apply:</span></span>  
  
-   <span data-ttu-id="5d1a7-124">已知内部循环非常长。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-124">The inner loop is known to be very long.</span></span>  
  
-   <span data-ttu-id="5d1a7-125">正在对每个订单执行开销极大的计算。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-125">You are performing an expensive computation on each order.</span></span> <span data-ttu-id="5d1a7-126">（示例中所示的操作开销不大。）</span><span class="sxs-lookup"><span data-stu-id="5d1a7-126">(The operation shown in the example is not expensive.)</span></span>  
  
-   <span data-ttu-id="5d1a7-127">已知目标系统具有足够的处理器来处理通过对 `cust.Orders` 上的查询进行并行化所产生的线程数。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-127">The target system is known to have enough processors to handle the number of threads that will be produced by parallelizing the query on `cust.Orders`.</span></span>  
  
 <span data-ttu-id="5d1a7-128">在所有情况下，确定最佳查询形式的最好方法是进行测试和测量。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-128">In all cases, the best way to determine the optimum query shape is to test and measure.</span></span>  
  
## <a name="avoid-calls-to-non-thread-safe-methods"></a><span data-ttu-id="5d1a7-129">避免调用非线程安全方法</span><span class="sxs-lookup"><span data-stu-id="5d1a7-129">Avoid Calls to Non-Thread-Safe Methods</span></span>  
 <span data-ttu-id="5d1a7-130">如果从并行循环中写入非线程安全实例方法，可能会导致出现程序可能检测到也可能检测不到的数据损坏。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-130">Writing to non-thread-safe instance methods from a parallel loop can lead to data corruption which may or may not go undetected in your program.</span></span> <span data-ttu-id="5d1a7-131">还可能会导致异常。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-131">It can also lead to exceptions.</span></span> <span data-ttu-id="5d1a7-132">在下面的示例中，多个线程尝试同时调用 <xref:System.IO.FileStream.WriteByte%2A?displayProperty=nameWithType> 方法，类并不支持这样做。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-132">In the following example, multiple threads would be attempting to call the <xref:System.IO.FileStream.WriteByte%2A?displayProperty=nameWithType> method simultaneously, which is not supported by the class.</span></span>  
  
 [!code-csharp[TPL_Pitfalls#04](../../../samples/snippets/csharp/VS_Snippets_Misc/tpl_pitfalls/cs/pitfalls.cs#04)]
 [!code-vb[TPL_Pitfalls#04](../../../samples/snippets/visualbasic/VS_Snippets_Misc/tpl_pitfalls/vb/pitfalls_vb.vb#04)]  
  
## <a name="limit-calls-to-thread-safe-methods"></a><span data-ttu-id="5d1a7-133">限制调用线程安全方法</span><span class="sxs-lookup"><span data-stu-id="5d1a7-133">Limit Calls to Thread-Safe Methods</span></span>  
 <span data-ttu-id="5d1a7-134">.NET Framework 中的大多数静态方法是线程安全的，并且可以同时从多个线程中调用。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-134">Most static methods in the .NET Framework are thread-safe and can be called from multiple threads concurrently.</span></span> <span data-ttu-id="5d1a7-135">但是，即使在这些情况下，所涉及到的同步也可能会导致查询速度大幅度下降。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-135">However, even in these cases, the synchronization involved can lead to significant slowdown in the query.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="5d1a7-136">可以自行对此进行测试，具体方法是在查询中插入一些 <xref:System.Console.WriteLine%2A> 调用。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-136">You can test for this yourself by inserting some calls to <xref:System.Console.WriteLine%2A> in your queries.</span></span> <span data-ttu-id="5d1a7-137">尽管出于演示目的，在文档示例中使用了此方法，但除非必要，否则不要在并行循环中使用它。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-137">Although this method is used in the documentation examples for demonstration purposes, do not use it in parallel loops unless necessary.</span></span>  
  
## <a name="be-aware-of-thread-affinity-issues"></a><span data-ttu-id="5d1a7-138">注意线程关联问题</span><span class="sxs-lookup"><span data-stu-id="5d1a7-138">Be Aware of Thread Affinity Issues</span></span>  
 <span data-ttu-id="5d1a7-139">某些技术（例如，单线程单元 (STA) 组件的 COM 互操作性、Windows 窗体以及 Windows Presentation Foundation (WPF)）具有要求代码在特定线程上运行的线程关联限制。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-139">Some technologies, for example, COM interoperability for Single-Threaded Apartment (STA) components, Windows Forms, and Windows Presentation Foundation (WPF), impose thread affinity restrictions that require code to run on a specific thread.</span></span> <span data-ttu-id="5d1a7-140">例如，在 Windows 窗体和 WPF 中，只能在创建控件的线程上访问该控件。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-140">For example, in both Windows Forms and WPF, a control can only be accessed on the thread on which it was created.</span></span> <span data-ttu-id="5d1a7-141">举例来说，这意味着，除非将线程调度器配置为仅将工作安排在 UI 线程上，否则你将无法从并行循环中更新列表控件。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-141">This means, for example, that you cannot update a list control from a parallel loop unless you configure the thread scheduler to schedule work only on the UI thread.</span></span> <span data-ttu-id="5d1a7-142">有关详细信息，请参阅[如何：在用户界面 (UI) 线程上安排工作](https://msdn.microsoft.com/library/32a846a5-d628-4933-907b-4888ff72c663)。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-142">For more information, see [How to: Schedule Work on the User Interface (UI) Thread](https://msdn.microsoft.com/library/32a846a5-d628-4933-907b-4888ff72c663).</span></span>  
  
## <a name="use-caution-when-waiting-in-delegates-that-are-called-by-parallelinvoke"></a><span data-ttu-id="5d1a7-143">在由 Parallel.Invoke 调用的委托中等待时请谨慎使用</span><span class="sxs-lookup"><span data-stu-id="5d1a7-143">Use Caution When Waiting in Delegates That Are Called by Parallel.Invoke</span></span>  
 <span data-ttu-id="5d1a7-144">在某些情况下，任务并行库将对任务进行内联操作，这意味着它将在当前正在执行的线程上的任务上运行。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-144">In certain circumstances, the Task Parallel Library will inline a task, which means it runs on the task on the currently executing thread.</span></span> <span data-ttu-id="5d1a7-145">（有关详细信息，请参阅[任务计划程序](https://msdn.microsoft.com/library/638f8ea5-21db-47a2-a934-86e1e961bf65)。）此性能优化在某些情况下可能会导致死锁。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-145">(For more information, see [Task Schedulers](https://msdn.microsoft.com/library/638f8ea5-21db-47a2-a934-86e1e961bf65).) This performance optimization can lead to deadlock in certain cases.</span></span> <span data-ttu-id="5d1a7-146">例如，两个任务可能运行相同的委托代码，该代码在事件发生时将发出信号，然后等待另一个任务发出信号。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-146">For example, two tasks might run the same delegate code, which signals when an event occurs, and then waits for the other task to signal.</span></span> <span data-ttu-id="5d1a7-147">如果在相同线程上将第二个任务内联为第一个，并且第一个任务进入等待状态，则第二个任务将永远无法发出其事件信号。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-147">If the second task is inlined on the same thread as the first, and the first goes into a Wait state, the second task will never be able to signal its event.</span></span> <span data-ttu-id="5d1a7-148">为了避免发生这种情况，可以在等待操作上指定超时，或使用显式线程构造函数来帮助确保一个任务无法阻止另一个任务。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-148">To avoid such an occurrence, you can specify a timeout on the Wait operation, or use explicit thread constructors to help ensure that one task cannot block the other.</span></span>  
  
## <a name="do-not-assume-that-iterations-of-foreach-for-and-forall-always-execute-in-parallel"></a><span data-ttu-id="5d1a7-149">不要假定 ForEach、For 和 ForAll 的迭代始终并行执行</span><span class="sxs-lookup"><span data-stu-id="5d1a7-149">Do Not Assume that Iterations of ForEach, For and ForAll Always Execute in Parallel</span></span>  
 <span data-ttu-id="5d1a7-150">请务必注意，<xref:System.Threading.Tasks.Parallel.For%2A>、<xref:System.Threading.Tasks.Parallel.ForEach%2A> 或 <xref:System.Linq.ParallelEnumerable.ForAll%2A> 循环中的各个迭代可能会（但不需要）并行执行。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-150">It is important to keep in mind that individual iterations in a <xref:System.Threading.Tasks.Parallel.For%2A>, <xref:System.Threading.Tasks.Parallel.ForEach%2A> or <xref:System.Linq.ParallelEnumerable.ForAll%2A> loop may but do not have to execute in parallel.</span></span> <span data-ttu-id="5d1a7-151">因此，应避免编写任何依赖于迭代并行执行的正确性或依赖于按任何特定顺序执行迭代的代码。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-151">Therefore, you should avoid writing any code that depends for correctness on parallel execution of iterations or on the execution of iterations in any particular order.</span></span> <span data-ttu-id="5d1a7-152">例如，此代码有可能会死锁：</span><span class="sxs-lookup"><span data-stu-id="5d1a7-152">For example, this code is likely to deadlock:</span></span>  
  
 [!code-csharp[TPL_Pitfalls#01](../../../samples/snippets/csharp/VS_Snippets_Misc/tpl_pitfalls/cs/pitfalls.cs#01)]
 [!code-vb[TPL_Pitfalls#01](../../../samples/snippets/visualbasic/VS_Snippets_Misc/tpl_pitfalls/vb/pitfalls_vb.vb#01)]  
  
 <span data-ttu-id="5d1a7-153">在此示例中，一个迭代设置一个事件，而所有的其他迭代则等待该事件。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-153">In this example, one iteration sets an event, and all other iterations wait on the event.</span></span> <span data-ttu-id="5d1a7-154">在设置事件的迭代完成之前，任何等待迭代均无法完成。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-154">None of the waiting iterations can complete until the event-setting iteration has completed.</span></span> <span data-ttu-id="5d1a7-155">但是，在设置事件的迭代有机会执行之前，等待迭代可能会阻止用于执行并行循环的所有线程。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-155">However, it is possible that the waiting iterations block all threads that are used to execute the parallel loop, before the event-setting iteration has had a chance to execute.</span></span> <span data-ttu-id="5d1a7-156">这将导致死锁 – 设置事件的迭代将永不会执行，并且等待迭代将永远不会醒来。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-156">This results in a deadlock – the event-setting iteration will never execute, and the waiting iterations will never wake up.</span></span>  
  
 <span data-ttu-id="5d1a7-157">具体而言，并行循环的一个迭代绝不应该等待循环的另一个迭代来继续执行。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-157">In particular, one iteration of a parallel loop should never wait on another iteration of the loop to make progress.</span></span> <span data-ttu-id="5d1a7-158">如果并行循环决定按相反的顺序安排迭代，则会发生死锁。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-158">If the parallel loop decides to schedule the iterations sequentially but in the opposite order, a deadlock will occur.</span></span>  
  
## <a name="avoid-executing-parallel-loops-on-the-ui-thread"></a><span data-ttu-id="5d1a7-159">避免在 UI 线程上执行并行循环</span><span class="sxs-lookup"><span data-stu-id="5d1a7-159">Avoid Executing Parallel Loops on the UI Thread</span></span>  
 <span data-ttu-id="5d1a7-160">务必要使应用程序的用户界面 (UI) 保持响应状态。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-160">It is important to keep your application's user interface (UI) responsive.</span></span> <span data-ttu-id="5d1a7-161">如果操作包含足够的工作来保证并行化，则可能不应在 UI 线程上运行该操作。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-161">If an operation contains enough work to warrant parallelization, then it likely should not be run that operation on the UI thread.</span></span>  <span data-ttu-id="5d1a7-162">相反，而是应卸载要在后台线程上运行的该操作。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-162">Instead, it should offload that operation to be run on a background thread.</span></span> <span data-ttu-id="5d1a7-163">例如，如果要使用并行循环来计算随后应在 UI 控件中呈现的某些数据，则应考虑在任务实例内执行循环，而不是直接在 UI 事件处理程序中执行。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-163">For example, if you want to use a parallel loop to compute some data that should then be rendered into a UI control, you should consider executing the loop within a task instance rather than directly in a UI event handler.</span></span>  <span data-ttu-id="5d1a7-164">只有先当核心计算完成后，才应将 UI 更新封送回 UI 线程。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-164">Only when the core computation has completed should you then marshal the UI update back to the UI thread.</span></span>  
  
 <span data-ttu-id="5d1a7-165">如果确实要在 UI 线程上运行并行循环，请当心，避免从循环内更新 UI 控件。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-165">If you do run parallel loops on the UI thread, be careful to avoid updating UI controls from within the loop.</span></span> <span data-ttu-id="5d1a7-166">如果尝试从在 UI 线程上执行的并行循环内更新 UI 控件，将可能会导致状态损坏、异常、更新延迟甚至死锁，具体将取决于 UI 更新的调用方式。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-166">Attempting to update UI controls from within a parallel loop that is executing on the UI thread can lead to state corruption, exceptions, delayed updates, and even deadlocks, depending on how the UI update is invoked.</span></span> <span data-ttu-id="5d1a7-167">在下面的示例中，在所有的迭代完成之前，并行循环将阻止它在其上执行的 UI 线程。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-167">In the following example, the parallel loop blocks the UI thread on which it’s executing until all iterations are complete.</span></span> <span data-ttu-id="5d1a7-168">不过，如果循环的迭代是对后台线程运行（就像 <xref:System.Threading.Tasks.Parallel.For%2A> 一样），调用 Invoke 可能会导致将消息提交到 UI 线程，并阻止等待处理相应消息。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-168">However, if an iteration of the loop is running on a background thread (as <xref:System.Threading.Tasks.Parallel.For%2A> may do), the call to Invoke causes a message to be submitted to the UI thread and blocks waiting for that message to be processed.</span></span> <span data-ttu-id="5d1a7-169">由于 UI 线程受阻止而无法运行 <xref:System.Threading.Tasks.Parallel.For%2A>，因此消息永远无法得到处理，且 UI 线程死锁。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-169">Since the UI thread is blocked running the <xref:System.Threading.Tasks.Parallel.For%2A>, the message can never be processed, and the UI thread deadlocks.</span></span>  
  
 [!code-csharp[TPL_Pitfalls#02](../../../samples/snippets/csharp/VS_Snippets_Misc/tpl_pitfalls/cs/pitfalls.cs#02)]
 [!code-vb[TPL_Pitfalls#02](../../../samples/snippets/visualbasic/VS_Snippets_Misc/tpl_pitfalls/vb/pitfalls_vb.vb#02)]  
  
 <span data-ttu-id="5d1a7-170">下面的示例演示如何通过在任务实例内运行循环来避免死锁。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-170">The following example shows how to avoid the deadlock, by running the loop inside a task instance.</span></span> <span data-ttu-id="5d1a7-171">循环不会阻止 UI 线程，并且消息可得到处理。</span><span class="sxs-lookup"><span data-stu-id="5d1a7-171">The UI thread is not blocked by the loop, and the message can be processed.</span></span>  
  
 [!code-csharp[TPL_Pitfalls#03](../../../samples/snippets/csharp/VS_Snippets_Misc/tpl_pitfalls/cs/pitfalls.cs#03)]
 [!code-vb[TPL_Pitfalls#03](../../../samples/snippets/visualbasic/VS_Snippets_Misc/tpl_pitfalls/vb/pitfalls_vb.vb#03)]  
  
## <a name="see-also"></a><span data-ttu-id="5d1a7-172">请参阅</span><span class="sxs-lookup"><span data-stu-id="5d1a7-172">See also</span></span>

- [<span data-ttu-id="5d1a7-173">并行编程</span><span class="sxs-lookup"><span data-stu-id="5d1a7-173">Parallel Programming</span></span>](../../../docs/standard/parallel-programming/index.md)
- [<span data-ttu-id="5d1a7-174">PLINQ 的潜在问题</span><span class="sxs-lookup"><span data-stu-id="5d1a7-174">Potential Pitfalls with PLINQ</span></span>](../../../docs/standard/parallel-programming/potential-pitfalls-with-plinq.md)
- [<span data-ttu-id="5d1a7-175">并行编程模式：了解并使用 .NET Framework 4 应用并行模式</span><span class="sxs-lookup"><span data-stu-id="5d1a7-175">Patterns for Parallel Programming: Understanding and Applying Parallel Patterns with the .NET Framework 4</span></span>](https://www.microsoft.com/download/details.aspx?id=19222)
