---
title: "编译器错误 CS1938"
ms.date: 07/20/2015
ms.prod: .net
ms.technology: devlang-csharp
ms.topic: article
f1_keywords: CS1938
helpviewer_keywords: CS1938
ms.assetid: fc8de996-f7a1-46e8-b07b-aea520b391b9
caps.latest.revision: "6"
author: BillWagner
ms.author: wiwagn
ms.openlocfilehash: 08308b7152768e94938ec2f3ba2bc312ce2ca7a8
ms.sourcegitcommit: bd1ef61f4bb794b25383d3d72e71041a5ced172e
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 10/18/2017
---
# <a name="compiler-error-cs1938"></a><span data-ttu-id="3555d-102">编译器错误 CS1938</span><span class="sxs-lookup"><span data-stu-id="3555d-102">Compiler Error CS1938</span></span>
<span data-ttu-id="3555d-103">名称“name”不在“等号”右侧的作用域中。</span><span class="sxs-lookup"><span data-stu-id="3555d-103">The name 'name' is not in scope on the right side of 'equals'.</span></span> <span data-ttu-id="3555d-104">请考虑交换位于“等号”任意一侧的表达式。</span><span class="sxs-lookup"><span data-stu-id="3555d-104">Consider swapping the expressions on either side of 'equals'.</span></span>  
  
 <span data-ttu-id="3555d-105">`equals` 关键字是一种特殊运算符，其在 `join` 子句中用于确定两个表达式之间是否相等。</span><span class="sxs-lookup"><span data-stu-id="3555d-105">The `equals` keyword is a special operator that is used in a `join` clause to determine equality between two expressions.</span></span> <span data-ttu-id="3555d-106">左侧源序列的范围变量在等号左侧的作用域中，右侧源的范围变量仅存在于等号左侧的作用域中。</span><span class="sxs-lookup"><span data-stu-id="3555d-106">The range variable for the left side source sequence is in scope on the left side of equals, and the range variable for the right side source is only in scope on the left side of equals.</span></span> <span data-ttu-id="3555d-107">可以通过以下代码示例中的 IntelliSense 进行实验，以对此进行验证。</span><span class="sxs-lookup"><span data-stu-id="3555d-107">You can verify this by experimenting with IntelliSense in the following code example.</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="3555d-108">更正此错误</span><span class="sxs-lookup"><span data-stu-id="3555d-108">To correct this error</span></span>  
  
1.  <span data-ttu-id="3555d-109">交换两个范围变量的位置，如以下示例中的注释行所示：</span><span class="sxs-lookup"><span data-stu-id="3555d-109">Swap the position of the two range variables as shown in the commented line in the following example:</span></span>  
  
## <a name="example"></a><span data-ttu-id="3555d-110">示例</span><span class="sxs-lookup"><span data-stu-id="3555d-110">Example</span></span>  
 <span data-ttu-id="3555d-111">以下代码生成 CS1938：</span><span class="sxs-lookup"><span data-stu-id="3555d-111">The following code generates CS1938:</span></span>  
  
```  
// cs1938.cs  
using System.Linq;  
class Test  
{  
    static void Main()  
    {  
        int[] sourceA = { 1, 2, 3, 4, 5 };  
        int[] sourceB = { 3, 4, 5, 6, 7 };  
  
        var query = from a in sourceA  
                    join b in sourceB on b equals a // CS1938  
                    // Try the following line instead.  
                    // join b in sourceB on a equals b  
                    select new { a, b };  
    }  
}  
```  
  
## <a name="see-also"></a><span data-ttu-id="3555d-112">另请参阅</span><span class="sxs-lookup"><span data-stu-id="3555d-112">See Also</span></span>  
 [<span data-ttu-id="3555d-113">join 子句</span><span class="sxs-lookup"><span data-stu-id="3555d-113">join clause</span></span>](../../csharp/language-reference/keywords/join-clause.md)
