---
title: 编译器错误 CS1935
ms.date: 07/20/2015
f1_keywords:
- CS1935
helpviewer_keywords:
- CS1935
ms.assetid: d5dda801-fbf3-4340-bfe1-f9409f2d344d
ms.openlocfilehash: edf0098beda11defdbe1d9754b85d4cf84fb1e01
ms.sourcegitcommit: 0be8a279af6d8a43e03141e349d3efd5d35f8767
ms.translationtype: HT
ms.contentlocale: zh-CN
ms.lasthandoff: 04/18/2019
ms.locfileid: "59298118"
---
# <a name="compiler-error-cs1935"></a><span data-ttu-id="552ec-102">编译器错误 CS1935</span><span class="sxs-lookup"><span data-stu-id="552ec-102">Compiler Error CS1935</span></span>
<span data-ttu-id="552ec-103">未能找到源类型“type”的查询模式的实现。</span><span class="sxs-lookup"><span data-stu-id="552ec-103">Could not find an implementation of the query pattern for source type 'type'.</span></span> <span data-ttu-id="552ec-104">找不到“method”。</span><span class="sxs-lookup"><span data-stu-id="552ec-104">'method' not found.</span></span> <span data-ttu-id="552ec-105">是否缺少对“System.Core.dll”的引用，或者缺少针对“System.Linq”的 using 指令？</span><span class="sxs-lookup"><span data-stu-id="552ec-105">Are you missing a reference to 'System.Core.dll' or a using directive for 'System.Linq'?</span></span>  
  
 <span data-ttu-id="552ec-106">在查询中的源类型必须是 `IEnumerable`、 `IEnumerable<T>`或派生的类型，或者你或其他人已为其实现了标准查询运算符的类型。</span><span class="sxs-lookup"><span data-stu-id="552ec-106">The source type in a query must be `IEnumerable`, `IEnumerable<T>`, or a derived type, or a type for which you or someone else has implemented the standard query operators.</span></span> <span data-ttu-id="552ec-107">如果源类型为 `IEnumerable` 或 `IEnumerable<T>`，必须添加一个对 system.core.dll 的引用和一个用于 System.Linq 命名空间的 `using` 指令，以便将标准查询运算符扩展方法引入作用域。</span><span class="sxs-lookup"><span data-stu-id="552ec-107">If the source type is an `IEnumerable` or `IEnumerable<T>`, you must add a reference to system.core.dll and a `using` directive for the System.Linq namespace in order to bring the standard query operator extension methods into scope.</span></span> <span data-ttu-id="552ec-108">必须以同样方式，使用 `using` 指令，以及（如有必要）使用对程序集的引用将标准查询运算符的自定义实现引入作用域。</span><span class="sxs-lookup"><span data-stu-id="552ec-108">Custom implementations of the standard query operators must be brought into scope in the same way, with a `using` directive and, if necessary, a reference to the assembly.</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="552ec-109">更正此错误</span><span class="sxs-lookup"><span data-stu-id="552ec-109">To correct this error</span></span>  
  
1. <span data-ttu-id="552ec-110">添加所需的 using 指令和对项目的引用。</span><span class="sxs-lookup"><span data-stu-id="552ec-110">Add the required using directives and references to the project.</span></span>  
  
## <a name="example"></a><span data-ttu-id="552ec-111">示例</span><span class="sxs-lookup"><span data-stu-id="552ec-111">Example</span></span>  
 <span data-ttu-id="552ec-112">以下代码生成 CS1935，因为用于 System.Linq 的 `using` 指令被注释掉：</span><span class="sxs-lookup"><span data-stu-id="552ec-112">The following code generates CS1935 because the `using` directive for System.Linq is commented out:</span></span>  
  
```csharp  
// cs1935.cs  
// CS1935  
using System;  
using System.Collections.Generic;  
// using System.Linq;  
  
class Test  
{  
    static int Main()  
    {  
        int[] nums = {0,1,2,3,4,5};  
        IEnumerable<int> e = from n in nums  
                        where n > 3  
                        select n;  
        return 0;  
    }  
}  
```  
  
## <a name="see-also"></a><span data-ttu-id="552ec-113">请参阅</span><span class="sxs-lookup"><span data-stu-id="552ec-113">See also</span></span>

- [<span data-ttu-id="552ec-114">标准查询运算符概述</span><span class="sxs-lookup"><span data-stu-id="552ec-114">Standard Query Operators Overview</span></span>](../../csharp/programming-guide/concepts/linq/standard-query-operators-overview.md)
