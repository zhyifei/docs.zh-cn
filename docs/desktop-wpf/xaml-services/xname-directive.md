---
title: x:Name 指令
ms.date: 03/30/2017
f1_keywords:
- x:Name
- xName
- Name
helpviewer_keywords:
- x:Name attribute [XAML Services]
- XAML [XAML Services], x:Name attribute
- Name attribute in XAML [XAML Services]
ms.assetid: b7e61222-e8cf-48d2-acd0-6df3b7685d48
ms.openlocfilehash: 9f812a49a3217a563be1bd7f1d999b641c28463d
ms.sourcegitcommit: c2d9718996402993cf31541f11e95531bc68bad0
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 02/27/2020
ms.locfileid: "81432712"
---
# <a name="xname-directive"></a><span data-ttu-id="b6e2a-102">x:Name 指令</span><span class="sxs-lookup"><span data-stu-id="b6e2a-102">x:Name Directive</span></span>

<span data-ttu-id="b6e2a-103">唯一标识 XAML 定义的元素在 XAML 命名范围中。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-103">Uniquely identifies XAML-defined elements in a XAML namescope.</span></span> <span data-ttu-id="b6e2a-104">当框架提供 API 或实现在运行时访问 XAML 创建的对象图的行为时，XAML 命名范围及其唯一性模型可以应用于实例化对象。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-104">XAML namescopes and their uniqueness models can be applied to the instantiated objects, when frameworks provide APIs or implement behaviors that access the XAML-created object graph at run time.</span></span>

## <a name="xaml-attribute-usage"></a><span data-ttu-id="b6e2a-105">XAML 属性用法</span><span class="sxs-lookup"><span data-stu-id="b6e2a-105">XAML Attribute Usage</span></span>

```xaml
<object x:Name="XAMLNameValue".../>
```

## <a name="xaml-values"></a><span data-ttu-id="b6e2a-106">XAML 值</span><span class="sxs-lookup"><span data-stu-id="b6e2a-106">XAML Values</span></span>

|||
|-|-|
|`XAMLNameValue`|<span data-ttu-id="b6e2a-107">符合[XamlName 语法](xamlname-grammar.md)限制的字符串。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-107">A string that conforms to the restrictions of the [XamlName Grammar](xamlname-grammar.md).</span></span>|

## <a name="remarks"></a><span data-ttu-id="b6e2a-108">备注</span><span class="sxs-lookup"><span data-stu-id="b6e2a-108">Remarks</span></span>

<span data-ttu-id="b6e2a-109">应用于`x:Name`框架的备份编程模型后，名称等效于保存对象引用的变量或构造函数返回的实例。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-109">After `x:Name` is applied to a framework's backing programming model, the name is equivalent to the variable that holds an object reference or an instance as returned by a constructor.</span></span>

<span data-ttu-id="b6e2a-110">`x:Name`指令用法的值必须在 XAML 名称范围内是唯一的。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-110">The value of an `x:Name` directive usage must be unique within a XAML namescope.</span></span> <span data-ttu-id="b6e2a-111">默认情况下，当 .NET XAML 服务 API 使用时，主 XAML 命名范围在单个 XAML 生产的 XAML 根元素中定义，并包含该 XAML 生产中包含的元素。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-111">By default when used by .NET XAML Services API, the primary XAML namescope is defined at the XAML root element of a single XAML production, and encompasses the elements that are contained in that XAML production.</span></span> <span data-ttu-id="b6e2a-112">框架可以定义单个 XAML 生产中可能发生的其他离散 XAML 命名范围，以解决特定方案。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-112">Additional discrete XAML namescopes that might occur within a single XAML production can be defined by frameworks to address specific scenarios.</span></span> <span data-ttu-id="b6e2a-113">例如，在 WPF 中，新的 XAML 命名范围由在 XAML 生产上定义的任何模板定义和创建。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-113">For example, in WPF, new XAML namescopes are defined and created by any template that is also defined on that XAML production.</span></span> <span data-ttu-id="b6e2a-114">有关 XAML 名称范围的详细信息（为 WPF 编写，但与许多 XAML 名称范围概念相关），请参阅[WPF XAML 名称范围](../../framework/wpf/advanced/wpf-xaml-namescopes.md)。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-114">For more information about XAML namescopes (written for WPF but relevant for many XAML namescope concepts), see [WPF XAML Namescopes](../../framework/wpf/advanced/wpf-xaml-namescopes.md).</span></span>

<span data-ttu-id="b6e2a-115">通常，`x:Name`不应应用于也使用`x:Key`的情况。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-115">In general, `x:Name` should not be applied in situations that also use `x:Key`.</span></span> <span data-ttu-id="b6e2a-116">由特定现有框架实现的 XAML 在 和`x:Key``x:Name`之间引入了替换概念，但这不是建议的做法。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-116">XAML implementations by specific existing frameworks have introduced substitution concepts between `x:Key` and `x:Name`, but that is not a recommended practice.</span></span> <span data-ttu-id="b6e2a-117">.NET XAML 服务在处理名称/密钥信息（如 或<xref:System.Windows.Markup.INameScope><xref:System.Windows.Markup.DictionaryKeyPropertyAttribute>） 时不支持此类替换概念。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-117">.NET XAML Services does not support such substitution concepts when handling name/key information such as <xref:System.Windows.Markup.INameScope> or <xref:System.Windows.Markup.DictionaryKeyPropertyAttribute>.</span></span>

<span data-ttu-id="b6e2a-118">允许`x:Name`性规则以及名称唯一性实施可能由特定的实现框架定义。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-118">Rules for permittance of `x:Name` as well as the name uniqueness enforcement are potentially defined by specific implementing frameworks.</span></span> <span data-ttu-id="b6e2a-119">但是，要与 .NET XAML 服务一起使用，XAML 命名范围的唯一性的框架定义应与本文档中<xref:System.Windows.Markup.INameScope>的信息定义一致，并且应该使用有关信息应用位置的相同规则。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-119">However, to be usable with .NET XAML Services, the framework definitions of XAML namescope uniqueness should be consistent with the definition of <xref:System.Windows.Markup.INameScope> information in this documentation, and should use the same rules regarding where the information is applied.</span></span> <span data-ttu-id="b6e2a-120">例如，[!INCLUDE[TLA#tla_winclient](../../../includes/tlasharptla-winclient-md.md)]实现将各种标记元素划分为单独的<xref:System.Windows.NameScope>范围，例如资源字典、由页面级 XAML 创建的逻辑树、模板和其他延迟内容，然后在每个 XAML 命名范围内强制实施 XAML 名称唯一性。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-120">For example, the [!INCLUDE[TLA#tla_winclient](../../../includes/tlasharptla-winclient-md.md)] implementation divides various markup elements into separate <xref:System.Windows.NameScope> ranges, such as resource dictionaries, the logical tree created by the page-level XAML, templates, and other deferred content, and then enforces XAML name uniqueness within each of those XAML namescopes.</span></span>

<span data-ttu-id="b6e2a-121">对于使用 .NET XAML 服务 XAML 对象编写器的自定义类型`x:Name`，可以建立或更改映射到类型的属性。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-121">For custom types that use .NET XAML Services XAML object writers, a property that maps to `x:Name` on a type can be established or changed.</span></span> <span data-ttu-id="b6e2a-122">通过引用要使用类型定义代码<xref:System.Windows.Markup.RuntimeNamePropertyAttribute>中映射的属性的名称来定义此行为。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-122">You define this behavior by referencing the name of the property to map with the <xref:System.Windows.Markup.RuntimeNamePropertyAttribute> in the type definition code.</span></span>  <span data-ttu-id="b6e2a-123"><xref:System.Windows.Markup.RuntimeNamePropertyAttribute>是类型级属性。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-123"><xref:System.Windows.Markup.RuntimeNamePropertyAttribute> is a type-level attribute.</span></span>

<span data-ttu-id="b6e2a-124">Using.NET XAML 服务，可以通过实现<xref:System.Windows.Markup.INameScope>接口以框架中立的方式定义 XAML 命名范围支持的备份逻辑。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-124">Using.NET XAML Services, the backing logic for XAML namescope support can be defined in a framework-neutral way by implementing the <xref:System.Windows.Markup.INameScope> interface.</span></span>

## <a name="wpf-usage-notes"></a><span data-ttu-id="b6e2a-125">WPF 用法说明</span><span class="sxs-lookup"><span data-stu-id="b6e2a-125">WPF Usage Notes</span></span>

<span data-ttu-id="b6e2a-126">在使用 XAML、[!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)]部分类和代码后面的应用程序的标准生成配置下，指定`x:Name`将成为在标记编译生成任务处理时[!INCLUDE[TLA2#tla_xaml](../../../includes/tla2sharptla-xaml-md.md)]在基础代码中创建的字段的名称，并且该字段包含对对象的引用。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-126">Under the standard build configuration for a [!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] application that uses XAML, partial classes, and code-behind, the specified `x:Name` becomes the name of a field that is created in the underlying code when [!INCLUDE[TLA2#tla_xaml](../../../includes/tla2sharptla-xaml-md.md)] is processed by a markup compilation build task, and that field holds a reference to the object.</span></span> <span data-ttu-id="b6e2a-127">默认情况下，创建的字段是内部字段。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-127">By default, the created field is internal.</span></span> <span data-ttu-id="b6e2a-128">您可以通过指定[x：字段修改器属性](xfieldmodifier-directive.md)来更改字段访问。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-128">You can change field access by specifying the [x:FieldModifier attribute](xfieldmodifier-directive.md).</span></span> <span data-ttu-id="b6e2a-129">在 WPF 和 Silverlight 中，序列是标记编译定义和命名部分类中的字段，但该值最初为空。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-129">In WPF and Silverlight, the sequence is that the markup compile defines and names the field in a partial class, but the value is initially empty.</span></span> <span data-ttu-id="b6e2a-130">然后，从类构造函数`InitializeComponent`中调用名为 生成的方法。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-130">Then, a generated method named `InitializeComponent` is called from within the class constructor.</span></span> <span data-ttu-id="b6e2a-131">`InitializeComponent`由`FindName`使用部分类 XAML 定义部分中存在的每个`x:Name`值作为输入字符串的调用组成。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-131">`InitializeComponent` consists of `FindName` calls using each of the `x:Name` values that exist in the XAML-defined part of the partial class as input strings.</span></span> <span data-ttu-id="b6e2a-132">然后，返回值分配给类似命名的字段引用，以使用从 XAML 分析创建的对象填充字段值。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-132">The return values are then assigned to the like-named field reference to fill the field values with objects that were created from XAML parsing.</span></span> <span data-ttu-id="b6e2a-133">执行 后`InitializeComponent`，可以直接使用`x:Name`/ 字段名称引用运行时对象图，而不必在需要引用 XAML`FindName`定义的对象时显式调用。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-133">The execution of `InitializeComponent` make it possible to reference the run time object graph using the `x:Name` / field name directly, rather than having to call `FindName` explicitly any time you need a reference to a XAML-defined object.</span></span>

<span data-ttu-id="b6e2a-134">对于使用`Page`Microsoft Visual Basic 目标并包含包含生成操作的 XAML 文件的 WPF 应用程序，在编译过程中将`WithEvents`关键字添加到 具有`x:Name`的所有元素，以支持`Handles`事件处理程序委托的语法。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-134">For a WPF application that uses the Microsoft Visual Basic targets and includes XAML files with `Page` build action, a separate reference property is created during compilation that adds the `WithEvents` keyword to all elements that have an `x:Name`, to support `Handles` syntax for event handler delegates.</span></span> <span data-ttu-id="b6e2a-135">此属性始终是公共的。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-135">This property is always public.</span></span> <span data-ttu-id="b6e2a-136">有关详细信息，请参阅 [Visual Basic 和 WPF 事件处理](../../framework/wpf/advanced/visual-basic-and-wpf-event-handling.md)。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-136">For more information, see [Visual Basic and WPF Event Handling](../../framework/wpf/advanced/visual-basic-and-wpf-event-handling.md).</span></span>

<span data-ttu-id="b6e2a-137">`x:Name`WPF XAML 处理器在加载时使用该名称注册到 XAML 命名范围中，即使对于页面未通过生成操作进行标记编译的情况也是如此（例如，资源字典的松散 XAML）。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-137">`x:Name` is used by the WPF XAML processor to register a name into a XAML namescope at load time, even for cases where the page is not markup-compiled by build actions (for example, loose XAML of a resource dictionary).</span></span> <span data-ttu-id="b6e2a-138">此行为的一个原因是绑定可能需要`x:Name`<xref:System.Windows.Data.Binding.ElementName%2A>。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-138">One reason for this behavior is because the `x:Name` is potentially needed for <xref:System.Windows.Data.Binding.ElementName%2A> binding.</span></span> <span data-ttu-id="b6e2a-139">有关详细信息，请参阅[数据绑定概述](../data/data-binding-overview.md)。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-139">For details, see [Data Binding Overview](../data/data-binding-overview.md).</span></span>

<span data-ttu-id="b6e2a-140">如前所述，（`x:Name`或`Name`）不应应用于也使用`x:Key`的情况。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-140">As mentioned previously, `x:Name` (or `Name`) should not be applied in situations that also use `x:Key`.</span></span> <span data-ttu-id="b6e2a-141">具有特殊[!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)]<xref:System.Windows.ResourceDictionary>行为，将自身定义为 XAML 命名范围，但返回 API 的<xref:System.Windows.Markup.INameScope>未实现或 null 值，作为强制执行此行为的一种方式。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-141">The [!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] <xref:System.Windows.ResourceDictionary> has a special behavior of defining itself as a XAML namescope but returning Not Implemented or null values for <xref:System.Windows.Markup.INameScope> APIs as a way to enforce this behavior.</span></span> <span data-ttu-id="b6e2a-142">如果 WPF XAML 解析`Name`器`x:Name`遇到或在 XAML<xref:System.Windows.ResourceDictionary>定义中，则名称不会添加到任何 XAML 名称范围。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-142">If the WPF XAML parser encounters `Name` or `x:Name` in a XAML-defined <xref:System.Windows.ResourceDictionary>, the name is not added to any XAML namescope.</span></span> <span data-ttu-id="b6e2a-143">尝试从任何 XAML 名称范围查找该名称和方法`FindName`将不会返回有效结果。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-143">Attempting to find that name from any XAML namescope and the `FindName` methods will not return valid results.</span></span>

### <a name="xname-and-name"></a><span data-ttu-id="b6e2a-144">x：名称和名称</span><span class="sxs-lookup"><span data-stu-id="b6e2a-144">x:Name and Name</span></span>

<span data-ttu-id="b6e2a-145">许多 WPF 应用程序方案`x:Name`可以避免对属性的任何使用，因为在默认`Name`的 XAML 命名空间中指定的依赖项属性适用于几个重要基类，如<xref:System.Windows.FrameworkElement>和<xref:System.Windows.FrameworkContentElement>满足此相同的用途。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-145">Many WPF application scenarios can avoid any use of the `x:Name` attribute, because the `Name` dependency property as specified in the default XAML namespace for several of the important base classes such as <xref:System.Windows.FrameworkElement> and <xref:System.Windows.FrameworkContentElement> satisfies this same purpose.</span></span> <span data-ttu-id="b6e2a-146">仍有一些常见的 XAML 和 WPF 方案，其中代码访问框架级别`Name`上没有属性的元素非常重要。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-146">There are still some common XAML and WPF scenarios where code access to an element with no `Name` property at the framework level is important.</span></span> <span data-ttu-id="b6e2a-147">例如，某些动画和情节提要支持类不支持`Name`属性，但它们通常需要在代码中引用才能控制动画。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-147">For example, certain animation and storyboard support classes do not support a `Name` property, but they often need to be referenced in code in order to control the animation.</span></span> <span data-ttu-id="b6e2a-148">如果以后打算`x:Name`从代码中引用它们，则应在时间线上指定为属性，并在 XAML 中创建的转换中指定属性。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-148">You should specify `x:Name` as an attribute on timelines and transforms that are created in XAML, if you intend to reference them from code later.</span></span>

<span data-ttu-id="b6e2a-149">如果在<xref:System.Windows.FrameworkElement.Name%2A>类上作为属性可用，<xref:System.Windows.FrameworkElement.Name%2A>`x:Name`并且可以作为属性互换使用，但如果在同一元素上指定两者，则会产生解析异常。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-149">If <xref:System.Windows.FrameworkElement.Name%2A> is available as a property on the class, <xref:System.Windows.FrameworkElement.Name%2A> and `x:Name` can be used interchangeably as attributes, but a parse exception will result if both are specified on the same element.</span></span> <span data-ttu-id="b6e2a-150">如果编译了 XAML，则异常将在标记编译上发生，否则将在加载时发生。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-150">If the XAML is markup compiled, the exception will occur on the markup compile, otherwise it occurs on load.</span></span>

<span data-ttu-id="b6e2a-151"><xref:System.Windows.FrameworkElement.Name%2A>可以使用 XAML 属性语法和 在 代码中使用<xref:System.Windows.DependencyObject.SetValue%2A>。但是请注意，在已<xref:System.Windows.FrameworkElement.Name%2A>加载 XAML 的大多数情况下，在代码中设置属性不会在 XAML 命名范围内创建具有代表性的字段引用。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-151"><xref:System.Windows.FrameworkElement.Name%2A> can be set using XAML attribute syntax, and in code using <xref:System.Windows.DependencyObject.SetValue%2A>; note however that setting the <xref:System.Windows.FrameworkElement.Name%2A> property in code does not create the representative field reference within the XAML namescope in most circumstances where the XAML is already loaded.</span></span> <span data-ttu-id="b6e2a-152">不要尝试在代码中设置<xref:System.Windows.FrameworkElement.Name%2A>，而是根据相应的<xref:System.Windows.NameScope>名称范围使用代码中的方法。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-152">Instead of attempting to set <xref:System.Windows.FrameworkElement.Name%2A> in code, use <xref:System.Windows.NameScope> methods from code, against the appropriate namescope.</span></span>

<span data-ttu-id="b6e2a-153"><xref:System.Windows.FrameworkElement.Name%2A>也可以使用包含内部文本的属性元素语法进行设置，但这种情况并不常见。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-153"><xref:System.Windows.FrameworkElement.Name%2A> can also be set using property element syntax with inner text, but that is uncommon.</span></span> <span data-ttu-id="b6e2a-154">相反，`x:Name`不能在 XAML 属性元素语法中设置，也不能在<xref:System.Windows.DependencyObject.SetValue%2A>代码中使用 。它只能使用对象的属性语法进行设置，因为它是指令。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-154">In contrast, `x:Name` cannot be set in XAML property element syntax, or in code using <xref:System.Windows.DependencyObject.SetValue%2A>; it can only be set using attribute syntax on objects because it is a directive.</span></span>

## <a name="silverlight-usage-notes"></a><span data-ttu-id="b6e2a-155">银光使用说明</span><span class="sxs-lookup"><span data-stu-id="b6e2a-155">Silverlight Usage Notes</span></span>

<span data-ttu-id="b6e2a-156">`x:Name`银光单独记录。</span><span class="sxs-lookup"><span data-stu-id="b6e2a-156">`x:Name` for Silverlight is documented separately.</span></span> <span data-ttu-id="b6e2a-157">有关详细信息，请参阅[XAML 命名空间 （x：）语言功能（银光）](https://docs.microsoft.com/previous-versions/windows/silverlight/dotnet-windows-silverlight/cc188995(v=vs.95)).</span><span class="sxs-lookup"><span data-stu-id="b6e2a-157">For more information, see [XAML Namespace (x:) Language Features (Silverlight)](https://docs.microsoft.com/previous-versions/windows/silverlight/dotnet-windows-silverlight/cc188995(v=vs.95)).</span></span>

## <a name="see-also"></a><span data-ttu-id="b6e2a-158">请参阅</span><span class="sxs-lookup"><span data-stu-id="b6e2a-158">See also</span></span>

- <xref:System.Windows.FrameworkElement.Name%2A?displayProperty=nameWithType>
- <xref:System.Windows.FrameworkContentElement.Name%2A?displayProperty=nameWithType>
- [<span data-ttu-id="b6e2a-159">WPF 中的树</span><span class="sxs-lookup"><span data-stu-id="b6e2a-159">Trees in WPF</span></span>](../../framework/wpf/advanced/trees-in-wpf.md)
