---
title: .NET XAML 服务的 XAML 命名空间
ms.date: 03/30/2017
ms.assetid: e4f15f13-c420-4c1e-aeab-9b6f50212047
ms.openlocfilehash: 2ae57d08fade85c59fea2a54b653a2f775b57415
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 03/12/2020
ms.locfileid: "81433060"
---
# <a name="xaml-namespaces-for-net-xaml-services"></a><span data-ttu-id="448be-102">.NET XAML 服务的 XAML 命名空间</span><span class="sxs-lookup"><span data-stu-id="448be-102">XAML Namespaces for .NET XAML Services</span></span>
<span data-ttu-id="448be-103">XAML 命名空间是一个扩展 XML 命名空间定义的概念。</span><span class="sxs-lookup"><span data-stu-id="448be-103">A XAML namespace is a concept that expands on the definition of an XML namespace.</span></span> <span data-ttu-id="448be-104">与 XML 命名空间类似，可以使用标记中`xmlns`的属性定义 XAML 命名空间。</span><span class="sxs-lookup"><span data-stu-id="448be-104">Similar to an XML namespace, you can define a XAML namespace using an `xmlns` attribute in markup.</span></span> <span data-ttu-id="448be-105">XAML 命名空间也表示在 XAML 节点流和其他 XAML 服务 API 中。</span><span class="sxs-lookup"><span data-stu-id="448be-105">XAML namespaces are also represented in the XAML node stream and other XAML Services APIs.</span></span> <span data-ttu-id="448be-106">本主题定义了 XAML 命名空间概念，并介绍了如何定义 XAML 命名空间并由 XAML 架构上下文和 .NET XAML 服务的其他方面使用。</span><span class="sxs-lookup"><span data-stu-id="448be-106">This topic defines the XAML namespace concept, and describes how XAML namespaces can be defined and are used by XAML schema contexts and other aspects of .NET XAML Services.</span></span>  
  
## <a name="xml-namespace-and-xaml-namespace"></a><span data-ttu-id="448be-107">XML 命名空间和 XAML 命名空间</span><span class="sxs-lookup"><span data-stu-id="448be-107">XML Namespace and XAML Namespace</span></span>  
 <span data-ttu-id="448be-108">XAML 命名空间是一个专门的 XML 命名空间，就像 XAML 是 XML 的专用形式，并使用基本的 XML 形式进行标记一样。</span><span class="sxs-lookup"><span data-stu-id="448be-108">A XAML namespace is a specialized XML namespace, just as XAML is a specialized form of XML and uses the basic XML form for its markup.</span></span> <span data-ttu-id="448be-109">在标记中，通过应用于元素`xmlns`的属性声明 XAML 命名空间及其映射。</span><span class="sxs-lookup"><span data-stu-id="448be-109">In markup, you declare a XAML namespace and its mapping through an `xmlns` attribute applied to an element.</span></span> <span data-ttu-id="448be-110">可以`xmlns`对声明 XAML 命名空间的同一元素进行声明。</span><span class="sxs-lookup"><span data-stu-id="448be-110">The `xmlns` declaration can be made to the same element that the XAML namespace is declared in.</span></span> <span data-ttu-id="448be-111">对元素所做的 XAML 命名空间声明对该元素、该元素的所有属性以及该元素的所有子级都有效。</span><span class="sxs-lookup"><span data-stu-id="448be-111">A XAML namespace declaration made to an element is valid for that element, all attributes of that element, and all children of that element.</span></span> <span data-ttu-id="448be-112">属性可以使用与包含该属性的元素不相同的 XAML 命名空间，只要属性名称本身在标记中引用前缀作为其属性名称的一部分。</span><span class="sxs-lookup"><span data-stu-id="448be-112">Attributes can use a XAML namespace that is not the same as the element that contains the attribute, so long as the attribute name itself references the prefix as part of its attribute name in markup.</span></span>  
  
 <span data-ttu-id="448be-113">XAML 命名空间与 XML 命名空间的区别是，XML 命名空间可用于引用架构或仅用于区分实体。</span><span class="sxs-lookup"><span data-stu-id="448be-113">The distinction of a XAML namespace versus an XML namespace is that an XML namespace might be used to reference a schema or simply to differentiate entities.</span></span> <span data-ttu-id="448be-114">对于 XAML，XAML 中使用的类型和成员最终必须解析为支持类型，并且 XML 架构概念不能很好地应用于此功能。</span><span class="sxs-lookup"><span data-stu-id="448be-114">For XAML, the types and members as used in XAML must ultimately be resolved to backing types, and XML schema concepts do not apply well to this capability.</span></span> <span data-ttu-id="448be-115">XAML 命名空间包含 XAML 架构上下文必须具有的信息，以便执行此类型映射。</span><span class="sxs-lookup"><span data-stu-id="448be-115">The XAML namespace contains information that the XAML schema context must have available in order to perform this type mapping.</span></span>  
  
## <a name="xaml-namespace-components"></a><span data-ttu-id="448be-116">XAML 命名空间组件</span><span class="sxs-lookup"><span data-stu-id="448be-116">XAML Namespace Components</span></span>  
 <span data-ttu-id="448be-117">XAML 命名空间定义有两个组件：前缀和标识符。</span><span class="sxs-lookup"><span data-stu-id="448be-117">The XAML namespace definition has two components: a prefix, and an identifier.</span></span> <span data-ttu-id="448be-118">当在标记中声明 XAML 命名空间或在 XAML 类型系统中定义时，每个组件都存在。</span><span class="sxs-lookup"><span data-stu-id="448be-118">Each of these components is present when a XAML namespace is declared in markup, or defined in the XAML type system.</span></span>  
  
 <span data-ttu-id="448be-119">前缀可以是[XML 1.0 规范中 W3C 命名空间](https://www.w3.org/TR/REC-xml-names/)允许的任何字符串。</span><span class="sxs-lookup"><span data-stu-id="448be-119">The prefix can be any string as allowed by the [W3C Namespaces in XML 1.0 specification](https://www.w3.org/TR/REC-xml-names/).</span></span> <span data-ttu-id="448be-120">按照惯例，前缀通常是短字符串，因为前缀在典型的标记文件中重复多次。</span><span class="sxs-lookup"><span data-stu-id="448be-120">By convention, the prefixes are typically short strings, because the prefix is repeated many times in a typical markup file.</span></span> <span data-ttu-id="448be-121">某些用于多个 XAML 实现中的 XAML 命名空间使用特定的常规前缀。</span><span class="sxs-lookup"><span data-stu-id="448be-121">Certain XAML namespaces that are intended to be used in multiple XAML implementations use particular conventional prefixes.</span></span> <span data-ttu-id="448be-122">例如，XAML 语言 XAML 命名空间通常使用前缀`x`进行映射。</span><span class="sxs-lookup"><span data-stu-id="448be-122">For example, the XAML language XAML namespace is typically mapped using the prefix `x`.</span></span> <span data-ttu-id="448be-123">您可以定义默认的 XAML 命名空间，其中前缀在定义中未给出，但如果by.NET XAML 服务 API 定义或查询，则表示为空字符串。</span><span class="sxs-lookup"><span data-stu-id="448be-123">You can define a default XAML namespace, where the prefix is not given in the definition but is represented as an empty string if defined or queried by.NET XAML Services API.</span></span> <span data-ttu-id="448be-124">通常，特意选择默认 XAML 命名空间，以便 XAML 实现技术及其方案和词汇量最大限度地省略标记。</span><span class="sxs-lookup"><span data-stu-id="448be-124">Typically, the default XAML namespace is deliberately chosen in order to promote a maximized amount of prefix-omitting markup by a XAML-implementing technology and its scenarios and vocabularies.</span></span>  
  
 <span data-ttu-id="448be-125">标识符可以是[XML 1.0 规范中 W3C 命名空间](https://www.w3.org/TR/REC-xml-names/)允许的任何字符串。</span><span class="sxs-lookup"><span data-stu-id="448be-125">The identifier can be any string as allowed by the [W3C Namespaces in XML 1.0 specification](https://www.w3.org/TR/REC-xml-names/).</span></span> <span data-ttu-id="448be-126">按照惯例，XML 命名空间或 XAML 命名空间的标识符通常以 URI 形式提供，通常作为协议限定的绝对 URI。</span><span class="sxs-lookup"><span data-stu-id="448be-126">By convention, identifiers for either XML namespaces or XAML namespaces are often given in URI form, typically as a protocol-qualified absolute URI.</span></span> <span data-ttu-id="448be-127">通常，定义特定 XAML 词汇表的版本信息作为路径字符串的一部分隐含。</span><span class="sxs-lookup"><span data-stu-id="448be-127">Often, version information that defines a particular XAML vocabulary is implied as part of the path string.</span></span> <span data-ttu-id="448be-128">XAML 命名空间添加除 XML URI 约定之外的其他标识符约定。</span><span class="sxs-lookup"><span data-stu-id="448be-128">XAML namespaces add an additional identifier convention beyond the XML URI convention.</span></span> <span data-ttu-id="448be-129">对于 XAML 命名空间，标识符传达 XAML 架构上下文所需的信息，以便解析在 XAML 命名空间下指定为元素的类型，或向成员解析属性。</span><span class="sxs-lookup"><span data-stu-id="448be-129">For XAML namespaces, the identifier communicates information that is needed by a XAML schema context in order to resolve the types that are specified as elements under that XAML namespace, or to resolve attributes to members.</span></span>  
  
 <span data-ttu-id="448be-130">为了将信息传达给 XAML 架构上下文，XAML 命名空间的标识符可能仍以 URI 形式。</span><span class="sxs-lookup"><span data-stu-id="448be-130">For purposes of communicating information to a XAML schema context, the identifier for a XAML namespace might still be in URI form.</span></span> <span data-ttu-id="448be-131">但是，在这种情况下，URI 也声明为特定程序集或程序集列表中的匹配标识符。</span><span class="sxs-lookup"><span data-stu-id="448be-131">However, in this case the URI is also declared as a matching identifier in a particular assembly or list of assemblies.</span></span> <span data-ttu-id="448be-132">这是通过在装配体中分配 程序集来实现<xref:System.Windows.Markup.XmlnsDefinitionAttribute>的。</span><span class="sxs-lookup"><span data-stu-id="448be-132">This is done in assemblies by attributing the assembly with <xref:System.Windows.Markup.XmlnsDefinitionAttribute>.</span></span> <span data-ttu-id="448be-133">在 .NET XAML 服务中，默认的 XAML 架构上下文支持此方法，用于标识 XAML 命名空间和在属性程序集中支持基于 CLR 的类型解析行为。</span><span class="sxs-lookup"><span data-stu-id="448be-133">This method of identifying the XAML namespace and supporting a CLR-based type resolution behavior in the attributed assembly is supported by the default XAML schema context in .NET XAML Services.</span></span> <span data-ttu-id="448be-134">更一般情况下，此约定可用于 XAML 架构上下文合并 CLR 或基于默认 XAML 架构上下文的情况，这是从 CLR 程序集读取 CLR 属性所必需的。</span><span class="sxs-lookup"><span data-stu-id="448be-134">More generally, this convention can be used for cases where the XAML schema context incorporates the CLR or is based on the default XAML schema context, which is necessary in order to read CLR attributes from CLR assemblies.</span></span>  
  
 <span data-ttu-id="448be-135">XAML 命名空间也可以由通信 CLR 命名空间和类型定义程序集的约定标识。</span><span class="sxs-lookup"><span data-stu-id="448be-135">XAML namespaces also can be identified by a convention that communicates a CLR namespace and a type-defining assembly.</span></span> <span data-ttu-id="448be-136">此约定用于包含类型的程序集中不存在<xref:System.Windows.Markup.XmlnsDefinitionAttribute>归因的情况。</span><span class="sxs-lookup"><span data-stu-id="448be-136">This convention is used in cases where no <xref:System.Windows.Markup.XmlnsDefinitionAttribute> attribution exists in the assemblies that contain types.</span></span> <span data-ttu-id="448be-137">此约定可能比 URI 约定复杂，并且具有歧义和重复的可能性，因为有多种方式引用程序集。</span><span class="sxs-lookup"><span data-stu-id="448be-137">This convention is potentially more complex than the URI convention, and also has the potential for ambiguity and duplication, because there are multiple ways of referring to an assembly.</span></span>  
  
 <span data-ttu-id="448be-138">使用 CLR 命名空间和程序集约定的标识符的最基本形式如下：</span><span class="sxs-lookup"><span data-stu-id="448be-138">The most basic form of an identifier that uses the CLR namespace and assembly convention is as follows:</span></span>  
  
 `clr-namespace:clrnsName; assembly=assemblyShortName`
  
 <span data-ttu-id="448be-139">`clr-namespace:`是`; assembly=`语法的文本组件。</span><span class="sxs-lookup"><span data-stu-id="448be-139">`clr-namespace:` and `; assembly=` are literal components of the syntax.</span></span>  
  
 <span data-ttu-id="448be-140">*clrnsName*是标识 CLR 命名空间的字符串名称。</span><span class="sxs-lookup"><span data-stu-id="448be-140">*clrnsName* is the string name that identifies a CLR namespace.</span></span> <span data-ttu-id="448be-141">此字符串名称包括任何内部点字符 （.），这些字符提供有关 CLR 命名空间及其与其他 CLR 命名空间的关系的提示。</span><span class="sxs-lookup"><span data-stu-id="448be-141">This string name includes any internal dot characters (.) that provide hints about the CLR namespace and its relation to other CLR namespaces.</span></span>
  
 <span data-ttu-id="448be-142">*程序集ShortName*是定义 XAML 中有用的类型的程序集的字符串名称。</span><span class="sxs-lookup"><span data-stu-id="448be-142">*assemblyShortName* is the string name of an assembly that defines types that are useful in XAML.</span></span> <span data-ttu-id="448be-143">要通过声明的 XAML 命名空间访问的类型应由程序集定义，并在*clrnsName*指定的 CLR 命名空间中声明。</span><span class="sxs-lookup"><span data-stu-id="448be-143">The types to be accessed through the declared XAML namespace are expected to be defined by the assembly and to be declared within the CLR namespace specified by *clrnsName*.</span></span> <span data-ttu-id="448be-144">此字符串名称通常与 报告<xref:System.Reflection.AssemblyName.Name%2A?displayProperty=nameWithType>的信息并行。</span><span class="sxs-lookup"><span data-stu-id="448be-144">This string name typically parallels the information as reported by <xref:System.Reflection.AssemblyName.Name%2A?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="448be-145">CLR 命名空间和程序集约定的更完整定义如下：</span><span class="sxs-lookup"><span data-stu-id="448be-145">A more complete definition of the CLR namespace and assembly convention is as follows:</span></span>  
  
 `clr-namespace:clrnsName; assembly=assemblyName`
  
 <span data-ttu-id="448be-146">*程序集名称*表示作为<xref:System.Reflection.Assembly.Load%28System.String%29?displayProperty=nameWithType>输入合法的任何字符串。</span><span class="sxs-lookup"><span data-stu-id="448be-146">*assemblyName* represents any string that is legal as an <xref:System.Reflection.Assembly.Load%28System.String%29?displayProperty=nameWithType> input.</span></span> <span data-ttu-id="448be-147">此字符串可以包括区域性、公钥或版本信息（这些概念的定义在<xref:System.Reflection.Assembly>的参考主题中定义。</span><span class="sxs-lookup"><span data-stu-id="448be-147">This string can include culture, public key, or version information (definitions of these concepts are defined in the reference topic for <xref:System.Reflection.Assembly>).</span></span> <span data-ttu-id="448be-148">COFF 格式和证据（如其他过载使用<xref:System.Reflection.Assembly.Load%2A>）与 XAML 程序集加载目的无关;所有加载信息必须作为字符串显示。</span><span class="sxs-lookup"><span data-stu-id="448be-148">COFF format and evidence (as used by other overloads of <xref:System.Reflection.Assembly.Load%2A>) are not relevant for XAML assembly loading purposes; all load information must be presented as a string.</span></span>  
  
 <span data-ttu-id="448be-149">为程序集指定公钥是 XAML 安全性的有用技术，或者用于删除如果程序集以简单名称加载或预先存在于缓存或应用程序域中可能存在的歧义。</span><span class="sxs-lookup"><span data-stu-id="448be-149">Specifying a public key for the assembly is a useful technique for XAML security, or for removing possible ambiguity that can exist if assemblies are loaded by simple name, or pre-exist in a cache or application domain.</span></span> <span data-ttu-id="448be-150">有关详细信息，请参阅[XAML 安全注意事项](security-considerations.md)。</span><span class="sxs-lookup"><span data-stu-id="448be-150">For more information, see [XAML Security Considerations](security-considerations.md).</span></span>  
  
## <a name="xaml-namespace-declarations-in-the-xaml-services-api"></a><span data-ttu-id="448be-151">XAML 服务 API 中的 XAML 命名空间声明</span><span class="sxs-lookup"><span data-stu-id="448be-151">XAML Namespace Declarations in the XAML Services API</span></span>  
 <span data-ttu-id="448be-152">在 XAML 服务 API 中，XAML 命名空间声明<xref:System.Xaml.NamespaceDeclaration>由对象表示。</span><span class="sxs-lookup"><span data-stu-id="448be-152">In the XAML Services API, a XAML namespace declaration is represented by a <xref:System.Xaml.NamespaceDeclaration> object.</span></span> <span data-ttu-id="448be-153">如果要在代码中声明 XAML 命名空间，则调用<xref:System.Xaml.NamespaceDeclaration.%23ctor%28System.String%2CSystem.String%29>构造函数。</span><span class="sxs-lookup"><span data-stu-id="448be-153">If you are declaring a XAML namespace in code, you call the <xref:System.Xaml.NamespaceDeclaration.%23ctor%28System.String%2CSystem.String%29> constructor.</span></span> <span data-ttu-id="448be-154">和`ns``prefix`参数指定为字符串，提供这些参数的输入对应于本主题中前面提供的 XAML 命名空间标识符和 XAML 命名空间前缀的定义。</span><span class="sxs-lookup"><span data-stu-id="448be-154">The `ns` and `prefix` parameters are specified as strings, and the input to provide for these parameters corresponds to the definition of XAML namespace identifier and XAML namespace prefix as provided previously in this topic.</span></span>  
  
 <span data-ttu-id="448be-155">如果要作为 XAML 节点流的一部分或通过对 XAML 类型系统的其他访问来检查 XAML 命名<xref:System.Xaml.NamespaceDeclaration.Namespace%2A?displayProperty=nameWithType>空间信息，则报告 XAML<xref:System.Xaml.NamespaceDeclaration.Prefix%2A?displayProperty=nameWithType>命名空间标识符，并报告 XAML 命名空间前缀。</span><span class="sxs-lookup"><span data-stu-id="448be-155">If you are examining XAML namespace information as part of a XAML node stream or through other access to the XAML type system, <xref:System.Xaml.NamespaceDeclaration.Namespace%2A?displayProperty=nameWithType> reports the XAML namespace identifier, and <xref:System.Xaml.NamespaceDeclaration.Prefix%2A?displayProperty=nameWithType> reports the XAML namespace prefix.</span></span>  
  
 <span data-ttu-id="448be-156">在 XAML 节点流中，XAML 命名空间信息可以显示为 XAML 节点，该节点位于它所应用的实体之前。</span><span class="sxs-lookup"><span data-stu-id="448be-156">In a XAML node stream, the XAML namespace information can appear as a XAML node that precedes the entity to which it applies.</span></span> <span data-ttu-id="448be-157">这包括 XAML 命名空间信息在 XAML`StartObject`根元素之前的情况。</span><span class="sxs-lookup"><span data-stu-id="448be-157">This includes cases where the XAML namespace information precedes the `StartObject` of the XAML root element.</span></span> <span data-ttu-id="448be-158">有关更多信息，请参见 [Understanding XAML Node Stream Structures and Concepts](understanding-xaml-node-stream-structures-and-concepts.md)。</span><span class="sxs-lookup"><span data-stu-id="448be-158">For more information, see [Understanding XAML Node Stream Structures and Concepts](understanding-xaml-node-stream-structures-and-concepts.md).</span></span>  
  
 <span data-ttu-id="448be-159">对于许多使用 .NET XAML 服务 API 的方案，预计至少存在一个 XAML 命名空间声明，并且声明必须包含或引用 XAML 架构上下文所需的信息。</span><span class="sxs-lookup"><span data-stu-id="448be-159">For many scenarios that use .NET XAML Services API, at least one XAML namespace declaration is expected to exist, and the declaration must either contain or refer to information that is required by a XAML schema context.</span></span> <span data-ttu-id="448be-160">XAML 命名空间必须指定要加载的程序集，或者帮助解析命名空间和程序集中已加载或 XAML 架构上下文已知的特定类型。</span><span class="sxs-lookup"><span data-stu-id="448be-160">The XAML namespaces must either specify assemblies to be loaded, or assist in resolving specific types within namespaces and assemblies that are already loaded or known by the XAML schema context.</span></span>  
  
 <span data-ttu-id="448be-161">为了生成 XAML 节点流，必须通过 XAML 架构上下文提供 XAML 类型信息。</span><span class="sxs-lookup"><span data-stu-id="448be-161">In order to generate a XAML node stream, XAML type information must be available, through the XAML schema context.</span></span> <span data-ttu-id="448be-162">如果不首先确定要创建的每个节点的相关 XAML 命名空间，则无法确定 XAML 类型信息。</span><span class="sxs-lookup"><span data-stu-id="448be-162">The XAML type information cannot be determined without first determining the relevant XAML namespace for each node to create.</span></span> <span data-ttu-id="448be-163">此时，尚未创建类型实例，但 XAML 架构上下文可能需要从定义程序集和备份类型中查找信息。</span><span class="sxs-lookup"><span data-stu-id="448be-163">At this point, no instances of types are created yet, but the XAML schema context may need to look up information from the defining assembly and backing type.</span></span> <span data-ttu-id="448be-164">例如`<Party><PartyFavor/></Party>`，为了处理标记，XAML 架构上下文必须能够确定`ContentProperty`的名称`Party`和类型，因此还必须知道`Party`和`PartyFavor`的 XAML 命名空间信息。</span><span class="sxs-lookup"><span data-stu-id="448be-164">For example, in order to process the markup `<Party><PartyFavor/></Party>`, the XAML schema context must be able to determine the name and type of the `ContentProperty` of `Party`, and thus also must know the XAML namespace information for `Party` and `PartyFavor`.</span></span> <span data-ttu-id="448be-165">对于默认 XAML 架构上下文，静态反射报告在节点流中生成 XAML 类型节点所需的许多 XAML 类型系统信息。</span><span class="sxs-lookup"><span data-stu-id="448be-165">In the case of the default XAML schema context, static reflection reports much of the XAML type system information that is needed to generate XAML type nodes in the node stream.</span></span>  
  
 <span data-ttu-id="448be-166">为了从 XAML 节点流生成对象图，必须在原始标记中使用的每个 XAML 前缀中存在 XAML 命名空间声明，并记录在 XAML 节点流中。</span><span class="sxs-lookup"><span data-stu-id="448be-166">In order to generate an object graph from a XAML node stream, XAML namespace declarations must exist for each XAML prefix used in the original markup and recorded in the XAML node stream.</span></span> <span data-ttu-id="448be-167">此时，将创建实例，并发生真正的类型映射行为。</span><span class="sxs-lookup"><span data-stu-id="448be-167">At this point, instances are being created, and true type-mapping behavior occurs.</span></span>  
  
 <span data-ttu-id="448be-168">如果需要预填充 XAML 命名空间信息，在标记中未定义要使用 XAML 架构上下文的 XAML 命名空间的情况下，可以使用的一种技术是在<xref:System.Xml.XmlParserContext>中声明<xref:System.Xml.XmlReader>的 XML 命名空间声明。</span><span class="sxs-lookup"><span data-stu-id="448be-168">If you need to prepopulate XAML namespace information, in cases where the XAML namespace you intend the XAML schema context to use is not defined in the markup, one technique you can use is to declare XML namespace declarations in the <xref:System.Xml.XmlParserContext> for an <xref:System.Xml.XmlReader>.</span></span> <span data-ttu-id="448be-169">然后，使用<xref:System.Xml.XmlReader>它作为 XAML 读取器构造函数的<xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29?displayProperty=nameWithType>输入，或 。</span><span class="sxs-lookup"><span data-stu-id="448be-169">Then use that <xref:System.Xml.XmlReader> as input for a XAML reader constructor, or <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="448be-170">在 .NET XAML 服务中与 XAML 命名空间处理相关的另外两个<xref:System.Windows.Markup.XmlnsDefinitionAttribute> <xref:System.Windows.Markup.XmlnsPrefixAttribute>API 是属性和 。</span><span class="sxs-lookup"><span data-stu-id="448be-170">Two other APIs that are relevant for XAML namespace handling in .NET XAML Services are the attributes <xref:System.Windows.Markup.XmlnsDefinitionAttribute> and <xref:System.Windows.Markup.XmlnsPrefixAttribute>.</span></span> <span data-ttu-id="448be-171">这些属性适用于程序集。</span><span class="sxs-lookup"><span data-stu-id="448be-171">These attributes apply to assemblies.</span></span> <span data-ttu-id="448be-172"><xref:System.Windows.Markup.XmlnsDefinitionAttribute>XAML 架构上下文用于解释包含 URI 的任何 XAML 命名空间声明。</span><span class="sxs-lookup"><span data-stu-id="448be-172"><xref:System.Windows.Markup.XmlnsDefinitionAttribute> is used by a XAML schema context to interpret any XAML namespace declaration that includes a URI.</span></span> <span data-ttu-id="448be-173"><xref:System.Windows.Markup.XmlnsPrefixAttribute>由发出 XAML 的工具使用，以便可以使用可预测的前缀序列化特定的 XAML 命名空间。</span><span class="sxs-lookup"><span data-stu-id="448be-173"><xref:System.Windows.Markup.XmlnsPrefixAttribute> is used by tools that emit XAML so that a particular XAML namespace can be serialized with a predictable prefix.</span></span> <span data-ttu-id="448be-174">有关详细信息，请参阅[自定义类型和库的 XAML 相关 CLR 属性](clr-attributes-with-custom-types-and-libraries.md)。</span><span class="sxs-lookup"><span data-stu-id="448be-174">For more information, see [XAML-Related CLR Attributes for Custom Types and Libraries](clr-attributes-with-custom-types-and-libraries.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="448be-175">请参阅</span><span class="sxs-lookup"><span data-stu-id="448be-175">See also</span></span>

- [<span data-ttu-id="448be-176">了解 XAML 节点流结构和概念</span><span class="sxs-lookup"><span data-stu-id="448be-176">Understanding XAML Node Stream Structures and Concepts</span></span>](understanding-xaml-node-stream-structures-and-concepts.md)
